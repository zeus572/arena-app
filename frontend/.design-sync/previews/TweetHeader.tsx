import { TweetHeader } from "frontend";

// Hot Take Battle format: a compact strip with the 280-char rule and a post counter.
export const InProgress = () => <TweetHeader totalTurns={6} currentTurn={4} />;
