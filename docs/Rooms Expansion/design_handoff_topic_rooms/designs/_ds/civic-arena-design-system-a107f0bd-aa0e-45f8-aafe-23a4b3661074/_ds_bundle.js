/* @ds-bundle: {"namespace":"CivicArena","components":[{"name":"BottomTabs","sourcePath":"components/magazine/BottomTabs/BottomTabs.jsx"},{"name":"BudgetFactCard","sourcePath":"components/magazine/BudgetFactCard/BudgetFactCard.jsx"},{"name":"Button","sourcePath":"components/magazine/Button/Button.jsx"},{"name":"CampaignPostCard","sourcePath":"components/magazine/CampaignPostCard/CampaignPostCard.jsx"},{"name":"CandidateAvatar","sourcePath":"components/magazine/CandidateAvatar/CandidateAvatar.jsx"},{"name":"CaveatGrid","sourcePath":"components/taxapportionment/CaveatGrid/CaveatGrid.jsx"},{"name":"CountdownTimer","sourcePath":"components/magazine/CountdownTimer/CountdownTimer.jsx"},{"name":"CoverStory","sourcePath":"components/magazine/CoverStory/CoverStory.jsx"},{"name":"DisclaimerBadge","sourcePath":"components/magazine/DisclaimerBadge/DisclaimerBadge.jsx"},{"name":"Flyout","sourcePath":"components/magazine/Flyout/Flyout.jsx"},{"name":"HouseholdCalculator","sourcePath":"components/taxapportionment/HouseholdCalculator/HouseholdCalculator.jsx"},{"name":"MobileMenu","sourcePath":"components/magazine/MobileMenu/MobileMenu.jsx"},{"name":"NavDropdown","sourcePath":"components/magazine/NavDropdown/NavDropdown.jsx"},{"name":"PlayReadToggle","sourcePath":"components/magazine/PlayReadToggle/PlayReadToggle.jsx"},{"name":"PonderSection","sourcePath":"components/taxapportionment/PonderSection/PonderSection.jsx"},{"name":"PullQuote","sourcePath":"components/magazine/PullQuote/PullQuote.jsx"},{"name":"ScalingTable","sourcePath":"components/taxapportionment/ScalingTable/ScalingTable.jsx"},{"name":"SharePreviewCard","sourcePath":"components/magazine/SharePreviewCard/SharePreviewCard.jsx"},{"name":"SignInPrompt","sourcePath":"components/magazine/SignInPrompt/SignInPrompt.jsx"},{"name":"SplitBar","sourcePath":"components/taxapportionment/SplitBar/SplitBar.jsx"},{"name":"StateCard","sourcePath":"components/taxapportionment/StateCard/StateCard.jsx"},{"name":"StatePicker","sourcePath":"components/taxapportionment/StatePicker/StatePicker.jsx"},{"name":"Term","sourcePath":"components/magazine/Term/Term.jsx"},{"name":"ValueChip","sourcePath":"components/magazine/ValueChip/ValueChip.jsx"}],"sourceHashes":{"components/magazine/BottomTabs/BottomTabs.jsx":"de2f65b058a8","components/magazine/BottomTabs/BottomTabs.d.ts":"6adc982e7839","components/magazine/BottomTabs/BottomTabs.prompt.md":"a4477984d84c","components/magazine/BudgetFactCard/BudgetFactCard.jsx":"c626f2cc2e41","components/magazine/BudgetFactCard/BudgetFactCard.d.ts":"f167684b7c38","components/magazine/BudgetFactCard/BudgetFactCard.prompt.md":"1d316a008281","components/magazine/Button/Button.jsx":"38b5a1b491ae","components/magazine/Button/Button.d.ts":"0f813b4331d1","components/magazine/Button/Button.prompt.md":"11daeab70ec2","components/magazine/CampaignPostCard/CampaignPostCard.jsx":"2fd726e92eec","components/magazine/CampaignPostCard/CampaignPostCard.d.ts":"dfd211a65cf7","components/magazine/CampaignPostCard/CampaignPostCard.prompt.md":"30a085318a99","components/magazine/CandidateAvatar/CandidateAvatar.jsx":"36047234ee46","components/magazine/CandidateAvatar/CandidateAvatar.d.ts":"860f05f37dfa","components/magazine/CandidateAvatar/CandidateAvatar.prompt.md":"4c055231e914","components/taxapportionment/CaveatGrid/CaveatGrid.jsx":"a5b641fc9a01","components/taxapportionment/CaveatGrid/CaveatGrid.d.ts":"7bf5abf956b7","components/taxapportionment/CaveatGrid/CaveatGrid.prompt.md":"23a03f72c1c3","components/magazine/CountdownTimer/CountdownTimer.jsx":"2f7050acb13a","components/magazine/CountdownTimer/CountdownTimer.d.ts":"a203e4dedd82","components/magazine/CountdownTimer/CountdownTimer.prompt.md":"4f141aafab85","components/magazine/CoverStory/CoverStory.jsx":"b092684d250d","components/magazine/CoverStory/CoverStory.d.ts":"54ad01730223","components/magazine/CoverStory/CoverStory.prompt.md":"7ce29540160a","components/magazine/DisclaimerBadge/DisclaimerBadge.jsx":"573d578cc8b3","components/magazine/DisclaimerBadge/DisclaimerBadge.d.ts":"a3eced05bbf8","components/magazine/DisclaimerBadge/DisclaimerBadge.prompt.md":"2001af0c3bad","components/magazine/Flyout/Flyout.jsx":"0e940d723ff3","components/magazine/Flyout/Flyout.d.ts":"6e2337473805","components/magazine/Flyout/Flyout.prompt.md":"c662b0be4c2a","components/taxapportionment/HouseholdCalculator/HouseholdCalculator.jsx":"0c372f397bb4","components/taxapportionment/HouseholdCalculator/HouseholdCalculator.d.ts":"2b65f09fb74b","components/taxapportionment/HouseholdCalculator/HouseholdCalculator.prompt.md":"53c39f3017ca","components/magazine/MobileMenu/MobileMenu.jsx":"f03c05268d25","components/magazine/MobileMenu/MobileMenu.d.ts":"9fd2ba71c14d","components/magazine/MobileMenu/MobileMenu.prompt.md":"48972ab3a5dd","components/magazine/NavDropdown/NavDropdown.jsx":"ffc4cd70a42f","components/magazine/NavDropdown/NavDropdown.d.ts":"51cee99bd549","components/magazine/NavDropdown/NavDropdown.prompt.md":"800d0df21b97","components/magazine/PlayReadToggle/PlayReadToggle.jsx":"76eccc969b72","components/magazine/PlayReadToggle/PlayReadToggle.d.ts":"15a0207c8852","components/magazine/PlayReadToggle/PlayReadToggle.prompt.md":"076a408c9b63","components/taxapportionment/PonderSection/PonderSection.jsx":"ff610574c99e","components/taxapportionment/PonderSection/PonderSection.d.ts":"9abd5d151bf1","components/taxapportionment/PonderSection/PonderSection.prompt.md":"838b71ed1b06","components/magazine/PullQuote/PullQuote.jsx":"2eb33a1565ff","components/magazine/PullQuote/PullQuote.d.ts":"55e5c44cad6d","components/magazine/PullQuote/PullQuote.prompt.md":"99564b2735f2","components/taxapportionment/ScalingTable/ScalingTable.jsx":"3565e0122e85","components/taxapportionment/ScalingTable/ScalingTable.d.ts":"d8f6319c677e","components/taxapportionment/ScalingTable/ScalingTable.prompt.md":"ca5a74562cef","components/magazine/SharePreviewCard/SharePreviewCard.jsx":"b0f6de54be3d","components/magazine/SharePreviewCard/SharePreviewCard.d.ts":"81341507ff8b","components/magazine/SharePreviewCard/SharePreviewCard.prompt.md":"8a705d363830","components/magazine/SignInPrompt/SignInPrompt.jsx":"031b531752e3","components/magazine/SignInPrompt/SignInPrompt.d.ts":"1dbb642379f6","components/magazine/SignInPrompt/SignInPrompt.prompt.md":"ff467736af90","components/taxapportionment/SplitBar/SplitBar.jsx":"7d5e921ac5f2","components/taxapportionment/SplitBar/SplitBar.d.ts":"195cf4518c42","components/taxapportionment/SplitBar/SplitBar.prompt.md":"cb82fdb0fe7f","components/taxapportionment/StateCard/StateCard.jsx":"ce817b3fd6a8","components/taxapportionment/StateCard/StateCard.d.ts":"515082d07898","components/taxapportionment/StateCard/StateCard.prompt.md":"24dd7975d8d3","components/taxapportionment/StatePicker/StatePicker.jsx":"5c9930ebe667","components/taxapportionment/StatePicker/StatePicker.d.ts":"d7f514fcb33d","components/taxapportionment/StatePicker/StatePicker.prompt.md":"41333dc4aa8b","components/magazine/Term/Term.jsx":"76a2917468d9","components/magazine/Term/Term.d.ts":"976425481d56","components/magazine/Term/Term.prompt.md":"14417c932fb4","components/magazine/ValueChip/ValueChip.jsx":"2a365570bec8","components/magazine/ValueChip/ValueChip.d.ts":"745d23a3e642","components/magazine/ValueChip/ValueChip.prompt.md":"e21ee1b90483"},"inlinedExternals":["axios","clsx","cookie","lucide-react","react-router","react-router-dom","set-cookie-parser"],"builtBy":"cc-design-sync"} */
var CivicArena = (() => {
  var __create = Object.create;
  var __defProp = Object.defineProperty;
  var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
  var __getOwnPropNames = Object.getOwnPropertyNames;
  var __getProtoOf = Object.getPrototypeOf;
  var __hasOwnProp = Object.prototype.hasOwnProperty;
  var __esm = (fn, res, err) => function __init() {
    if (err) throw err[0];
    try {
      return fn && (res = (0, fn[__getOwnPropNames(fn)[0]])(fn = 0)), res;
    } catch (e) {
      throw err = [e], e;
    }
  };
  var __commonJS = (cb, mod) => function __require() {
    try {
      return mod || (0, cb[__getOwnPropNames(cb)[0]])((mod = { exports: {} }).exports, mod), mod.exports;
    } catch (e) {
      throw mod = 0, e;
    }
  };
  var __export = (target, all3) => {
    for (var name in all3)
      __defProp(target, name, { get: all3[name], enumerable: true });
  };
  var __copyProps = (to, from, except, desc) => {
    if (from && typeof from === "object" || typeof from === "function") {
      for (let key of __getOwnPropNames(from))
        if (!__hasOwnProp.call(to, key) && key !== except)
          __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
    }
    return to;
  };
  var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
    // If the importer is in node compatibility mode or this is not an ESM
    // file that has been converted to a CommonJS file using a Babel-
    // compatible transform (i.e. "__esModule" has not been set), then set
    // "default" to the CommonJS "module.exports" for node compatibility.
    isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
    mod
  ));
  var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);

  // <define:import.meta.env>
  var define_import_meta_env_default;
  var init_define_import_meta_env = __esm({
    "<define:import.meta.env>"() {
      define_import_meta_env_default = { MODE: "development", DEV: true, PROD: false, SSR: false, BASE_URL: "/" };
    }
  });

  // shim:react-shim
  var require_react_shim = __commonJS({
    "shim:react-shim"(exports, module) {
      init_define_import_meta_env();
      var R = window.React;
      function jsx(t, p, k) {
        return R.createElement(t, k === void 0 ? p : Object.assign({ key: k }, p));
      }
      module.exports = R;
      module.exports.jsx = jsx;
      module.exports.jsxs = jsx;
      module.exports.jsxDEV = jsx;
      module.exports.Fragment = R.Fragment;
    }
  });

  // shim:react-dom-shim
  var require_react_dom_shim = __commonJS({
    "shim:react-dom-shim"(exports, module) {
      init_define_import_meta_env();
      var D = window.ReactDOM;
      var n = function() {
      };
      module.exports = Object.assign({ preload: n, preinit: n, preconnect: n, prefetchDNS: n, preloadModule: n, preinitModule: n }, D);
    }
  });

  // frontend-civic/.ds-barrel.ts
  var ds_barrel_exports = {};
  __export(ds_barrel_exports, {
    BottomTabs: () => BottomTabs,
    BudgetFactCard: () => BudgetFactCard,
    Button: () => Button,
    ButtonLink: () => ButtonLink,
    CampaignPostCard: () => CampaignPostCard,
    CandidateAvatar: () => CandidateAvatar,
    CaveatGrid: () => CaveatGrid,
    CountdownTimer: () => CountdownTimer,
    CoverStory: () => CoverStory,
    DesignProvider: () => DesignProvider,
    DisclaimerBadge: () => DisclaimerBadge,
    Flyout: () => Flyout,
    HouseholdCalculator: () => HouseholdCalculator,
    MAGAZINE_HOME: () => MAGAZINE_HOME,
    MobileMenu: () => MobileMenu,
    NavDropdown: () => NavDropdown,
    PLAYER_HOME: () => PLAYER_HOME,
    PlayReadToggle: () => PlayReadToggle,
    PonderSection: () => PonderSection,
    PullQuote: () => PullQuote,
    ScalingTable: () => ScalingTable,
    SharePreviewCard: () => SharePreviewCard,
    SignInPrompt: () => SignInPrompt,
    SplitBar: () => SplitBar,
    StateCard: () => StateCard,
    StatePicker: () => StatePicker,
    Term: () => Term,
    ValueChip: () => ValueChip,
    buttonClasses: () => buttonClasses
  });
  init_define_import_meta_env();

  // frontend-civic/src/prototypes/magazine/components/Button.tsx
  init_define_import_meta_env();
  var import_react = __toESM(require_react_shim(), 1);

  // frontend-civic/node_modules/react-router/dist/development/chunk-4N6VE7H7.mjs
  init_define_import_meta_env();
  var React2 = __toESM(require_react_shim(), 1);
  var React22 = __toESM(require_react_shim(), 1);
  var React3 = __toESM(require_react_shim(), 1);
  var React4 = __toESM(require_react_shim(), 1);
  var React9 = __toESM(require_react_shim(), 1);
  var React8 = __toESM(require_react_shim(), 1);
  var React7 = __toESM(require_react_shim(), 1);
  var React6 = __toESM(require_react_shim(), 1);
  var React5 = __toESM(require_react_shim(), 1);
  var React10 = __toESM(require_react_shim(), 1);
  var React11 = __toESM(require_react_shim(), 1);
  var import_meta = {};
  function isLocation(obj) {
    return typeof obj === "object" && obj != null && "pathname" in obj && "search" in obj && "hash" in obj && "state" in obj && "key" in obj;
  }
  function createMemoryHistory(options = {}) {
    let { initialEntries = ["/"], initialIndex, v5Compat = false } = options;
    let entries;
    entries = initialEntries.map(
      (entry, index2) => createMemoryLocation(
        entry,
        typeof entry === "string" ? null : entry.state,
        index2 === 0 ? "default" : void 0,
        typeof entry === "string" ? void 0 : entry.mask
      )
    );
    let index = clampIndex(
      initialIndex == null ? entries.length - 1 : initialIndex
    );
    let action = "POP";
    let listener = null;
    function clampIndex(n) {
      return Math.min(Math.max(n, 0), entries.length - 1);
    }
    function getCurrentLocation() {
      return entries[index];
    }
    function createMemoryLocation(to, state = null, key, mask) {
      let location = createLocation(
        entries ? getCurrentLocation().pathname : "/",
        to,
        state,
        key,
        mask
      );
      warning(
        location.pathname.charAt(0) === "/",
        `relative pathnames are not supported in memory history: ${JSON.stringify(
          to
        )}`
      );
      return location;
    }
    function createHref2(to) {
      return typeof to === "string" ? to : createPath(to);
    }
    let history = {
      get index() {
        return index;
      },
      get action() {
        return action;
      },
      get location() {
        return getCurrentLocation();
      },
      createHref: createHref2,
      createURL(to) {
        return new URL(createHref2(to), "http://localhost");
      },
      encodeLocation(to) {
        let path = typeof to === "string" ? parsePath(to) : to;
        return {
          pathname: path.pathname || "",
          search: path.search || "",
          hash: path.hash || ""
        };
      },
      push(to, state) {
        action = "PUSH";
        let nextLocation = isLocation(to) ? to : createMemoryLocation(to, state);
        index += 1;
        entries.splice(index, entries.length, nextLocation);
        if (v5Compat && listener) {
          listener({ action, location: nextLocation, delta: 1 });
        }
      },
      replace(to, state) {
        action = "REPLACE";
        let nextLocation = isLocation(to) ? to : createMemoryLocation(to, state);
        entries[index] = nextLocation;
        if (v5Compat && listener) {
          listener({ action, location: nextLocation, delta: 0 });
        }
      },
      go(delta) {
        action = "POP";
        let nextIndex = clampIndex(index + delta);
        let nextLocation = entries[nextIndex];
        index = nextIndex;
        if (listener) {
          listener({ action, location: nextLocation, delta });
        }
      },
      listen(fn) {
        listener = fn;
        return () => {
          listener = null;
        };
      }
    };
    return history;
  }
  function invariant(value, message) {
    if (value === false || value === null || typeof value === "undefined") {
      throw new Error(message);
    }
  }
  function warning(cond, message) {
    if (!cond) {
      if (typeof console !== "undefined") console.warn(message);
      try {
        throw new Error(message);
      } catch (e) {
      }
    }
  }
  function createKey() {
    return Math.random().toString(36).substring(2, 10);
  }
  function createLocation(current, to, state = null, key, mask) {
    let location = {
      pathname: typeof current === "string" ? current : current.pathname,
      search: "",
      hash: "",
      ...typeof to === "string" ? parsePath(to) : to,
      state,
      // TODO: This could be cleaned up.  push/replace should probably just take
      // full Locations now and avoid the need to run through this flow at all
      // But that's a pretty big refactor to the current test suite so going to
      // keep as is for the time being and just let any incoming keys take precedence
      key: to && to.key || key || createKey(),
      mask
    };
    return location;
  }
  function createPath({
    pathname = "/",
    search = "",
    hash = ""
  }) {
    if (search && search !== "?")
      pathname += search.charAt(0) === "?" ? search : "?" + search;
    if (hash && hash !== "#")
      pathname += hash.charAt(0) === "#" ? hash : "#" + hash;
    return pathname;
  }
  function parsePath(path) {
    let parsedPath = {};
    if (path) {
      let hashIndex = path.indexOf("#");
      if (hashIndex >= 0) {
        parsedPath.hash = path.substring(hashIndex);
        path = path.substring(0, hashIndex);
      }
      let searchIndex = path.indexOf("?");
      if (searchIndex >= 0) {
        parsedPath.search = path.substring(searchIndex);
        path = path.substring(0, searchIndex);
      }
      if (path) {
        parsedPath.pathname = path;
      }
    }
    return parsedPath;
  }
  var _map;
  _map = /* @__PURE__ */ new WeakMap();
  function matchRoutes(routes, locationArg, basename = "/") {
    return matchRoutesImpl(routes, locationArg, basename, false);
  }
  function matchRoutesImpl(routes, locationArg, basename, allowPartial, precomputedBranches) {
    let location = typeof locationArg === "string" ? parsePath(locationArg) : locationArg;
    let pathname = stripBasename(location.pathname || "/", basename);
    if (pathname == null) {
      return null;
    }
    let branches = precomputedBranches ?? flattenAndRankRoutes(routes);
    let matches = null;
    let decoded = decodePath(pathname);
    for (let i = 0; matches == null && i < branches.length; ++i) {
      matches = matchRouteBranch(
        branches[i],
        decoded,
        allowPartial
      );
    }
    return matches;
  }
  function convertRouteMatchToUiMatch(match, loaderData) {
    let { route, pathname, params } = match;
    return {
      id: route.id,
      pathname,
      params,
      data: loaderData[route.id],
      loaderData: loaderData[route.id],
      handle: route.handle
    };
  }
  function flattenAndRankRoutes(routes) {
    let branches = flattenRoutes(routes);
    rankRouteBranches(branches);
    return branches;
  }
  function flattenRoutes(routes, branches = [], parentsMeta = [], parentPath = "", _hasParentOptionalSegments = false) {
    let flattenRoute = (route, index, hasParentOptionalSegments = _hasParentOptionalSegments, relativePath) => {
      let meta = {
        relativePath: relativePath === void 0 ? route.path || "" : relativePath,
        caseSensitive: route.caseSensitive === true,
        childrenIndex: index,
        route
      };
      if (meta.relativePath.startsWith("/")) {
        if (!meta.relativePath.startsWith(parentPath) && hasParentOptionalSegments) {
          return;
        }
        invariant(
          meta.relativePath.startsWith(parentPath),
          `Absolute route path "${meta.relativePath}" nested under path "${parentPath}" is not valid. An absolute child route path must start with the combined path of all its parent routes.`
        );
        meta.relativePath = meta.relativePath.slice(parentPath.length);
      }
      let path = joinPaths([parentPath, meta.relativePath]);
      let routesMeta = parentsMeta.concat(meta);
      if (route.children && route.children.length > 0) {
        invariant(
          // Our types know better, but runtime JS may not!
          // @ts-expect-error
          route.index !== true,
          `Index routes must not have child routes. Please remove all child routes from route path "${path}".`
        );
        flattenRoutes(
          route.children,
          branches,
          routesMeta,
          path,
          hasParentOptionalSegments
        );
      }
      if (route.path == null && !route.index) {
        return;
      }
      branches.push({
        path,
        score: computeScore(path, route.index),
        routesMeta
      });
    };
    routes.forEach((route, index) => {
      if (route.path === "" || !route.path?.includes("?")) {
        flattenRoute(route, index);
      } else {
        for (let exploded of explodeOptionalSegments(route.path)) {
          flattenRoute(route, index, true, exploded);
        }
      }
    });
    return branches;
  }
  function explodeOptionalSegments(path) {
    let segments = path.split("/");
    if (segments.length === 0) return [];
    let [first, ...rest] = segments;
    let isOptional = first.endsWith("?");
    let required = first.replace(/\?$/, "");
    if (rest.length === 0) {
      return isOptional ? [required, ""] : [required];
    }
    let restExploded = explodeOptionalSegments(rest.join("/"));
    let result = [];
    result.push(
      ...restExploded.map(
        (subpath) => subpath === "" ? required : [required, subpath].join("/")
      )
    );
    if (isOptional) {
      result.push(...restExploded);
    }
    return result.map(
      (exploded) => path.startsWith("/") && exploded === "" ? "/" : exploded
    );
  }
  function rankRouteBranches(branches) {
    branches.sort(
      (a, b) => a.score !== b.score ? b.score - a.score : compareIndexes(
        a.routesMeta.map((meta) => meta.childrenIndex),
        b.routesMeta.map((meta) => meta.childrenIndex)
      )
    );
  }
  var paramRe = /^:[\w-]+$/;
  var dynamicSegmentValue = 3;
  var indexRouteValue = 2;
  var emptySegmentValue = 1;
  var staticSegmentValue = 10;
  var splatPenalty = -2;
  var isSplat = (s) => s === "*";
  function computeScore(path, index) {
    let segments = path.split("/");
    let initialScore = segments.length;
    if (segments.some(isSplat)) {
      initialScore += splatPenalty;
    }
    if (index) {
      initialScore += indexRouteValue;
    }
    return segments.filter((s) => !isSplat(s)).reduce(
      (score, segment) => score + (paramRe.test(segment) ? dynamicSegmentValue : segment === "" ? emptySegmentValue : staticSegmentValue),
      initialScore
    );
  }
  function compareIndexes(a, b) {
    let siblings = a.length === b.length && a.slice(0, -1).every((n, i) => n === b[i]);
    return siblings ? (
      // If two routes are siblings, we should try to match the earlier sibling
      // first. This allows people to have fine-grained control over the matching
      // behavior by simply putting routes with identical paths in the order they
      // want them tried.
      a[a.length - 1] - b[b.length - 1]
    ) : (
      // Otherwise, it doesn't really make sense to rank non-siblings by index,
      // so they sort equally.
      0
    );
  }
  function matchRouteBranch(branch, pathname, allowPartial = false) {
    let { routesMeta } = branch;
    let matchedParams = {};
    let matchedPathname = "/";
    let matches = [];
    for (let i = 0; i < routesMeta.length; ++i) {
      let meta = routesMeta[i];
      let end = i === routesMeta.length - 1;
      let remainingPathname = matchedPathname === "/" ? pathname : pathname.slice(matchedPathname.length) || "/";
      let match = matchPath(
        { path: meta.relativePath, caseSensitive: meta.caseSensitive, end },
        remainingPathname
      );
      let route = meta.route;
      if (!match && end && allowPartial && !routesMeta[routesMeta.length - 1].route.index) {
        match = matchPath(
          {
            path: meta.relativePath,
            caseSensitive: meta.caseSensitive,
            end: false
          },
          remainingPathname
        );
      }
      if (!match) {
        return null;
      }
      Object.assign(matchedParams, match.params);
      matches.push({
        // TODO: Can this as be avoided?
        params: matchedParams,
        pathname: joinPaths([matchedPathname, match.pathname]),
        pathnameBase: normalizePathname(
          joinPaths([matchedPathname, match.pathnameBase])
        ),
        route
      });
      if (match.pathnameBase !== "/") {
        matchedPathname = joinPaths([matchedPathname, match.pathnameBase]);
      }
    }
    return matches;
  }
  function matchPath(pattern, pathname) {
    if (typeof pattern === "string") {
      pattern = { path: pattern, caseSensitive: false, end: true };
    }
    let [matcher, compiledParams] = compilePath(
      pattern.path,
      pattern.caseSensitive,
      pattern.end
    );
    let match = pathname.match(matcher);
    if (!match) return null;
    let matchedPathname = match[0];
    let pathnameBase = matchedPathname.replace(/(.)\/+$/, "$1");
    let captureGroups = match.slice(1);
    let params = compiledParams.reduce(
      (memo2, { paramName, isOptional }, index) => {
        if (paramName === "*") {
          let splatValue = captureGroups[index] || "";
          pathnameBase = matchedPathname.slice(0, matchedPathname.length - splatValue.length).replace(/(.)\/+$/, "$1");
        }
        const value = captureGroups[index];
        if (isOptional && !value) {
          memo2[paramName] = void 0;
        } else {
          memo2[paramName] = (value || "").replace(/%2F/g, "/");
        }
        return memo2;
      },
      {}
    );
    return {
      params,
      pathname: matchedPathname,
      pathnameBase,
      pattern
    };
  }
  function compilePath(path, caseSensitive = false, end = true) {
    warning(
      path === "*" || !path.endsWith("*") || path.endsWith("/*"),
      `Route path "${path}" will be treated as if it were "${path.replace(/\*$/, "/*")}" because the \`*\` character must always follow a \`/\` in the pattern. To get rid of this warning, please change the route path to "${path.replace(/\*$/, "/*")}".`
    );
    let params = [];
    let regexpSource = "^" + path.replace(/\/*\*?$/, "").replace(/^\/*/, "/").replace(/[\\.*+^${}|()[\]]/g, "\\$&").replace(
      /\/:([\w-]+)(\?)?/g,
      (match, paramName, isOptional, index, str) => {
        params.push({ paramName, isOptional: isOptional != null });
        if (isOptional) {
          let nextChar = str.charAt(index + match.length);
          if (nextChar && nextChar !== "/") {
            return "/([^\\/]*)";
          }
          return "(?:/([^\\/]*))?";
        }
        return "/([^\\/]+)";
      }
    ).replace(/\/([\w-]+)\?(\/|$)/g, "(/$1)?$2");
    if (path.endsWith("*")) {
      params.push({ paramName: "*" });
      regexpSource += path === "*" || path === "/*" ? "(.*)$" : "(?:\\/(.+)|\\/*)$";
    } else if (end) {
      regexpSource += "\\/*$";
    } else if (path !== "" && path !== "/") {
      regexpSource += "(?:(?=\\/|$))";
    } else {
    }
    let matcher = new RegExp(regexpSource, caseSensitive ? void 0 : "i");
    return [matcher, params];
  }
  function decodePath(value) {
    try {
      return value.split("/").map((v) => decodeURIComponent(v).replace(/\//g, "%2F")).join("/");
    } catch (error) {
      warning(
        false,
        `The URL path "${value}" could not be decoded because it is a malformed URL segment. This is probably due to a bad percent encoding (${error}).`
      );
      return value;
    }
  }
  function stripBasename(pathname, basename) {
    if (basename === "/") return pathname;
    if (!pathname.toLowerCase().startsWith(basename.toLowerCase())) {
      return null;
    }
    let startIndex = basename.endsWith("/") ? basename.length - 1 : basename.length;
    let nextChar = pathname.charAt(startIndex);
    if (nextChar && nextChar !== "/") {
      return null;
    }
    return pathname.slice(startIndex) || "/";
  }
  var ABSOLUTE_URL_REGEX = /^(?:[a-z][a-z0-9+.-]*:|\/\/)/i;
  function resolvePath(to, fromPathname = "/") {
    let {
      pathname: toPathname,
      search = "",
      hash = ""
    } = typeof to === "string" ? parsePath(to) : to;
    let pathname;
    if (toPathname) {
      toPathname = removeDoubleSlashes(toPathname);
      if (toPathname.startsWith("/")) {
        pathname = resolvePathname(toPathname.substring(1), "/");
      } else {
        pathname = resolvePathname(toPathname, fromPathname);
      }
    } else {
      pathname = fromPathname;
    }
    return {
      pathname,
      search: normalizeSearch(search),
      hash: normalizeHash(hash)
    };
  }
  function resolvePathname(relativePath, fromPathname) {
    let segments = removeTrailingSlash(fromPathname).split("/");
    let relativeSegments = relativePath.split("/");
    relativeSegments.forEach((segment) => {
      if (segment === "..") {
        if (segments.length > 1) segments.pop();
      } else if (segment !== ".") {
        segments.push(segment);
      }
    });
    return segments.length > 1 ? segments.join("/") : "/";
  }
  function getInvalidPathError(char, field, dest, path) {
    return `Cannot include a '${char}' character in a manually specified \`to.${field}\` field [${JSON.stringify(
      path
    )}].  Please separate it out to the \`to.${dest}\` field. Alternatively you may provide the full path as a string in <Link to="..."> and the router will parse it for you.`;
  }
  function getPathContributingMatches(matches) {
    return matches.filter(
      (match, index) => index === 0 || match.route.path && match.route.path.length > 0
    );
  }
  function getResolveToMatches(matches) {
    let pathMatches = getPathContributingMatches(matches);
    return pathMatches.map(
      (match, idx) => idx === pathMatches.length - 1 ? match.pathname : match.pathnameBase
    );
  }
  function resolveTo(toArg, routePathnames, locationPathname, isPathRelative = false) {
    let to;
    if (typeof toArg === "string") {
      to = parsePath(toArg);
    } else {
      to = { ...toArg };
      invariant(
        !to.pathname || !to.pathname.includes("?"),
        getInvalidPathError("?", "pathname", "search", to)
      );
      invariant(
        !to.pathname || !to.pathname.includes("#"),
        getInvalidPathError("#", "pathname", "hash", to)
      );
      invariant(
        !to.search || !to.search.includes("#"),
        getInvalidPathError("#", "search", "hash", to)
      );
    }
    let isEmptyPath = toArg === "" || to.pathname === "";
    let toPathname = isEmptyPath ? "/" : to.pathname;
    let from;
    if (toPathname == null) {
      from = locationPathname;
    } else {
      let routePathnameIndex = routePathnames.length - 1;
      if (!isPathRelative && toPathname.startsWith("..")) {
        let toSegments = toPathname.split("/");
        while (toSegments[0] === "..") {
          toSegments.shift();
          routePathnameIndex -= 1;
        }
        to.pathname = toSegments.join("/");
      }
      from = routePathnameIndex >= 0 ? routePathnames[routePathnameIndex] : "/";
    }
    let path = resolvePath(to, from);
    let hasExplicitTrailingSlash = toPathname && toPathname !== "/" && toPathname.endsWith("/");
    let hasCurrentTrailingSlash = (isEmptyPath || toPathname === ".") && locationPathname.endsWith("/");
    if (!path.pathname.endsWith("/") && (hasExplicitTrailingSlash || hasCurrentTrailingSlash)) {
      path.pathname += "/";
    }
    return path;
  }
  var removeDoubleSlashes = (path) => path.replace(/\/\/+/g, "/");
  var joinPaths = (paths) => removeDoubleSlashes(paths.join("/"));
  var removeTrailingSlash = (path) => path.replace(/\/+$/, "");
  var normalizePathname = (pathname) => removeTrailingSlash(pathname).replace(/^\/*/, "/");
  var normalizeSearch = (search) => !search || search === "?" ? "" : search.startsWith("?") ? search : "?" + search;
  var normalizeHash = (hash) => !hash || hash === "#" ? "" : hash.startsWith("#") ? hash : "#" + hash;
  var ErrorResponseImpl = class {
    constructor(status, statusText, data2, internal = false) {
      this.status = status;
      this.statusText = statusText || "";
      this.internal = internal;
      if (data2 instanceof Error) {
        this.data = data2.toString();
        this.error = data2;
      } else {
        this.data = data2;
      }
    }
  };
  function isRouteErrorResponse(error) {
    return error != null && typeof error.status === "number" && typeof error.statusText === "string" && typeof error.internal === "boolean" && "data" in error;
  }
  function getRoutePattern(matches) {
    let parts = matches.map((m) => m.route.path).filter(Boolean);
    return joinPaths(parts) || "/";
  }
  var isBrowser = typeof window !== "undefined" && typeof window.document !== "undefined" && typeof window.document.createElement !== "undefined";
  function parseToInfo(_to, basename) {
    let to = _to;
    if (typeof to !== "string" || !ABSOLUTE_URL_REGEX.test(to)) {
      return {
        absoluteURL: void 0,
        isExternal: false,
        to
      };
    }
    let absoluteURL = to;
    let isExternal = false;
    if (isBrowser) {
      try {
        let currentUrl = new URL(window.location.href);
        let targetUrl = to.startsWith("//") ? new URL(currentUrl.protocol + to) : new URL(to);
        let path = stripBasename(targetUrl.pathname, basename);
        if (targetUrl.origin === currentUrl.origin && path != null) {
          to = path + targetUrl.search + targetUrl.hash;
        } else {
          isExternal = true;
        }
      } catch (e) {
        warning(
          false,
          `<Link to="${to}"> contains an invalid URL which will probably break when clicked - please update to a valid URL path.`
        );
      }
    }
    return {
      absoluteURL,
      isExternal,
      to
    };
  }
  var objectProtoNames = Object.getOwnPropertyNames(Object.prototype).sort().join("\0");
  var validMutationMethodsArr = [
    "POST",
    "PUT",
    "PATCH",
    "DELETE"
  ];
  var validMutationMethods = new Set(
    validMutationMethodsArr
  );
  var validRequestMethodsArr = [
    "GET",
    ...validMutationMethodsArr
  ];
  var validRequestMethods = new Set(validRequestMethodsArr);
  var _routes;
  var _branches;
  var _hmrRoutes;
  var _hmrBranches;
  _routes = /* @__PURE__ */ new WeakMap();
  _branches = /* @__PURE__ */ new WeakMap();
  _hmrRoutes = /* @__PURE__ */ new WeakMap();
  _hmrBranches = /* @__PURE__ */ new WeakMap();
  var DataRouterContext = React2.createContext(null);
  DataRouterContext.displayName = "DataRouter";
  var DataRouterStateContext = React2.createContext(null);
  DataRouterStateContext.displayName = "DataRouterState";
  var RSCRouterContext = React2.createContext(false);
  function useIsRSCRouterContext() {
    return React2.useContext(RSCRouterContext);
  }
  var ViewTransitionContext = React2.createContext({
    isTransitioning: false
  });
  ViewTransitionContext.displayName = "ViewTransition";
  var FetchersContext = React2.createContext(
    /* @__PURE__ */ new Map()
  );
  FetchersContext.displayName = "Fetchers";
  var AwaitContext = React2.createContext(null);
  AwaitContext.displayName = "Await";
  var NavigationContext = React2.createContext(
    null
  );
  NavigationContext.displayName = "Navigation";
  var LocationContext = React2.createContext(
    null
  );
  LocationContext.displayName = "Location";
  var RouteContext = React2.createContext({
    outlet: null,
    matches: [],
    isDataRoute: false
  });
  RouteContext.displayName = "Route";
  var RouteErrorContext = React2.createContext(null);
  RouteErrorContext.displayName = "RouteError";
  var ENABLE_DEV_WARNINGS = true;
  var ERROR_DIGEST_BASE = "REACT_ROUTER_ERROR";
  var ERROR_DIGEST_REDIRECT = "REDIRECT";
  var ERROR_DIGEST_ROUTE_ERROR_RESPONSE = "ROUTE_ERROR_RESPONSE";
  function decodeRedirectErrorDigest(digest) {
    if (digest.startsWith(`${ERROR_DIGEST_BASE}:${ERROR_DIGEST_REDIRECT}:{`)) {
      try {
        let parsed = JSON.parse(digest.slice(28));
        if (typeof parsed === "object" && parsed && typeof parsed.status === "number" && typeof parsed.statusText === "string" && typeof parsed.location === "string" && typeof parsed.reloadDocument === "boolean" && typeof parsed.replace === "boolean") {
          return parsed;
        }
      } catch {
      }
    }
  }
  function decodeRouteErrorResponseDigest(digest) {
    if (digest.startsWith(
      `${ERROR_DIGEST_BASE}:${ERROR_DIGEST_ROUTE_ERROR_RESPONSE}:{`
    )) {
      try {
        let parsed = JSON.parse(digest.slice(40));
        if (typeof parsed === "object" && parsed && typeof parsed.status === "number" && typeof parsed.statusText === "string") {
          return new ErrorResponseImpl(
            parsed.status,
            parsed.statusText,
            parsed.data
          );
        }
      } catch {
      }
    }
  }
  function useHref(to, { relative } = {}) {
    invariant(
      useInRouterContext(),
      // TODO: This error is probably because they somehow have 2 versions of the
      // router loaded. We can help them understand how to avoid that.
      `useHref() may be used only in the context of a <Router> component.`
    );
    let { basename, navigator: navigator2 } = React22.useContext(NavigationContext);
    let { hash, pathname, search } = useResolvedPath(to, { relative });
    let joinedPathname = pathname;
    if (basename !== "/") {
      joinedPathname = pathname === "/" ? basename : joinPaths([basename, pathname]);
    }
    return navigator2.createHref({ pathname: joinedPathname, search, hash });
  }
  function useInRouterContext() {
    return React22.useContext(LocationContext) != null;
  }
  function useLocation() {
    invariant(
      useInRouterContext(),
      // TODO: This error is probably because they somehow have 2 versions of the
      // router loaded. We can help them understand how to avoid that.
      `useLocation() may be used only in the context of a <Router> component.`
    );
    return React22.useContext(LocationContext).location;
  }
  var navigateEffectWarning = `You should call navigate() in a React.useEffect(), not when your component is first rendered.`;
  function useIsomorphicLayoutEffect(cb) {
    let isStatic = React22.useContext(NavigationContext).static;
    if (!isStatic) {
      React22.useLayoutEffect(cb);
    }
  }
  function useNavigate() {
    let { isDataRoute } = React22.useContext(RouteContext);
    return isDataRoute ? useNavigateStable() : useNavigateUnstable();
  }
  function useNavigateUnstable() {
    invariant(
      useInRouterContext(),
      // TODO: This error is probably because they somehow have 2 versions of the
      // router loaded. We can help them understand how to avoid that.
      `useNavigate() may be used only in the context of a <Router> component.`
    );
    let dataRouterContext = React22.useContext(DataRouterContext);
    let { basename, navigator: navigator2 } = React22.useContext(NavigationContext);
    let { matches } = React22.useContext(RouteContext);
    let { pathname: locationPathname } = useLocation();
    let routePathnamesJson = JSON.stringify(getResolveToMatches(matches));
    let activeRef = React22.useRef(false);
    useIsomorphicLayoutEffect(() => {
      activeRef.current = true;
    });
    let navigate = React22.useCallback(
      (to, options = {}) => {
        warning(activeRef.current, navigateEffectWarning);
        if (!activeRef.current) return;
        if (typeof to === "number") {
          navigator2.go(to);
          return;
        }
        let path = resolveTo(
          to,
          JSON.parse(routePathnamesJson),
          locationPathname,
          options.relative === "path"
        );
        if (dataRouterContext == null && basename !== "/") {
          path.pathname = path.pathname === "/" ? basename : joinPaths([basename, path.pathname]);
        }
        (!!options.replace ? navigator2.replace : navigator2.push)(
          path,
          options.state,
          options
        );
      },
      [
        basename,
        navigator2,
        routePathnamesJson,
        locationPathname,
        dataRouterContext
      ]
    );
    return navigate;
  }
  var OutletContext = React22.createContext(null);
  function useResolvedPath(to, { relative } = {}) {
    let { matches } = React22.useContext(RouteContext);
    let { pathname: locationPathname } = useLocation();
    let routePathnamesJson = JSON.stringify(getResolveToMatches(matches));
    return React22.useMemo(
      () => resolveTo(
        to,
        JSON.parse(routePathnamesJson),
        locationPathname,
        relative === "path"
      ),
      [to, routePathnamesJson, locationPathname, relative]
    );
  }
  function useRoutesImpl(routes, locationArg, dataRouterOpts) {
    invariant(
      useInRouterContext(),
      // TODO: This error is probably because they somehow have 2 versions of the
      // router loaded. We can help them understand how to avoid that.
      `useRoutes() may be used only in the context of a <Router> component.`
    );
    let { navigator: navigator2 } = React22.useContext(NavigationContext);
    let { matches: parentMatches } = React22.useContext(RouteContext);
    let routeMatch = parentMatches[parentMatches.length - 1];
    let parentParams = routeMatch ? routeMatch.params : {};
    let parentPathname = routeMatch ? routeMatch.pathname : "/";
    let parentPathnameBase = routeMatch ? routeMatch.pathnameBase : "/";
    let parentRoute = routeMatch && routeMatch.route;
    if (ENABLE_DEV_WARNINGS) {
      let parentPath = parentRoute && parentRoute.path || "";
      warningOnce(
        parentPathname,
        !parentRoute || parentPath.endsWith("*") || parentPath.endsWith("*?"),
        `You rendered descendant <Routes> (or called \`useRoutes()\`) at "${parentPathname}" (under <Route path="${parentPath}">) but the parent route path has no trailing "*". This means if you navigate deeper, the parent won't match anymore and therefore the child routes will never render.

Please change the parent <Route path="${parentPath}"> to <Route path="${parentPath === "/" ? "*" : `${parentPath}/*`}">.`
      );
    }
    let locationFromContext = useLocation();
    let location;
    if (locationArg) {
      let parsedLocationArg = typeof locationArg === "string" ? parsePath(locationArg) : locationArg;
      invariant(
        parentPathnameBase === "/" || parsedLocationArg.pathname?.startsWith(parentPathnameBase),
        `When overriding the location using \`<Routes location>\` or \`useRoutes(routes, location)\`, the location pathname must begin with the portion of the URL pathname that was matched by all parent routes. The current pathname base is "${parentPathnameBase}" but pathname "${parsedLocationArg.pathname}" was given in the \`location\` prop.`
      );
      location = parsedLocationArg;
    } else {
      location = locationFromContext;
    }
    let pathname = location.pathname || "/";
    let remainingPathname = pathname;
    if (parentPathnameBase !== "/") {
      let parentSegments = parentPathnameBase.replace(/^\//, "").split("/");
      let segments = pathname.replace(/^\//, "").split("/");
      remainingPathname = "/" + segments.slice(parentSegments.length).join("/");
    }
    let matches = dataRouterOpts && dataRouterOpts.state.matches.length ? (
      // If we're in a data router, use the matches we've already identified but ensure
      // we have the latest route instances from the manifest in case elements have changed
      dataRouterOpts.state.matches.map(
        (m) => Object.assign(m, {
          route: dataRouterOpts.manifest[m.route.id] || m.route
        })
      )
    ) : matchRoutes(routes, { pathname: remainingPathname });
    if (ENABLE_DEV_WARNINGS) {
      warning(
        parentRoute || matches != null,
        `No routes matched location "${location.pathname}${location.search}${location.hash}" `
      );
      warning(
        matches == null || matches[matches.length - 1].route.element !== void 0 || matches[matches.length - 1].route.Component !== void 0 || matches[matches.length - 1].route.lazy !== void 0,
        `Matched leaf route at location "${location.pathname}${location.search}${location.hash}" does not have an element or Component. This means it will render an <Outlet /> with a null value by default resulting in an "empty" page.`
      );
    }
    let renderedMatches = _renderMatches(
      matches && matches.map(
        (match) => Object.assign({}, match, {
          params: Object.assign({}, parentParams, match.params),
          pathname: joinPaths([
            parentPathnameBase,
            // Re-encode pathnames that were decoded inside matchRoutes.
            // Pre-encode `%`, `?` and `#` ahead of `encodeLocation` because it uses
            // `new URL()` internally and we need to prevent it from treating
            // them as separators
            navigator2.encodeLocation ? navigator2.encodeLocation(
              match.pathname.replace(/%/g, "%25").replace(/\?/g, "%3F").replace(/#/g, "%23")
            ).pathname : match.pathname
          ]),
          pathnameBase: match.pathnameBase === "/" ? parentPathnameBase : joinPaths([
            parentPathnameBase,
            // Re-encode pathnames that were decoded inside matchRoutes
            // Pre-encode `%`, `?` and `#` ahead of `encodeLocation` because it uses
            // `new URL()` internally and we need to prevent it from treating
            // them as separators
            navigator2.encodeLocation ? navigator2.encodeLocation(
              match.pathnameBase.replace(/%/g, "%25").replace(/\?/g, "%3F").replace(/#/g, "%23")
            ).pathname : match.pathnameBase
          ])
        })
      ),
      parentMatches,
      dataRouterOpts
    );
    if (locationArg && renderedMatches) {
      return /* @__PURE__ */ React22.createElement(
        LocationContext.Provider,
        {
          value: {
            location: {
              pathname: "/",
              search: "",
              hash: "",
              state: null,
              key: "default",
              mask: void 0,
              ...location
            },
            navigationType: "POP"
            /* Pop */
          }
        },
        renderedMatches
      );
    }
    return renderedMatches;
  }
  function DefaultErrorComponent() {
    let error = useRouteError();
    let message = isRouteErrorResponse(error) ? `${error.status} ${error.statusText}` : error instanceof Error ? error.message : JSON.stringify(error);
    let stack = error instanceof Error ? error.stack : null;
    let lightgrey = "rgba(200,200,200, 0.5)";
    let preStyles = { padding: "0.5rem", backgroundColor: lightgrey };
    let codeStyles = { padding: "2px 4px", backgroundColor: lightgrey };
    let devInfo = null;
    if (ENABLE_DEV_WARNINGS) {
      console.error(
        "Error handled by React Router default ErrorBoundary:",
        error
      );
      devInfo = /* @__PURE__ */ React22.createElement(React22.Fragment, null, /* @__PURE__ */ React22.createElement("p", null, "\u{1F4BF} Hey developer \u{1F44B}"), /* @__PURE__ */ React22.createElement("p", null, "You can provide a way better UX than this when your app throws errors by providing your own ", /* @__PURE__ */ React22.createElement("code", { style: codeStyles }, "ErrorBoundary"), " or", " ", /* @__PURE__ */ React22.createElement("code", { style: codeStyles }, "errorElement"), " prop on your route."));
    }
    return /* @__PURE__ */ React22.createElement(React22.Fragment, null, /* @__PURE__ */ React22.createElement("h2", null, "Unexpected Application Error!"), /* @__PURE__ */ React22.createElement("h3", { style: { fontStyle: "italic" } }, message), stack ? /* @__PURE__ */ React22.createElement("pre", { style: preStyles }, stack) : null, devInfo);
  }
  var defaultErrorElement = /* @__PURE__ */ React22.createElement(DefaultErrorComponent, null);
  var RenderErrorBoundary = class extends React22.Component {
    constructor(props) {
      super(props);
      this.state = {
        location: props.location,
        revalidation: props.revalidation,
        error: props.error
      };
    }
    static getDerivedStateFromError(error) {
      return { error };
    }
    static getDerivedStateFromProps(props, state) {
      if (state.location !== props.location || state.revalidation !== "idle" && props.revalidation === "idle") {
        return {
          error: props.error,
          location: props.location,
          revalidation: props.revalidation
        };
      }
      return {
        error: props.error !== void 0 ? props.error : state.error,
        location: state.location,
        revalidation: props.revalidation || state.revalidation
      };
    }
    componentDidCatch(error, errorInfo) {
      if (this.props.onError) {
        this.props.onError(error, errorInfo);
      } else {
        console.error(
          "React Router caught the following error during render",
          error
        );
      }
    }
    render() {
      let error = this.state.error;
      if (this.context && typeof error === "object" && error && "digest" in error && typeof error.digest === "string") {
        const decoded = decodeRouteErrorResponseDigest(error.digest);
        if (decoded) error = decoded;
      }
      let result = error !== void 0 ? /* @__PURE__ */ React22.createElement(RouteContext.Provider, { value: this.props.routeContext }, /* @__PURE__ */ React22.createElement(
        RouteErrorContext.Provider,
        {
          value: error,
          children: this.props.component
        }
      )) : this.props.children;
      if (this.context) {
        return /* @__PURE__ */ React22.createElement(RSCErrorHandler, { error }, result);
      }
      return result;
    }
  };
  RenderErrorBoundary.contextType = RSCRouterContext;
  var errorRedirectHandledMap = /* @__PURE__ */ new WeakMap();
  function RSCErrorHandler({
    children,
    error
  }) {
    let { basename } = React22.useContext(NavigationContext);
    if (typeof error === "object" && error && "digest" in error && typeof error.digest === "string") {
      let redirect2 = decodeRedirectErrorDigest(error.digest);
      if (redirect2) {
        let existingRedirect = errorRedirectHandledMap.get(error);
        if (existingRedirect) throw existingRedirect;
        let parsed = parseToInfo(redirect2.location, basename);
        if (isBrowser && !errorRedirectHandledMap.get(error)) {
          if (parsed.isExternal || redirect2.reloadDocument) {
            window.location.href = parsed.absoluteURL || parsed.to;
          } else {
            const redirectPromise = Promise.resolve().then(
              () => window.__reactRouterDataRouter.navigate(parsed.to, {
                replace: redirect2.replace
              })
            );
            errorRedirectHandledMap.set(error, redirectPromise);
            throw redirectPromise;
          }
        }
        return /* @__PURE__ */ React22.createElement(
          "meta",
          {
            httpEquiv: "refresh",
            content: `0;url=${parsed.absoluteURL || parsed.to}`
          }
        );
      }
    }
    return children;
  }
  function RenderedRoute({ routeContext, match, children }) {
    let dataRouterContext = React22.useContext(DataRouterContext);
    if (dataRouterContext && dataRouterContext.static && dataRouterContext.staticContext && (match.route.errorElement || match.route.ErrorBoundary)) {
      dataRouterContext.staticContext._deepestRenderedBoundaryId = match.route.id;
    }
    return /* @__PURE__ */ React22.createElement(RouteContext.Provider, { value: routeContext }, children);
  }
  function _renderMatches(matches, parentMatches = [], dataRouterOpts) {
    let dataRouterState = dataRouterOpts?.state;
    if (matches == null) {
      if (!dataRouterState) {
        return null;
      }
      if (dataRouterState.errors) {
        matches = dataRouterState.matches;
      } else if (parentMatches.length === 0 && !dataRouterState.initialized && dataRouterState.matches.length > 0) {
        matches = dataRouterState.matches;
      } else {
        return null;
      }
    }
    let renderedMatches = matches;
    let errors = dataRouterState?.errors;
    if (errors != null) {
      let errorIndex = renderedMatches.findIndex(
        (m) => m.route.id && errors?.[m.route.id] !== void 0
      );
      invariant(
        errorIndex >= 0,
        `Could not find a matching route for errors on route IDs: ${Object.keys(
          errors
        ).join(",")}`
      );
      renderedMatches = renderedMatches.slice(
        0,
        Math.min(renderedMatches.length, errorIndex + 1)
      );
    }
    let renderFallback = false;
    let fallbackIndex = -1;
    if (dataRouterOpts && dataRouterState) {
      renderFallback = dataRouterState.renderFallback;
      for (let i = 0; i < renderedMatches.length; i++) {
        let match = renderedMatches[i];
        if (match.route.HydrateFallback || match.route.hydrateFallbackElement) {
          fallbackIndex = i;
        }
        if (match.route.id) {
          let { loaderData, errors: errors2 } = dataRouterState;
          let needsToRunLoader = match.route.loader && !loaderData.hasOwnProperty(match.route.id) && (!errors2 || errors2[match.route.id] === void 0);
          if (match.route.lazy || needsToRunLoader) {
            if (dataRouterOpts.isStatic) {
              renderFallback = true;
            }
            if (fallbackIndex >= 0) {
              renderedMatches = renderedMatches.slice(0, fallbackIndex + 1);
            } else {
              renderedMatches = [renderedMatches[0]];
            }
            break;
          }
        }
      }
    }
    let onErrorHandler = dataRouterOpts?.onError;
    let onError = dataRouterState && onErrorHandler ? (error, errorInfo) => {
      onErrorHandler(error, {
        location: dataRouterState.location,
        params: dataRouterState.matches?.[0]?.params ?? {},
        pattern: getRoutePattern(dataRouterState.matches),
        errorInfo
      });
    } : void 0;
    return renderedMatches.reduceRight(
      (outlet, match, index) => {
        let error;
        let shouldRenderHydrateFallback = false;
        let errorElement = null;
        let hydrateFallbackElement = null;
        if (dataRouterState) {
          error = errors && match.route.id ? errors[match.route.id] : void 0;
          errorElement = match.route.errorElement || defaultErrorElement;
          if (renderFallback) {
            if (fallbackIndex < 0 && index === 0) {
              warningOnce(
                "route-fallback",
                false,
                "No `HydrateFallback` element provided to render during initial hydration"
              );
              shouldRenderHydrateFallback = true;
              hydrateFallbackElement = null;
            } else if (fallbackIndex === index) {
              shouldRenderHydrateFallback = true;
              hydrateFallbackElement = match.route.hydrateFallbackElement || null;
            }
          }
        }
        let matches2 = parentMatches.concat(renderedMatches.slice(0, index + 1));
        let getChildren = () => {
          let children;
          if (error) {
            children = errorElement;
          } else if (shouldRenderHydrateFallback) {
            children = hydrateFallbackElement;
          } else if (match.route.Component) {
            children = /* @__PURE__ */ React22.createElement(match.route.Component, null);
          } else if (match.route.element) {
            children = match.route.element;
          } else {
            children = outlet;
          }
          return /* @__PURE__ */ React22.createElement(
            RenderedRoute,
            {
              match,
              routeContext: {
                outlet,
                matches: matches2,
                isDataRoute: dataRouterState != null
              },
              children
            }
          );
        };
        return dataRouterState && (match.route.ErrorBoundary || match.route.errorElement || index === 0) ? /* @__PURE__ */ React22.createElement(
          RenderErrorBoundary,
          {
            location: dataRouterState.location,
            revalidation: dataRouterState.revalidation,
            component: errorElement,
            error,
            children: getChildren(),
            routeContext: { outlet: null, matches: matches2, isDataRoute: true },
            onError
          }
        ) : getChildren();
      },
      null
    );
  }
  function getDataRouterConsoleError(hookName) {
    return `${hookName} must be used within a data router.  See https://reactrouter.com/en/main/routers/picking-a-router.`;
  }
  function useDataRouterContext(hookName) {
    let ctx = React22.useContext(DataRouterContext);
    invariant(ctx, getDataRouterConsoleError(hookName));
    return ctx;
  }
  function useDataRouterState(hookName) {
    let state = React22.useContext(DataRouterStateContext);
    invariant(state, getDataRouterConsoleError(hookName));
    return state;
  }
  function useRouteContext(hookName) {
    let route = React22.useContext(RouteContext);
    invariant(route, getDataRouterConsoleError(hookName));
    return route;
  }
  function useCurrentRouteId(hookName) {
    let route = useRouteContext(hookName);
    let thisRoute = route.matches[route.matches.length - 1];
    invariant(
      thisRoute.route.id,
      `${hookName} can only be used on routes that contain a unique "id"`
    );
    return thisRoute.route.id;
  }
  function useRouteId() {
    return useCurrentRouteId(
      "useRouteId"
      /* UseRouteId */
    );
  }
  function useNavigation() {
    let state = useDataRouterState(
      "useNavigation"
      /* UseNavigation */
    );
    return React22.useMemo(() => {
      let { matches, historyAction, ...rest } = state.navigation;
      return rest;
    }, [state.navigation]);
  }
  function useMatches() {
    let { matches, loaderData } = useDataRouterState(
      "useMatches"
      /* UseMatches */
    );
    return React22.useMemo(
      () => matches.map((m) => convertRouteMatchToUiMatch(m, loaderData)),
      [matches, loaderData]
    );
  }
  function useRouteError() {
    let error = React22.useContext(RouteErrorContext);
    let state = useDataRouterState(
      "useRouteError"
      /* UseRouteError */
    );
    let routeId = useCurrentRouteId(
      "useRouteError"
      /* UseRouteError */
    );
    if (error !== void 0) {
      return error;
    }
    return state.errors?.[routeId];
  }
  function useNavigateStable() {
    let { router } = useDataRouterContext(
      "useNavigate"
      /* UseNavigateStable */
    );
    let id = useCurrentRouteId(
      "useNavigate"
      /* UseNavigateStable */
    );
    let activeRef = React22.useRef(false);
    useIsomorphicLayoutEffect(() => {
      activeRef.current = true;
    });
    let navigate = React22.useCallback(
      async (to, options = {}) => {
        warning(activeRef.current, navigateEffectWarning);
        if (!activeRef.current) return;
        if (typeof to === "number") {
          await router.navigate(to);
        } else {
          await router.navigate(to, { fromRouteId: id, ...options });
        }
      },
      [router, id]
    );
    return navigate;
  }
  var alreadyWarned = {};
  function warningOnce(key, cond, message) {
    if (!cond && !alreadyWarned[key]) {
      alreadyWarned[key] = true;
      warning(false, message);
    }
  }
  var USE_OPTIMISTIC = "useOptimistic";
  var useOptimisticImpl = React3[USE_OPTIMISTIC];
  var MemoizedDataRoutes = React3.memo(DataRoutes2);
  function DataRoutes2({
    routes,
    manifest,
    future,
    state,
    isStatic,
    onError
  }) {
    return useRoutesImpl(routes, void 0, {
      manifest,
      state,
      isStatic,
      onError,
      future
    });
  }
  function MemoryRouter({
    basename,
    children,
    initialEntries,
    initialIndex,
    useTransitions
  }) {
    let historyRef = React3.useRef();
    if (historyRef.current == null) {
      historyRef.current = createMemoryHistory({
        initialEntries,
        initialIndex,
        v5Compat: true
      });
    }
    let history = historyRef.current;
    let [state, setStateImpl] = React3.useState({
      action: history.action,
      location: history.location
    });
    let setState = React3.useCallback(
      (newState) => {
        if (useTransitions === false) {
          setStateImpl(newState);
        } else {
          React3.startTransition(() => setStateImpl(newState));
        }
      },
      [useTransitions]
    );
    React3.useLayoutEffect(() => history.listen(setState), [history, setState]);
    return /* @__PURE__ */ React3.createElement(
      Router,
      {
        basename,
        children,
        location: state.location,
        navigationType: state.action,
        navigator: history,
        useTransitions
      }
    );
  }
  function Router({
    basename: basenameProp = "/",
    children = null,
    location: locationProp,
    navigationType = "POP",
    navigator: navigator2,
    static: staticProp = false,
    useTransitions
  }) {
    invariant(
      !useInRouterContext(),
      `You cannot render a <Router> inside another <Router>. You should never have more than one in your app.`
    );
    let basename = basenameProp.replace(/^\/*/, "/");
    let navigationContext = React3.useMemo(
      () => ({
        basename,
        navigator: navigator2,
        static: staticProp,
        useTransitions,
        future: {}
      }),
      [basename, navigator2, staticProp, useTransitions]
    );
    if (typeof locationProp === "string") {
      locationProp = parsePath(locationProp);
    }
    let {
      pathname = "/",
      search = "",
      hash = "",
      state = null,
      key = "default",
      mask
    } = locationProp;
    let locationContext = React3.useMemo(() => {
      let trailingPathname = stripBasename(pathname, basename);
      if (trailingPathname == null) {
        return null;
      }
      return {
        location: {
          pathname: trailingPathname,
          search,
          hash,
          state,
          key,
          mask
        },
        navigationType
      };
    }, [basename, pathname, search, hash, state, key, navigationType, mask]);
    warning(
      locationContext != null,
      `<Router basename="${basename}"> is not able to match the URL "${pathname}${search}${hash}" because it does not start with the basename, so the <Router> won't render anything.`
    );
    if (locationContext == null) {
      return null;
    }
    return /* @__PURE__ */ React3.createElement(NavigationContext.Provider, { value: navigationContext }, /* @__PURE__ */ React3.createElement(LocationContext.Provider, { children, value: locationContext }));
  }
  var defaultMethod = "get";
  var defaultEncType = "application/x-www-form-urlencoded";
  function isHtmlElement(object) {
    return typeof HTMLElement !== "undefined" && object instanceof HTMLElement;
  }
  function isButtonElement(object) {
    return isHtmlElement(object) && object.tagName.toLowerCase() === "button";
  }
  function isFormElement(object) {
    return isHtmlElement(object) && object.tagName.toLowerCase() === "form";
  }
  function isInputElement(object) {
    return isHtmlElement(object) && object.tagName.toLowerCase() === "input";
  }
  function isModifiedEvent(event) {
    return !!(event.metaKey || event.altKey || event.ctrlKey || event.shiftKey);
  }
  function shouldProcessLinkClick(event, target) {
    return event.button === 0 && // Ignore everything but left clicks
    (!target || target === "_self") && // Let browser handle "target=_blank" etc.
    !isModifiedEvent(event);
  }
  var _formDataSupportsSubmitter = null;
  function isFormDataSubmitterSupported() {
    if (_formDataSupportsSubmitter === null) {
      try {
        new FormData(
          document.createElement("form"),
          // @ts-expect-error if FormData supports the submitter parameter, this will throw
          0
        );
        _formDataSupportsSubmitter = false;
      } catch (e) {
        _formDataSupportsSubmitter = true;
      }
    }
    return _formDataSupportsSubmitter;
  }
  var supportedFormEncTypes = /* @__PURE__ */ new Set([
    "application/x-www-form-urlencoded",
    "multipart/form-data",
    "text/plain"
  ]);
  function getFormEncType(encType) {
    if (encType != null && !supportedFormEncTypes.has(encType)) {
      warning(
        false,
        `"${encType}" is not a valid \`encType\` for \`<Form>\`/\`<fetcher.Form>\` and will default to "${defaultEncType}"`
      );
      return null;
    }
    return encType;
  }
  function getFormSubmissionInfo(target, basename) {
    let method;
    let action;
    let encType;
    let formData;
    let body;
    if (isFormElement(target)) {
      let attr = target.getAttribute("action");
      action = attr ? stripBasename(attr, basename) : null;
      method = target.getAttribute("method") || defaultMethod;
      encType = getFormEncType(target.getAttribute("enctype")) || defaultEncType;
      formData = new FormData(target);
    } else if (isButtonElement(target) || isInputElement(target) && (target.type === "submit" || target.type === "image")) {
      let form = target.form;
      if (form == null) {
        throw new Error(
          `Cannot submit a <button> or <input type="submit"> without a <form>`
        );
      }
      let attr = target.getAttribute("formaction") || form.getAttribute("action");
      action = attr ? stripBasename(attr, basename) : null;
      method = target.getAttribute("formmethod") || form.getAttribute("method") || defaultMethod;
      encType = getFormEncType(target.getAttribute("formenctype")) || getFormEncType(form.getAttribute("enctype")) || defaultEncType;
      formData = new FormData(form, target);
      if (!isFormDataSubmitterSupported()) {
        let { name, type, value } = target;
        if (type === "image") {
          let prefix = name ? `${name}.` : "";
          formData.append(`${prefix}x`, "0");
          formData.append(`${prefix}y`, "0");
        } else if (name) {
          formData.append(name, value);
        }
      }
    } else if (isHtmlElement(target)) {
      throw new Error(
        `Cannot submit element that is not <form>, <button>, or <input type="submit|image">`
      );
    } else {
      method = defaultMethod;
      action = null;
      encType = defaultEncType;
      body = target;
    }
    if (formData && encType === "text/plain") {
      body = formData;
      formData = void 0;
    }
    return { action, method: method.toLowerCase(), encType, formData, body };
  }
  var objectProtoNames2 = Object.getOwnPropertyNames(Object.prototype).sort().join("\0");
  var ESCAPE_LOOKUP = {
    "&": "\\u0026",
    ">": "\\u003e",
    "<": "\\u003c",
    "\u2028": "\\u2028",
    "\u2029": "\\u2029"
  };
  var ESCAPE_REGEX = /[&><\u2028\u2029]/g;
  function escapeHtml(html) {
    return html.replace(ESCAPE_REGEX, (match) => ESCAPE_LOOKUP[match]);
  }
  function invariant2(value, message) {
    if (value === false || value === null || typeof value === "undefined") {
      throw new Error(message);
    }
  }
  function singleFetchUrl(reqUrl, basename, trailingSlashAware, extension) {
    let url = typeof reqUrl === "string" ? new URL(
      reqUrl,
      // This can be called during the SSR flow via PrefetchPageLinksImpl so
      // don't assume window is available
      typeof window === "undefined" ? "server://singlefetch/" : window.location.origin
    ) : reqUrl;
    if (trailingSlashAware) {
      if (url.pathname.endsWith("/")) {
        url.pathname = `${url.pathname}_.${extension}`;
      } else {
        url.pathname = `${url.pathname}.${extension}`;
      }
    } else {
      if (url.pathname === "/") {
        url.pathname = `_root.${extension}`;
      } else if (basename && stripBasename(url.pathname, basename) === "/") {
        url.pathname = `${removeTrailingSlash(basename)}/_root.${extension}`;
      } else {
        url.pathname = `${removeTrailingSlash(url.pathname)}.${extension}`;
      }
    }
    return url;
  }
  async function loadRouteModule(route, routeModulesCache) {
    if (route.id in routeModulesCache) {
      return routeModulesCache[route.id];
    }
    try {
      let routeModule = await import(
        /* @vite-ignore */
        /* webpackIgnore: true */
        route.module
      );
      routeModulesCache[route.id] = routeModule;
      return routeModule;
    } catch (error) {
      console.error(
        `Error loading route module \`${route.module}\`, reloading page...`
      );
      console.error(error);
      if (window.__reactRouterContext && window.__reactRouterContext.isSpaMode && // @ts-expect-error
      import_meta.hot) {
        throw error;
      }
      window.location.reload();
      return new Promise(() => {
      });
    }
  }
  function isPageLinkDescriptor(object) {
    return object != null && typeof object.page === "string";
  }
  function isHtmlLinkDescriptor(object) {
    if (object == null) {
      return false;
    }
    if (object.href == null) {
      return object.rel === "preload" && typeof object.imageSrcSet === "string" && typeof object.imageSizes === "string";
    }
    return typeof object.rel === "string" && typeof object.href === "string";
  }
  async function getKeyedPrefetchLinks(matches, manifest, routeModules) {
    let links = await Promise.all(
      matches.map(async (match) => {
        let route = manifest.routes[match.route.id];
        if (route) {
          let mod = await loadRouteModule(route, routeModules);
          return mod.links ? mod.links() : [];
        }
        return [];
      })
    );
    return dedupeLinkDescriptors(
      links.flat(1).filter(isHtmlLinkDescriptor).filter((link) => link.rel === "stylesheet" || link.rel === "preload").map(
        (link) => link.rel === "stylesheet" ? { ...link, rel: "prefetch", as: "style" } : { ...link, rel: "prefetch" }
      )
    );
  }
  function getNewMatchesForLinks(page, nextMatches, currentMatches, manifest, location, mode) {
    let isNew = (match, index) => {
      if (!currentMatches[index]) return true;
      return match.route.id !== currentMatches[index].route.id;
    };
    let matchPathChanged = (match, index) => {
      return (
        // param change, /users/123 -> /users/456
        currentMatches[index].pathname !== match.pathname || // splat param changed, which is not present in match.path
        // e.g. /files/images/avatar.jpg -> files/finances.xls
        currentMatches[index].route.path?.endsWith("*") && currentMatches[index].params["*"] !== match.params["*"]
      );
    };
    if (mode === "assets") {
      return nextMatches.filter(
        (match, index) => isNew(match, index) || matchPathChanged(match, index)
      );
    }
    if (mode === "data") {
      return nextMatches.filter((match, index) => {
        let manifestRoute = manifest.routes[match.route.id];
        if (!manifestRoute || !manifestRoute.hasLoader) {
          return false;
        }
        if (isNew(match, index) || matchPathChanged(match, index)) {
          return true;
        }
        if (match.route.shouldRevalidate) {
          let routeChoice = match.route.shouldRevalidate({
            currentUrl: new URL(
              location.pathname + location.search + location.hash,
              window.origin
            ),
            currentParams: currentMatches[0]?.params || {},
            nextUrl: new URL(page, window.origin),
            nextParams: match.params,
            defaultShouldRevalidate: true
          });
          if (typeof routeChoice === "boolean") {
            return routeChoice;
          }
        }
        return true;
      });
    }
    return [];
  }
  function getModuleLinkHrefs(matches, manifest, { includeHydrateFallback } = {}) {
    return dedupeHrefs(
      matches.map((match) => {
        let route = manifest.routes[match.route.id];
        if (!route) return [];
        let hrefs = [route.module];
        if (route.clientActionModule) {
          hrefs = hrefs.concat(route.clientActionModule);
        }
        if (route.clientLoaderModule) {
          hrefs = hrefs.concat(route.clientLoaderModule);
        }
        if (includeHydrateFallback && route.hydrateFallbackModule) {
          hrefs = hrefs.concat(route.hydrateFallbackModule);
        }
        if (route.imports) {
          hrefs = hrefs.concat(route.imports);
        }
        return hrefs;
      }).flat(1)
    );
  }
  function dedupeHrefs(hrefs) {
    return [...new Set(hrefs)];
  }
  function sortKeys(obj) {
    let sorted = {};
    let keys = Object.keys(obj).sort();
    for (let key of keys) {
      sorted[key] = obj[key];
    }
    return sorted;
  }
  function dedupeLinkDescriptors(descriptors, preloads) {
    let set = /* @__PURE__ */ new Set();
    let preloadsSet = new Set(preloads);
    return descriptors.reduce((deduped, descriptor) => {
      let alreadyModulePreload = preloads && !isPageLinkDescriptor(descriptor) && descriptor.as === "script" && descriptor.href && preloadsSet.has(descriptor.href);
      if (alreadyModulePreload) {
        return deduped;
      }
      let key = JSON.stringify(sortKeys(descriptor));
      if (!set.has(key)) {
        set.add(key);
        deduped.push({ key, link: descriptor });
      }
      return deduped;
    }, []);
  }
  function useDataRouterContext2() {
    let context = React8.useContext(DataRouterContext);
    invariant2(
      context,
      "You must render this element inside a <DataRouterContext.Provider> element"
    );
    return context;
  }
  function useDataRouterStateContext() {
    let context = React8.useContext(DataRouterStateContext);
    invariant2(
      context,
      "You must render this element inside a <DataRouterStateContext.Provider> element"
    );
    return context;
  }
  var FrameworkContext = React8.createContext(void 0);
  FrameworkContext.displayName = "FrameworkContext";
  function useFrameworkContext() {
    let context = React8.useContext(FrameworkContext);
    invariant2(
      context,
      "You must render this element inside a <HydratedRouter> element"
    );
    return context;
  }
  function usePrefetchBehavior(prefetch, theirElementProps) {
    let frameworkContext = React8.useContext(FrameworkContext);
    let [maybePrefetch, setMaybePrefetch] = React8.useState(false);
    let [shouldPrefetch, setShouldPrefetch] = React8.useState(false);
    let { onFocus, onBlur, onMouseEnter, onMouseLeave, onTouchStart } = theirElementProps;
    let ref = React8.useRef(null);
    React8.useEffect(() => {
      if (prefetch === "render") {
        setShouldPrefetch(true);
      }
      if (prefetch === "viewport") {
        let callback = (entries) => {
          entries.forEach((entry) => {
            setShouldPrefetch(entry.isIntersecting);
          });
        };
        let observer = new IntersectionObserver(callback, { threshold: 0.5 });
        if (ref.current) observer.observe(ref.current);
        return () => {
          observer.disconnect();
        };
      }
    }, [prefetch]);
    React8.useEffect(() => {
      if (maybePrefetch) {
        let id = setTimeout(() => {
          setShouldPrefetch(true);
        }, 100);
        return () => {
          clearTimeout(id);
        };
      }
    }, [maybePrefetch]);
    let setIntent = () => {
      setMaybePrefetch(true);
    };
    let cancelIntent = () => {
      setMaybePrefetch(false);
      setShouldPrefetch(false);
    };
    if (!frameworkContext) {
      return [false, ref, {}];
    }
    if (prefetch !== "intent") {
      return [shouldPrefetch, ref, {}];
    }
    return [
      shouldPrefetch,
      ref,
      {
        onFocus: composeEventHandlers(onFocus, setIntent),
        onBlur: composeEventHandlers(onBlur, cancelIntent),
        onMouseEnter: composeEventHandlers(onMouseEnter, setIntent),
        onMouseLeave: composeEventHandlers(onMouseLeave, cancelIntent),
        onTouchStart: composeEventHandlers(onTouchStart, setIntent)
      }
    ];
  }
  function composeEventHandlers(theirHandler, ourHandler) {
    return (event) => {
      theirHandler && theirHandler(event);
      if (!event.defaultPrevented) {
        ourHandler(event);
      }
    };
  }
  function PrefetchPageLinks({ page, ...linkProps }) {
    let rsc = useIsRSCRouterContext();
    let { router } = useDataRouterContext2();
    let matches = React8.useMemo(
      () => matchRoutes(router.routes, page, router.basename),
      [router.routes, page, router.basename]
    );
    if (!matches) {
      return null;
    }
    if (rsc) {
      return /* @__PURE__ */ React8.createElement(RSCPrefetchPageLinksImpl, { page, matches, ...linkProps });
    }
    return /* @__PURE__ */ React8.createElement(PrefetchPageLinksImpl, { page, matches, ...linkProps });
  }
  function useKeyedPrefetchLinks(matches) {
    let { manifest, routeModules } = useFrameworkContext();
    let [keyedPrefetchLinks, setKeyedPrefetchLinks] = React8.useState([]);
    React8.useEffect(() => {
      let interrupted = false;
      void getKeyedPrefetchLinks(matches, manifest, routeModules).then(
        (links) => {
          if (!interrupted) {
            setKeyedPrefetchLinks(links);
          }
        }
      );
      return () => {
        interrupted = true;
      };
    }, [matches, manifest, routeModules]);
    return keyedPrefetchLinks;
  }
  function RSCPrefetchPageLinksImpl({
    page,
    matches: nextMatches,
    ...linkProps
  }) {
    let location = useLocation();
    let { future } = useFrameworkContext();
    let { basename } = useDataRouterContext2();
    let dataHrefs = React8.useMemo(() => {
      if (page === location.pathname + location.search + location.hash) {
        return [];
      }
      let url = singleFetchUrl(
        page,
        basename,
        future.unstable_trailingSlashAwareDataRequests,
        "rsc"
      );
      let hasSomeRoutesWithShouldRevalidate = false;
      let targetRoutes = [];
      for (let match of nextMatches) {
        if (typeof match.route.shouldRevalidate === "function") {
          hasSomeRoutesWithShouldRevalidate = true;
        } else {
          targetRoutes.push(match.route.id);
        }
      }
      if (hasSomeRoutesWithShouldRevalidate && targetRoutes.length > 0) {
        url.searchParams.set("_routes", targetRoutes.join(","));
      }
      return [url.pathname + url.search];
    }, [
      basename,
      future.unstable_trailingSlashAwareDataRequests,
      page,
      location,
      nextMatches
    ]);
    return /* @__PURE__ */ React8.createElement(React8.Fragment, null, dataHrefs.map((href) => /* @__PURE__ */ React8.createElement("link", { key: href, rel: "prefetch", as: "fetch", href, ...linkProps })));
  }
  function PrefetchPageLinksImpl({
    page,
    matches: nextMatches,
    ...linkProps
  }) {
    let location = useLocation();
    let { future, manifest, routeModules } = useFrameworkContext();
    let { basename } = useDataRouterContext2();
    let { loaderData, matches } = useDataRouterStateContext();
    let newMatchesForData = React8.useMemo(
      () => getNewMatchesForLinks(
        page,
        nextMatches,
        matches,
        manifest,
        location,
        "data"
      ),
      [page, nextMatches, matches, manifest, location]
    );
    let newMatchesForAssets = React8.useMemo(
      () => getNewMatchesForLinks(
        page,
        nextMatches,
        matches,
        manifest,
        location,
        "assets"
      ),
      [page, nextMatches, matches, manifest, location]
    );
    let dataHrefs = React8.useMemo(() => {
      if (page === location.pathname + location.search + location.hash) {
        return [];
      }
      let routesParams = /* @__PURE__ */ new Set();
      let foundOptOutRoute = false;
      nextMatches.forEach((m) => {
        let manifestRoute = manifest.routes[m.route.id];
        if (!manifestRoute || !manifestRoute.hasLoader) {
          return;
        }
        if (!newMatchesForData.some((m2) => m2.route.id === m.route.id) && m.route.id in loaderData && routeModules[m.route.id]?.shouldRevalidate) {
          foundOptOutRoute = true;
        } else if (manifestRoute.hasClientLoader) {
          foundOptOutRoute = true;
        } else {
          routesParams.add(m.route.id);
        }
      });
      if (routesParams.size === 0) {
        return [];
      }
      let url = singleFetchUrl(
        page,
        basename,
        future.unstable_trailingSlashAwareDataRequests,
        "data"
      );
      if (foundOptOutRoute && routesParams.size > 0) {
        url.searchParams.set(
          "_routes",
          nextMatches.filter((m) => routesParams.has(m.route.id)).map((m) => m.route.id).join(",")
        );
      }
      return [url.pathname + url.search];
    }, [
      basename,
      future.unstable_trailingSlashAwareDataRequests,
      loaderData,
      location,
      manifest,
      newMatchesForData,
      nextMatches,
      page,
      routeModules
    ]);
    let moduleHrefs = React8.useMemo(
      () => getModuleLinkHrefs(newMatchesForAssets, manifest),
      [newMatchesForAssets, manifest]
    );
    let keyedPrefetchLinks = useKeyedPrefetchLinks(newMatchesForAssets);
    return /* @__PURE__ */ React8.createElement(React8.Fragment, null, dataHrefs.map((href) => /* @__PURE__ */ React8.createElement("link", { key: href, rel: "prefetch", as: "fetch", href, ...linkProps })), moduleHrefs.map((href) => /* @__PURE__ */ React8.createElement("link", { key: href, rel: "modulepreload", href, ...linkProps })), keyedPrefetchLinks.map(({ key, link }) => (
      // these don't spread `linkProps` because they are full link descriptors
      // already with their own props
      /* @__PURE__ */ React8.createElement(
        "link",
        {
          key,
          nonce: linkProps.nonce,
          ...link,
          crossOrigin: link.crossOrigin ?? linkProps.crossOrigin
        }
      )
    )));
  }
  function mergeRefs(...refs) {
    return (value) => {
      refs.forEach((ref) => {
        if (typeof ref === "function") {
          ref(value);
        } else if (ref != null) {
          ref.current = value;
        }
      });
    };
  }
  var isBrowser2 = typeof window !== "undefined" && typeof window.document !== "undefined" && typeof window.document.createElement !== "undefined";
  try {
    if (isBrowser2) {
      window.__reactRouterVersion = // @ts-expect-error
      "7.15.1";
    }
  } catch (e) {
  }
  function HistoryRouter({
    basename,
    children,
    history,
    useTransitions
  }) {
    let [state, setStateImpl] = React10.useState({
      action: history.action,
      location: history.location
    });
    let setState = React10.useCallback(
      (newState) => {
        if (useTransitions === false) {
          setStateImpl(newState);
        } else {
          React10.startTransition(() => setStateImpl(newState));
        }
      },
      [useTransitions]
    );
    React10.useLayoutEffect(() => history.listen(setState), [history, setState]);
    return /* @__PURE__ */ React10.createElement(
      Router,
      {
        basename,
        children,
        location: state.location,
        navigationType: state.action,
        navigator: history,
        useTransitions
      }
    );
  }
  HistoryRouter.displayName = "unstable_HistoryRouter";
  var ABSOLUTE_URL_REGEX2 = /^(?:[a-z][a-z0-9+.-]*:|\/\/)/i;
  var Link = React10.forwardRef(
    function LinkWithRef({
      onClick,
      discover = "render",
      prefetch = "none",
      relative,
      reloadDocument,
      replace: replace2,
      mask,
      state,
      target,
      to,
      preventScrollReset,
      viewTransition,
      defaultShouldRevalidate,
      ...rest
    }, forwardedRef) {
      let { basename, navigator: navigator2, useTransitions } = React10.useContext(NavigationContext);
      let isAbsolute = typeof to === "string" && ABSOLUTE_URL_REGEX2.test(to);
      let parsed = parseToInfo(to, basename);
      to = parsed.to;
      let href = useHref(to, { relative });
      let location = useLocation();
      let maskedHref = null;
      if (mask) {
        let resolved = resolveTo(
          mask,
          [],
          location.mask ? location.mask.pathname : "/",
          true
        );
        if (basename !== "/") {
          resolved.pathname = resolved.pathname === "/" ? basename : joinPaths([basename, resolved.pathname]);
        }
        maskedHref = navigator2.createHref(resolved);
      }
      let [shouldPrefetch, prefetchRef, prefetchHandlers] = usePrefetchBehavior(
        prefetch,
        rest
      );
      let internalOnClick = useLinkClickHandler(to, {
        replace: replace2,
        mask,
        state,
        target,
        preventScrollReset,
        relative,
        viewTransition,
        defaultShouldRevalidate,
        useTransitions
      });
      function handleClick(event) {
        if (onClick) onClick(event);
        if (!event.defaultPrevented) {
          internalOnClick(event);
        }
      }
      let isSpaLink = !(parsed.isExternal || reloadDocument);
      let link = (
        // eslint-disable-next-line jsx-a11y/anchor-has-content
        /* @__PURE__ */ React10.createElement(
          "a",
          {
            ...rest,
            ...prefetchHandlers,
            href: (isSpaLink ? maskedHref : void 0) || parsed.absoluteURL || href,
            onClick: isSpaLink ? handleClick : onClick,
            ref: mergeRefs(forwardedRef, prefetchRef),
            target,
            "data-discover": !isAbsolute && discover === "render" ? "true" : void 0
          }
        )
      );
      return shouldPrefetch && !isAbsolute ? /* @__PURE__ */ React10.createElement(React10.Fragment, null, link, /* @__PURE__ */ React10.createElement(PrefetchPageLinks, { page: href })) : link;
    }
  );
  Link.displayName = "Link";
  var NavLink = React10.forwardRef(
    function NavLinkWithRef({
      "aria-current": ariaCurrentProp = "page",
      caseSensitive = false,
      className: classNameProp = "",
      end = false,
      style: styleProp,
      to,
      viewTransition,
      children,
      ...rest
    }, ref) {
      let path = useResolvedPath(to, { relative: rest.relative });
      let location = useLocation();
      let routerState = React10.useContext(DataRouterStateContext);
      let { navigator: navigator2, basename } = React10.useContext(NavigationContext);
      let isTransitioning = routerState != null && // Conditional usage is OK here because the usage of a data router is static
      // eslint-disable-next-line react-hooks/rules-of-hooks
      useViewTransitionState(path) && viewTransition === true;
      let toPathname = navigator2.encodeLocation ? navigator2.encodeLocation(path).pathname : path.pathname;
      let locationPathname = location.pathname;
      let nextLocationPathname = routerState && routerState.navigation && routerState.navigation.location ? routerState.navigation.location.pathname : null;
      if (!caseSensitive) {
        locationPathname = locationPathname.toLowerCase();
        nextLocationPathname = nextLocationPathname ? nextLocationPathname.toLowerCase() : null;
        toPathname = toPathname.toLowerCase();
      }
      if (nextLocationPathname && basename) {
        nextLocationPathname = stripBasename(nextLocationPathname, basename) || nextLocationPathname;
      }
      const endSlashPosition = toPathname !== "/" && toPathname.endsWith("/") ? toPathname.length - 1 : toPathname.length;
      let isActive = locationPathname === toPathname || !end && locationPathname.startsWith(toPathname) && locationPathname.charAt(endSlashPosition) === "/";
      let isPending = nextLocationPathname != null && (nextLocationPathname === toPathname || !end && nextLocationPathname.startsWith(toPathname) && nextLocationPathname.charAt(toPathname.length) === "/");
      let renderProps = {
        isActive,
        isPending,
        isTransitioning
      };
      let ariaCurrent = isActive ? ariaCurrentProp : void 0;
      let className;
      if (typeof classNameProp === "function") {
        className = classNameProp(renderProps);
      } else {
        className = [
          classNameProp,
          isActive ? "active" : null,
          isPending ? "pending" : null,
          isTransitioning ? "transitioning" : null
        ].filter(Boolean).join(" ");
      }
      let style = typeof styleProp === "function" ? styleProp(renderProps) : styleProp;
      return /* @__PURE__ */ React10.createElement(
        Link,
        {
          ...rest,
          "aria-current": ariaCurrent,
          className,
          ref,
          style,
          to,
          viewTransition
        },
        typeof children === "function" ? children(renderProps) : children
      );
    }
  );
  NavLink.displayName = "NavLink";
  var Form = React10.forwardRef(
    ({
      discover = "render",
      fetcherKey,
      navigate,
      reloadDocument,
      replace: replace2,
      state,
      method = defaultMethod,
      action,
      onSubmit,
      relative,
      preventScrollReset,
      viewTransition,
      defaultShouldRevalidate,
      ...props
    }, forwardedRef) => {
      let { useTransitions } = React10.useContext(NavigationContext);
      let submit = useSubmit();
      let formAction = useFormAction(action, { relative });
      let formMethod = method.toLowerCase() === "get" ? "get" : "post";
      let isAbsolute = typeof action === "string" && ABSOLUTE_URL_REGEX2.test(action);
      let submitHandler = (event) => {
        onSubmit && onSubmit(event);
        if (event.defaultPrevented) return;
        event.preventDefault();
        let submitter = event.nativeEvent.submitter;
        let submitMethod = submitter?.getAttribute("formmethod") || method;
        let doSubmit = () => submit(submitter || event.currentTarget, {
          fetcherKey,
          method: submitMethod,
          navigate,
          replace: replace2,
          state,
          relative,
          preventScrollReset,
          viewTransition,
          defaultShouldRevalidate
        });
        if (useTransitions && navigate !== false) {
          React10.startTransition(() => doSubmit());
        } else {
          doSubmit();
        }
      };
      return /* @__PURE__ */ React10.createElement(
        "form",
        {
          ref: forwardedRef,
          method: formMethod,
          action: formAction,
          onSubmit: reloadDocument ? onSubmit : submitHandler,
          ...props,
          "data-discover": !isAbsolute && discover === "render" ? "true" : void 0
        }
      );
    }
  );
  Form.displayName = "Form";
  function ScrollRestoration({
    getKey,
    storageKey,
    ...props
  }) {
    let remixContext = React10.useContext(FrameworkContext);
    let { basename } = React10.useContext(NavigationContext);
    let location = useLocation();
    let matches = useMatches();
    useScrollRestoration({ getKey, storageKey });
    let ssrKey = React10.useMemo(
      () => {
        if (!remixContext || !getKey) return null;
        let userKey = getScrollRestorationKey(
          location,
          matches,
          basename,
          getKey
        );
        return userKey !== location.key ? userKey : null;
      },
      // Nah, we only need this the first time for the SSR render
      // eslint-disable-next-line react-hooks/exhaustive-deps
      []
    );
    if (!remixContext || remixContext.isSpaMode) {
      return null;
    }
    let restoreScroll = ((storageKey2, restoreKey) => {
      if (!window.history.state || !window.history.state.key) {
        let key = Math.random().toString(32).slice(2);
        window.history.replaceState({ key }, "");
      }
      try {
        let positions = JSON.parse(sessionStorage.getItem(storageKey2) || "{}");
        let storedY = positions[restoreKey || window.history.state.key];
        if (typeof storedY === "number") {
          window.scrollTo(0, storedY);
        }
      } catch (error) {
        console.error(error);
        sessionStorage.removeItem(storageKey2);
      }
    }).toString();
    return /* @__PURE__ */ React10.createElement(
      "script",
      {
        ...props,
        suppressHydrationWarning: true,
        dangerouslySetInnerHTML: {
          __html: `(${restoreScroll})(${escapeHtml(
            JSON.stringify(storageKey || SCROLL_RESTORATION_STORAGE_KEY)
          )}, ${escapeHtml(JSON.stringify(ssrKey))})`
        }
      }
    );
  }
  ScrollRestoration.displayName = "ScrollRestoration";
  function getDataRouterConsoleError2(hookName) {
    return `${hookName} must be used within a data router.  See https://reactrouter.com/en/main/routers/picking-a-router.`;
  }
  function useDataRouterContext3(hookName) {
    let ctx = React10.useContext(DataRouterContext);
    invariant(ctx, getDataRouterConsoleError2(hookName));
    return ctx;
  }
  function useDataRouterState2(hookName) {
    let state = React10.useContext(DataRouterStateContext);
    invariant(state, getDataRouterConsoleError2(hookName));
    return state;
  }
  function useLinkClickHandler(to, {
    target,
    replace: replaceProp,
    mask,
    state,
    preventScrollReset,
    relative,
    viewTransition,
    defaultShouldRevalidate,
    useTransitions
  } = {}) {
    let navigate = useNavigate();
    let location = useLocation();
    let path = useResolvedPath(to, { relative });
    return React10.useCallback(
      (event) => {
        if (shouldProcessLinkClick(event, target)) {
          event.preventDefault();
          let replace2 = replaceProp !== void 0 ? replaceProp : createPath(location) === createPath(path);
          let doNavigate = () => navigate(to, {
            replace: replace2,
            mask,
            state,
            preventScrollReset,
            relative,
            viewTransition,
            defaultShouldRevalidate
          });
          if (useTransitions) {
            React10.startTransition(() => doNavigate());
          } else {
            doNavigate();
          }
        }
      },
      [
        location,
        navigate,
        path,
        replaceProp,
        mask,
        state,
        target,
        to,
        preventScrollReset,
        relative,
        viewTransition,
        defaultShouldRevalidate,
        useTransitions
      ]
    );
  }
  var fetcherId = 0;
  var getUniqueFetcherId = () => `__${String(++fetcherId)}__`;
  function useSubmit() {
    let { router } = useDataRouterContext3(
      "useSubmit"
      /* UseSubmit */
    );
    let { basename } = React10.useContext(NavigationContext);
    let currentRouteId = useRouteId();
    let routerFetch = router.fetch;
    let routerNavigate = router.navigate;
    return React10.useCallback(
      async (target, options = {}) => {
        let { action, method, encType, formData, body } = getFormSubmissionInfo(
          target,
          basename
        );
        if (options.navigate === false) {
          let key = options.fetcherKey || getUniqueFetcherId();
          await routerFetch(key, currentRouteId, options.action || action, {
            defaultShouldRevalidate: options.defaultShouldRevalidate,
            preventScrollReset: options.preventScrollReset,
            formData,
            body,
            formMethod: options.method || method,
            formEncType: options.encType || encType,
            flushSync: options.flushSync
          });
        } else {
          await routerNavigate(options.action || action, {
            defaultShouldRevalidate: options.defaultShouldRevalidate,
            preventScrollReset: options.preventScrollReset,
            formData,
            body,
            formMethod: options.method || method,
            formEncType: options.encType || encType,
            replace: options.replace,
            state: options.state,
            fromRouteId: currentRouteId,
            flushSync: options.flushSync,
            viewTransition: options.viewTransition
          });
        }
      },
      [routerFetch, routerNavigate, basename, currentRouteId]
    );
  }
  function useFormAction(action, { relative } = {}) {
    let { basename } = React10.useContext(NavigationContext);
    let routeContext = React10.useContext(RouteContext);
    invariant(routeContext, "useFormAction must be used inside a RouteContext");
    let [match] = routeContext.matches.slice(-1);
    let path = { ...useResolvedPath(action ? action : ".", { relative }) };
    let location = useLocation();
    if (action == null) {
      path.search = location.search;
      let params = new URLSearchParams(path.search);
      let indexValues = params.getAll("index");
      let hasNakedIndexParam = indexValues.some((v) => v === "");
      if (hasNakedIndexParam) {
        params.delete("index");
        indexValues.filter((v) => v).forEach((v) => params.append("index", v));
        let qs = params.toString();
        path.search = qs ? `?${qs}` : "";
      }
    }
    if ((!action || action === ".") && match.route.index) {
      path.search = path.search ? path.search.replace(/^\?/, "?index&") : "?index";
    }
    if (basename !== "/") {
      path.pathname = path.pathname === "/" ? basename : joinPaths([basename, path.pathname]);
    }
    return createPath(path);
  }
  var SCROLL_RESTORATION_STORAGE_KEY = "react-router-scroll-positions";
  var savedScrollPositions = {};
  function getScrollRestorationKey(location, matches, basename, getKey) {
    let key = null;
    if (getKey) {
      if (basename !== "/") {
        key = getKey(
          {
            ...location,
            pathname: stripBasename(location.pathname, basename) || location.pathname
          },
          matches
        );
      } else {
        key = getKey(location, matches);
      }
    }
    if (key == null) {
      key = location.key;
    }
    return key;
  }
  function useScrollRestoration({
    getKey,
    storageKey
  } = {}) {
    let { router } = useDataRouterContext3(
      "useScrollRestoration"
      /* UseScrollRestoration */
    );
    let { restoreScrollPosition, preventScrollReset } = useDataRouterState2(
      "useScrollRestoration"
      /* UseScrollRestoration */
    );
    let { basename } = React10.useContext(NavigationContext);
    let location = useLocation();
    let matches = useMatches();
    let navigation = useNavigation();
    React10.useEffect(() => {
      window.history.scrollRestoration = "manual";
      return () => {
        window.history.scrollRestoration = "auto";
      };
    }, []);
    usePageHide(
      React10.useCallback(() => {
        if (navigation.state === "idle") {
          let key = getScrollRestorationKey(location, matches, basename, getKey);
          savedScrollPositions[key] = window.scrollY;
        }
        try {
          sessionStorage.setItem(
            storageKey || SCROLL_RESTORATION_STORAGE_KEY,
            JSON.stringify(savedScrollPositions)
          );
        } catch (error) {
          warning(
            false,
            `Failed to save scroll positions in sessionStorage, <ScrollRestoration /> will not work properly (${error}).`
          );
        }
        window.history.scrollRestoration = "auto";
      }, [navigation.state, getKey, basename, location, matches, storageKey])
    );
    if (typeof document !== "undefined") {
      React10.useLayoutEffect(() => {
        try {
          let sessionPositions = sessionStorage.getItem(
            storageKey || SCROLL_RESTORATION_STORAGE_KEY
          );
          if (sessionPositions) {
            savedScrollPositions = JSON.parse(sessionPositions);
          }
        } catch (e) {
        }
      }, [storageKey]);
      React10.useLayoutEffect(() => {
        let disableScrollRestoration = router?.enableScrollRestoration(
          savedScrollPositions,
          () => window.scrollY,
          getKey ? (location2, matches2) => getScrollRestorationKey(location2, matches2, basename, getKey) : void 0
        );
        return () => disableScrollRestoration && disableScrollRestoration();
      }, [router, basename, getKey]);
      React10.useLayoutEffect(() => {
        if (restoreScrollPosition === false) {
          return;
        }
        if (typeof restoreScrollPosition === "number") {
          window.scrollTo(0, restoreScrollPosition);
          return;
        }
        try {
          if (location.hash) {
            let el = document.getElementById(
              decodeURIComponent(location.hash.slice(1))
            );
            if (el) {
              el.scrollIntoView();
              return;
            }
          }
        } catch {
          warning(
            false,
            `"${location.hash.slice(
              1
            )}" is not a decodable element ID. The view will not scroll to it.`
          );
        }
        if (preventScrollReset === true) {
          return;
        }
        window.scrollTo(0, 0);
      }, [location, restoreScrollPosition, preventScrollReset]);
    }
  }
  function usePageHide(callback, options) {
    let { capture } = options || {};
    React10.useEffect(() => {
      let opts = capture != null ? { capture } : void 0;
      window.addEventListener("pagehide", callback, opts);
      return () => {
        window.removeEventListener("pagehide", callback, opts);
      };
    }, [callback, capture]);
  }
  function useViewTransitionState(to, { relative } = {}) {
    let vtContext = React10.useContext(ViewTransitionContext);
    invariant(
      vtContext != null,
      "`useViewTransitionState` must be used within `react-router-dom`'s `RouterProvider`.  Did you accidentally import `RouterProvider` from `react-router`?"
    );
    let { basename } = useDataRouterContext3(
      "useViewTransitionState"
      /* useViewTransitionState */
    );
    let path = useResolvedPath(to, { relative });
    if (!vtContext.isTransitioning) {
      return false;
    }
    let currentPath = stripBasename(vtContext.currentLocation.pathname, basename) || vtContext.currentLocation.pathname;
    let nextPath = stripBasename(vtContext.nextLocation.pathname, basename) || vtContext.nextLocation.pathname;
    return matchPath(path.pathname, nextPath) != null || matchPath(path.pathname, currentPath) != null;
  }

  // frontend-civic/node_modules/react-router/dist/development/index.mjs
  init_define_import_meta_env();

  // frontend-civic/src/prototypes/magazine/components/Button.tsx
  var base = "inline-flex items-center justify-center gap-1.5 rounded-full font-semibold transition disabled:opacity-50 disabled:pointer-events-none";
  var sizes = {
    sm: "px-2 py-0.5 text-[11px] [&_svg]:h-3 [&_svg]:w-3",
    md: "px-3.5 py-1 text-[13px] [&_svg]:h-3.5 [&_svg]:w-3.5"
  };
  var variants = {
    primary: "bg-[var(--accent)] text-white hover:opacity-90",
    secondary: "border border-[var(--accent)] text-[var(--accent)] hover:bg-[var(--accent)]/5",
    positive: "bg-emerald-600 text-white hover:opacity-90",
    ghost: "border border-[var(--border)] text-[var(--fg-soft)] hover:border-[var(--accent)]",
    danger: "border border-[var(--border)] text-[var(--fg-soft)] hover:border-red-400 hover:text-red-600",
    link: "text-[var(--muted)] hover:text-[var(--accent)]"
  };
  function buttonClasses(opts = {}) {
    const { variant = "primary", size = "md", fullWidth = false, className = "" } = opts;
    return [base, sizes[size], variants[variant], fullWidth ? "w-full" : "", className].filter(Boolean).join(" ");
  }
  var Button = (0, import_react.forwardRef)(function Button2({ variant = "primary", size = "md", fullWidth = false, className = "", type = "button", ...rest }, ref) {
    return /* @__PURE__ */ React.createElement("button", { ref, type, className: buttonClasses({ variant, size, fullWidth, className }), ...rest });
  });
  function ButtonLink({ variant = "primary", size = "md", fullWidth = false, className = "", ...rest }) {
    return /* @__PURE__ */ React.createElement(Link, { className: buttonClasses({ variant, size, fullWidth, className }), ...rest });
  }

  // frontend-civic/src/prototypes/magazine/components/ValueChip.tsx
  init_define_import_meta_env();

  // frontend-civic/src/lib/cn.ts
  init_define_import_meta_env();

  // frontend-civic/node_modules/clsx/dist/clsx.mjs
  init_define_import_meta_env();
  function r(e) {
    var t, f, n = "";
    if ("string" == typeof e || "number" == typeof e) n += e;
    else if ("object" == typeof e) if (Array.isArray(e)) {
      var o = e.length;
      for (t = 0; t < o; t++) e[t] && (f = r(e[t])) && (n && (n += " "), n += f);
    } else for (f in e) e[f] && (n && (n += " "), n += f);
    return n;
  }
  function clsx() {
    for (var e, t, f = 0, n = "", o = arguments.length; f < o; f++) (e = arguments[f]) && (t = r(e)) && (n && (n += " "), n += t);
    return n;
  }
  var clsx_default = clsx;

  // frontend-civic/src/lib/cn.ts
  function cn(...values) {
    return clsx_default(values);
  }

  // frontend-civic/src/prototypes/magazine/components/ValueChip.tsx
  function ValueChip({
    label,
    selected = false,
    onClick
  }) {
    return /* @__PURE__ */ React.createElement(
      "button",
      {
        type: "button",
        onClick,
        className: cn(
          "rounded-full border-2 px-5 py-2 text-base font-semibold transition",
          selected ? "border-[var(--accent)] bg-[var(--accent)] text-white" : "border-[var(--border)] bg-[var(--bg-elev)] text-[var(--fg)] hover:border-[var(--accent)]"
        )
      },
      label
    );
  }

  // frontend-civic/src/prototypes/magazine/components/PullQuote.tsx
  init_define_import_meta_env();
  function PullQuote({
    text,
    source
  }) {
    return /* @__PURE__ */ React.createElement("figure", { className: "my-10 border-l-4 border-[var(--accent)] py-2 pl-6" }, /* @__PURE__ */ React.createElement("blockquote", { className: "display text-2xl leading-tight text-[var(--fg)] md:text-3xl" }, "\u201C", text, "\u201D"), source && /* @__PURE__ */ React.createElement("figcaption", { className: "mt-3 text-xs font-semibold uppercase tracking-wider text-[var(--muted)]" }, "\u2014 ", source));
  }

  // frontend-civic/src/prototypes/magazine/components/DisclaimerBadge.tsx
  init_define_import_meta_env();

  // frontend-civic/node_modules/lucide-react/dist/esm/lucide-react.js
  init_define_import_meta_env();

  // frontend-civic/node_modules/lucide-react/dist/esm/createLucideIcon.js
  init_define_import_meta_env();
  var import_react3 = __toESM(require_react_shim());

  // frontend-civic/node_modules/lucide-react/dist/esm/shared/src/utils.js
  init_define_import_meta_env();
  var toKebabCase = (string) => string.replace(/([a-z0-9])([A-Z])/g, "$1-$2").toLowerCase();
  var mergeClasses = (...classes) => classes.filter((className, index, array) => {
    return Boolean(className) && className.trim() !== "" && array.indexOf(className) === index;
  }).join(" ").trim();

  // frontend-civic/node_modules/lucide-react/dist/esm/Icon.js
  init_define_import_meta_env();
  var import_react2 = __toESM(require_react_shim());

  // frontend-civic/node_modules/lucide-react/dist/esm/defaultAttributes.js
  init_define_import_meta_env();
  var defaultAttributes = {
    xmlns: "http://www.w3.org/2000/svg",
    width: 24,
    height: 24,
    viewBox: "0 0 24 24",
    fill: "none",
    stroke: "currentColor",
    strokeWidth: 2,
    strokeLinecap: "round",
    strokeLinejoin: "round"
  };

  // frontend-civic/node_modules/lucide-react/dist/esm/Icon.js
  var Icon = (0, import_react2.forwardRef)(
    ({
      color = "currentColor",
      size = 24,
      strokeWidth = 2,
      absoluteStrokeWidth,
      className = "",
      children,
      iconNode,
      ...rest
    }, ref) => {
      return (0, import_react2.createElement)(
        "svg",
        {
          ref,
          ...defaultAttributes,
          width: size,
          height: size,
          stroke: color,
          strokeWidth: absoluteStrokeWidth ? Number(strokeWidth) * 24 / Number(size) : strokeWidth,
          className: mergeClasses("lucide", className),
          ...rest
        },
        [
          ...iconNode.map(([tag, attrs]) => (0, import_react2.createElement)(tag, attrs)),
          ...Array.isArray(children) ? children : [children]
        ]
      );
    }
  );

  // frontend-civic/node_modules/lucide-react/dist/esm/createLucideIcon.js
  var createLucideIcon = (iconName, iconNode) => {
    const Component4 = (0, import_react3.forwardRef)(
      ({ className, ...props }, ref) => (0, import_react3.createElement)(Icon, {
        ref,
        iconNode,
        className: mergeClasses(`lucide-${toKebabCase(iconName)}`, className),
        ...props
      })
    );
    Component4.displayName = `${iconName}`;
    return Component4;
  };

  // frontend-civic/node_modules/lucide-react/dist/esm/icons/arrow-up-right.js
  init_define_import_meta_env();
  var ArrowUpRight = createLucideIcon("ArrowUpRight", [
    ["path", { d: "M7 7h10v10", key: "1tivn9" }],
    ["path", { d: "M7 17 17 7", key: "1vkiza" }]
  ]);

  // frontend-civic/node_modules/lucide-react/dist/esm/icons/bot.js
  init_define_import_meta_env();
  var Bot = createLucideIcon("Bot", [
    ["path", { d: "M12 8V4H8", key: "hb8ula" }],
    ["rect", { width: "16", height: "12", x: "4", y: "8", rx: "2", key: "enze0r" }],
    ["path", { d: "M2 14h2", key: "vft8re" }],
    ["path", { d: "M20 14h2", key: "4cs60a" }],
    ["path", { d: "M15 13v2", key: "1xurst" }],
    ["path", { d: "M9 13v2", key: "rq6x2g" }]
  ]);

  // frontend-civic/node_modules/lucide-react/dist/esm/icons/flame.js
  init_define_import_meta_env();
  var Flame = createLucideIcon("Flame", [
    [
      "path",
      {
        d: "M8.5 14.5A2.5 2.5 0 0 0 11 12c0-1.38-.5-2-1-3-1.072-2.143-.224-4.054 2-6 .5 2.5 2 4.9 4 6.5 2 1.6 3 3.5 3 5.5a7 7 0 1 1-14 0c0-1.153.433-2.294 1-3a2.5 2.5 0 0 0 2.5 2.5z",
        key: "96xj49"
      }
    ]
  ]);

  // frontend-civic/node_modules/lucide-react/dist/esm/icons/handshake.js
  init_define_import_meta_env();
  var Handshake = createLucideIcon("Handshake", [
    ["path", { d: "m11 17 2 2a1 1 0 1 0 3-3", key: "efffak" }],
    [
      "path",
      {
        d: "m14 14 2.5 2.5a1 1 0 1 0 3-3l-3.88-3.88a3 3 0 0 0-4.24 0l-.88.88a1 1 0 1 1-3-3l2.81-2.81a5.79 5.79 0 0 1 7.06-.87l.47.28a2 2 0 0 0 1.42.25L21 4",
        key: "9pr0kb"
      }
    ],
    ["path", { d: "m21 3 1 11h-2", key: "1tisrp" }],
    ["path", { d: "M3 3 2 14l6.5 6.5a1 1 0 1 0 3-3", key: "1uvwmv" }],
    ["path", { d: "M3 4h8", key: "1ep09j" }]
  ]);

  // frontend-civic/node_modules/lucide-react/dist/esm/icons/highlighter.js
  init_define_import_meta_env();
  var Highlighter = createLucideIcon("Highlighter", [
    ["path", { d: "m9 11-6 6v3h9l3-3", key: "1a3l36" }],
    ["path", { d: "m22 12-4.6 4.6a2 2 0 0 1-2.8 0l-5.2-5.2a2 2 0 0 1 0-2.8L14 4", key: "14a9rk" }]
  ]);

  // frontend-civic/node_modules/lucide-react/dist/esm/icons/house.js
  init_define_import_meta_env();
  var House = createLucideIcon("House", [
    ["path", { d: "M15 21v-8a1 1 0 0 0-1-1h-4a1 1 0 0 0-1 1v8", key: "5wwlr5" }],
    [
      "path",
      {
        d: "M3 10a2 2 0 0 1 .709-1.528l7-5.999a2 2 0 0 1 2.582 0l7 5.999A2 2 0 0 1 21 10v9a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2z",
        key: "1d0kgt"
      }
    ]
  ]);

  // frontend-civic/node_modules/lucide-react/dist/esm/icons/log-in.js
  init_define_import_meta_env();
  var LogIn = createLucideIcon("LogIn", [
    ["path", { d: "M15 3h4a2 2 0 0 1 2 2v14a2 2 0 0 1-2 2h-4", key: "u53s6r" }],
    ["polyline", { points: "10 17 15 12 10 7", key: "1ail0h" }],
    ["line", { x1: "15", x2: "3", y1: "12", y2: "12", key: "v6grx8" }]
  ]);

  // frontend-civic/node_modules/lucide-react/dist/esm/icons/megaphone.js
  init_define_import_meta_env();
  var Megaphone = createLucideIcon("Megaphone", [
    ["path", { d: "m3 11 18-5v12L3 14v-3z", key: "n962bs" }],
    ["path", { d: "M11.6 16.8a3 3 0 1 1-5.8-1.6", key: "1yl0tm" }]
  ]);

  // frontend-civic/node_modules/lucide-react/dist/esm/icons/menu.js
  init_define_import_meta_env();
  var Menu = createLucideIcon("Menu", [
    ["line", { x1: "4", x2: "20", y1: "12", y2: "12", key: "1e0a9i" }],
    ["line", { x1: "4", x2: "20", y1: "6", y2: "6", key: "1owob3" }],
    ["line", { x1: "4", x2: "20", y1: "18", y2: "18", key: "yk5zj1" }]
  ]);

  // frontend-civic/node_modules/lucide-react/dist/esm/icons/quote.js
  init_define_import_meta_env();
  var Quote = createLucideIcon("Quote", [
    [
      "path",
      {
        d: "M16 3a2 2 0 0 0-2 2v6a2 2 0 0 0 2 2 1 1 0 0 1 1 1v1a2 2 0 0 1-2 2 1 1 0 0 0-1 1v2a1 1 0 0 0 1 1 6 6 0 0 0 6-6V5a2 2 0 0 0-2-2z",
        key: "rib7q0"
      }
    ],
    [
      "path",
      {
        d: "M5 3a2 2 0 0 0-2 2v6a2 2 0 0 0 2 2 1 1 0 0 1 1 1v1a2 2 0 0 1-2 2 1 1 0 0 0-1 1v2a1 1 0 0 0 1 1 6 6 0 0 0 6-6V5a2 2 0 0 0-2-2z",
        key: "1ymkrd"
      }
    ]
  ]);

  // frontend-civic/node_modules/lucide-react/dist/esm/icons/share-2.js
  init_define_import_meta_env();
  var Share2 = createLucideIcon("Share2", [
    ["circle", { cx: "18", cy: "5", r: "3", key: "gq8acd" }],
    ["circle", { cx: "6", cy: "12", r: "3", key: "w7nqdw" }],
    ["circle", { cx: "18", cy: "19", r: "3", key: "1xt0gg" }],
    ["line", { x1: "8.59", x2: "15.42", y1: "13.51", y2: "17.49", key: "47mynk" }],
    ["line", { x1: "15.41", x2: "8.59", y1: "6.51", y2: "10.49", key: "1n3mei" }]
  ]);

  // frontend-civic/node_modules/lucide-react/dist/esm/icons/thumbs-down.js
  init_define_import_meta_env();
  var ThumbsDown = createLucideIcon("ThumbsDown", [
    ["path", { d: "M17 14V2", key: "8ymqnk" }],
    [
      "path",
      {
        d: "M9 18.12 10 14H4.17a2 2 0 0 1-1.92-2.56l2.33-8A2 2 0 0 1 6.5 2H20a2 2 0 0 1 2 2v8a2 2 0 0 1-2 2h-2.76a2 2 0 0 0-1.79 1.11L12 22a3.13 3.13 0 0 1-3-3.88Z",
        key: "m61m77"
      }
    ]
  ]);

  // frontend-civic/node_modules/lucide-react/dist/esm/icons/thumbs-up.js
  init_define_import_meta_env();
  var ThumbsUp = createLucideIcon("ThumbsUp", [
    ["path", { d: "M7 10v12", key: "1qc93n" }],
    [
      "path",
      {
        d: "M15 5.88 14 10h5.83a2 2 0 0 1 1.92 2.56l-2.33 8A2 2 0 0 1 17.5 22H4a2 2 0 0 1-2-2v-8a2 2 0 0 1 2-2h2.76a2 2 0 0 0 1.79-1.11L12 2a3.13 3.13 0 0 1 3 3.88Z",
        key: "emmmcr"
      }
    ]
  ]);

  // frontend-civic/node_modules/lucide-react/dist/esm/icons/trophy.js
  init_define_import_meta_env();
  var Trophy = createLucideIcon("Trophy", [
    ["path", { d: "M6 9H4.5a2.5 2.5 0 0 1 0-5H6", key: "17hqa7" }],
    ["path", { d: "M18 9h1.5a2.5 2.5 0 0 0 0-5H18", key: "lmptdp" }],
    ["path", { d: "M4 22h16", key: "57wxv0" }],
    ["path", { d: "M10 14.66V17c0 .55-.47.98-.97 1.21C7.85 18.75 7 20.24 7 22", key: "1nw9bq" }],
    ["path", { d: "M14 14.66V17c0 .55.47.98.97 1.21C16.15 18.75 17 20.24 17 22", key: "1np0yb" }],
    ["path", { d: "M18 2H6v7a6 6 0 0 0 12 0V2Z", key: "u46fv3" }]
  ]);

  // frontend-civic/node_modules/lucide-react/dist/esm/icons/user.js
  init_define_import_meta_env();
  var User = createLucideIcon("User", [
    ["path", { d: "M19 21v-2a4 4 0 0 0-4-4H9a4 4 0 0 0-4 4v2", key: "975kel" }],
    ["circle", { cx: "12", cy: "7", r: "4", key: "17ys0d" }]
  ]);

  // frontend-civic/node_modules/lucide-react/dist/esm/icons/x.js
  init_define_import_meta_env();
  var X = createLucideIcon("X", [
    ["path", { d: "M18 6 6 18", key: "1bl5f8" }],
    ["path", { d: "m6 6 12 12", key: "d8bk6v" }]
  ]);

  // frontend-civic/src/prototypes/magazine/components/DisclaimerBadge.tsx
  function DisclaimerBadge({ className }) {
    return /* @__PURE__ */ React.createElement(
      "span",
      {
        className: cn(
          "inline-flex items-center gap-1 rounded-full border border-indigo-300 bg-indigo-50 px-2 py-0.5 text-[10px] font-semibold uppercase tracking-wider text-indigo-700",
          className
        ),
        "data-testid": "fictional-disclaimer",
        title: "This is an AI-generated fictional candidate, not a real person."
      },
      /* @__PURE__ */ React.createElement(Bot, { className: "h-3 w-3" }),
      "AI simulation \xB7 Fictional"
    );
  }

  // frontend-civic/src/prototypes/magazine/components/Term.tsx
  init_define_import_meta_env();
  var import_react4 = __toESM(require_react_shim(), 1);
  var GLOSSARY = {
    plank: {
      title: "Plank",
      body: "One concrete policy provision in a platform \u2014 the single proposal a coalition forms around."
    },
    teeth: {
      title: "Teeth",
      body: "How enforceable a provision is \u2014 real, binding specifics rather than vague or symbolic language."
    },
    relegate: {
      title: "Relegate",
      body: "Drop to a lower league next cycle. Like relegation in sports, weaker records move down a tier."
    },
    promote: {
      title: "Promote",
      body: "Move up to a tougher league next cycle, where bridging a wider spectrum earns more."
    }
  };
  function Term({ term, children }) {
    const entry = GLOSSARY[term.toLowerCase()];
    const [open, setOpen] = (0, import_react4.useState)(false);
    const id = (0, import_react4.useId)();
    const ref = (0, import_react4.useRef)(null);
    (0, import_react4.useEffect)(() => {
      if (!open) return;
      const onKey = (e) => {
        if (e.key === "Escape") setOpen(false);
      };
      const onDown = (e) => {
        if (ref.current && !ref.current.contains(e.target)) setOpen(false);
      };
      window.addEventListener("keydown", onKey);
      document.addEventListener("mousedown", onDown);
      return () => {
        window.removeEventListener("keydown", onKey);
        document.removeEventListener("mousedown", onDown);
      };
    }, [open]);
    if (!entry) return /* @__PURE__ */ React.createElement(React.Fragment, null, children ?? term);
    return /* @__PURE__ */ React.createElement("span", { ref, className: "relative inline-block" }, /* @__PURE__ */ React.createElement(
      "button",
      {
        type: "button",
        "aria-describedby": open ? id : void 0,
        onClick: () => setOpen((v) => !v),
        onMouseEnter: () => setOpen(true),
        onMouseLeave: () => setOpen(false),
        onFocus: () => setOpen(true),
        onBlur: () => setOpen(false),
        "data-testid": `term-${term.toLowerCase()}`,
        className: "cursor-help border-b border-dotted border-current font-semibold decoration-from-font focus-visible:outline focus-visible:outline-2 focus-visible:outline-[var(--accent)]"
      },
      children ?? entry.title
    ), open && /* @__PURE__ */ React.createElement(
      "span",
      {
        role: "tooltip",
        id,
        className: "absolute bottom-full left-1/2 z-40 mb-2 w-56 -translate-x-1/2 border border-[var(--border)] bg-[var(--bg-elev)] p-3 text-left text-xs font-normal normal-case leading-snug tracking-normal text-[var(--fg-soft)] shadow-lg"
      },
      /* @__PURE__ */ React.createElement("span", { className: "mb-1 block text-[11px] font-semibold uppercase tracking-wider text-[var(--fg)]" }, entry.title),
      entry.body
    ));
  }

  // frontend-civic/src/prototypes/magazine/components/CandidateAvatar.tsx
  init_define_import_meta_env();
  var import_react5 = __toESM(require_react_shim(), 1);
  var PALETTE = [
    "#6366f1",
    "#0891b2",
    "#16a34a",
    "#d97706",
    "#dc2626",
    "#9333ea",
    "#0d9488",
    "#db2777"
  ];
  function colorFor(slug) {
    let hash = 0;
    for (let i = 0; i < slug.length; i++) hash = hash * 31 + slug.charCodeAt(i) | 0;
    return PALETTE[Math.abs(hash) % PALETTE.length];
  }
  function initials(name) {
    return name.split(" ").filter(Boolean).slice(0, 2).map((p) => p[0]?.toUpperCase() ?? "").join("");
  }
  function CandidateAvatar({
    candidate,
    size = 44,
    className
  }) {
    const key = candidate.avatarBaseUrl?.trim() || candidate.slug;
    const src = key ? `/avatars/${key}.png` : null;
    const [imgFailed, setImgFailed] = (0, import_react5.useState)(false);
    return /* @__PURE__ */ React.createElement(
      "span",
      {
        className: cn(
          "relative inline-flex shrink-0 items-center justify-center overflow-hidden rounded-full font-semibold text-white",
          className
        ),
        style: {
          width: size,
          height: size,
          backgroundColor: colorFor(candidate.slug),
          fontSize: size * 0.4
        },
        "data-testid": "candidate-avatar",
        "aria-label": candidate.name
      },
      /* @__PURE__ */ React.createElement("span", { "aria-hidden": true, className: "absolute inset-0 flex items-center justify-center" }, initials(candidate.name)),
      src && !imgFailed && /* @__PURE__ */ React.createElement(
        "img",
        {
          src,
          alt: "",
          loading: "lazy",
          onError: () => setImgFailed(true),
          className: "absolute inset-0 h-full w-full object-cover"
        }
      )
    );
  }

  // frontend-civic/src/prototypes/magazine/components/CountdownTimer.tsx
  init_define_import_meta_env();
  var import_react6 = __toESM(require_react_shim(), 1);

  // frontend-civic/src/api/elections.ts
  init_define_import_meta_env();

  // frontend-civic/src/api/client.ts
  init_define_import_meta_env();

  // frontend-civic/node_modules/axios/index.js
  init_define_import_meta_env();

  // frontend-civic/node_modules/axios/lib/axios.js
  init_define_import_meta_env();

  // frontend-civic/node_modules/axios/lib/utils.js
  init_define_import_meta_env();

  // frontend-civic/node_modules/axios/lib/helpers/bind.js
  init_define_import_meta_env();
  function bind(fn, thisArg) {
    return function wrap() {
      return fn.apply(thisArg, arguments);
    };
  }

  // frontend-civic/node_modules/axios/lib/utils.js
  var { toString } = Object.prototype;
  var { getPrototypeOf } = Object;
  var { iterator, toStringTag } = Symbol;
  var kindOf = /* @__PURE__ */ ((cache) => (thing) => {
    const str = toString.call(thing);
    return cache[str] || (cache[str] = str.slice(8, -1).toLowerCase());
  })(/* @__PURE__ */ Object.create(null));
  var kindOfTest = (type) => {
    type = type.toLowerCase();
    return (thing) => kindOf(thing) === type;
  };
  var typeOfTest = (type) => (thing) => typeof thing === type;
  var { isArray } = Array;
  var isUndefined = typeOfTest("undefined");
  function isBuffer(val) {
    return val !== null && !isUndefined(val) && val.constructor !== null && !isUndefined(val.constructor) && isFunction(val.constructor.isBuffer) && val.constructor.isBuffer(val);
  }
  var isArrayBuffer = kindOfTest("ArrayBuffer");
  function isArrayBufferView(val) {
    let result;
    if (typeof ArrayBuffer !== "undefined" && ArrayBuffer.isView) {
      result = ArrayBuffer.isView(val);
    } else {
      result = val && val.buffer && isArrayBuffer(val.buffer);
    }
    return result;
  }
  var isString = typeOfTest("string");
  var isFunction = typeOfTest("function");
  var isNumber = typeOfTest("number");
  var isObject = (thing) => thing !== null && typeof thing === "object";
  var isBoolean = (thing) => thing === true || thing === false;
  var isPlainObject = (val) => {
    if (kindOf(val) !== "object") {
      return false;
    }
    const prototype2 = getPrototypeOf(val);
    return (prototype2 === null || prototype2 === Object.prototype || Object.getPrototypeOf(prototype2) === null) && !(toStringTag in val) && !(iterator in val);
  };
  var isEmptyObject = (val) => {
    if (!isObject(val) || isBuffer(val)) {
      return false;
    }
    try {
      return Object.keys(val).length === 0 && Object.getPrototypeOf(val) === Object.prototype;
    } catch (e) {
      return false;
    }
  };
  var isDate = kindOfTest("Date");
  var isFile = kindOfTest("File");
  var isReactNativeBlob = (value) => {
    return !!(value && typeof value.uri !== "undefined");
  };
  var isReactNative = (formData) => formData && typeof formData.getParts !== "undefined";
  var isBlob = kindOfTest("Blob");
  var isFileList = kindOfTest("FileList");
  var isStream = (val) => isObject(val) && isFunction(val.pipe);
  function getGlobal() {
    if (typeof globalThis !== "undefined") return globalThis;
    if (typeof self !== "undefined") return self;
    if (typeof window !== "undefined") return window;
    if (typeof global !== "undefined") return global;
    return {};
  }
  var G = getGlobal();
  var FormDataCtor = typeof G.FormData !== "undefined" ? G.FormData : void 0;
  var isFormData = (thing) => {
    if (!thing) return false;
    if (FormDataCtor && thing instanceof FormDataCtor) return true;
    const proto = getPrototypeOf(thing);
    if (!proto || proto === Object.prototype) return false;
    if (!isFunction(thing.append)) return false;
    const kind = kindOf(thing);
    return kind === "formdata" || // detect form-data instance
    kind === "object" && isFunction(thing.toString) && thing.toString() === "[object FormData]";
  };
  var isURLSearchParams = kindOfTest("URLSearchParams");
  var [isReadableStream, isRequest, isResponse, isHeaders] = [
    "ReadableStream",
    "Request",
    "Response",
    "Headers"
  ].map(kindOfTest);
  var trim = (str) => {
    return str.trim ? str.trim() : str.replace(/^[\s\uFEFF\xA0]+|[\s\uFEFF\xA0]+$/g, "");
  };
  function forEach(obj, fn, { allOwnKeys = false } = {}) {
    if (obj === null || typeof obj === "undefined") {
      return;
    }
    let i;
    let l;
    if (typeof obj !== "object") {
      obj = [obj];
    }
    if (isArray(obj)) {
      for (i = 0, l = obj.length; i < l; i++) {
        fn.call(null, obj[i], i, obj);
      }
    } else {
      if (isBuffer(obj)) {
        return;
      }
      const keys = allOwnKeys ? Object.getOwnPropertyNames(obj) : Object.keys(obj);
      const len = keys.length;
      let key;
      for (i = 0; i < len; i++) {
        key = keys[i];
        fn.call(null, obj[key], key, obj);
      }
    }
  }
  function findKey(obj, key) {
    if (isBuffer(obj)) {
      return null;
    }
    key = key.toLowerCase();
    const keys = Object.keys(obj);
    let i = keys.length;
    let _key;
    while (i-- > 0) {
      _key = keys[i];
      if (key === _key.toLowerCase()) {
        return _key;
      }
    }
    return null;
  }
  var _global = (() => {
    if (typeof globalThis !== "undefined") return globalThis;
    return typeof self !== "undefined" ? self : typeof window !== "undefined" ? window : global;
  })();
  var isContextDefined = (context) => !isUndefined(context) && context !== _global;
  function merge(...objs) {
    const { caseless, skipUndefined } = isContextDefined(this) && this || {};
    const result = {};
    const assignValue = (val, key) => {
      if (key === "__proto__" || key === "constructor" || key === "prototype") {
        return;
      }
      const targetKey = caseless && findKey(result, key) || key;
      const existing = hasOwnProperty(result, targetKey) ? result[targetKey] : void 0;
      if (isPlainObject(existing) && isPlainObject(val)) {
        result[targetKey] = merge(existing, val);
      } else if (isPlainObject(val)) {
        result[targetKey] = merge({}, val);
      } else if (isArray(val)) {
        result[targetKey] = val.slice();
      } else if (!skipUndefined || !isUndefined(val)) {
        result[targetKey] = val;
      }
    };
    for (let i = 0, l = objs.length; i < l; i++) {
      objs[i] && forEach(objs[i], assignValue);
    }
    return result;
  }
  var extend = (a, b, thisArg, { allOwnKeys } = {}) => {
    forEach(
      b,
      (val, key) => {
        if (thisArg && isFunction(val)) {
          Object.defineProperty(a, key, {
            // Null-proto descriptor so a polluted Object.prototype.get cannot
            // hijack defineProperty's accessor-vs-data resolution.
            __proto__: null,
            value: bind(val, thisArg),
            writable: true,
            enumerable: true,
            configurable: true
          });
        } else {
          Object.defineProperty(a, key, {
            __proto__: null,
            value: val,
            writable: true,
            enumerable: true,
            configurable: true
          });
        }
      },
      { allOwnKeys }
    );
    return a;
  };
  var stripBOM = (content) => {
    if (content.charCodeAt(0) === 65279) {
      content = content.slice(1);
    }
    return content;
  };
  var inherits = (constructor, superConstructor, props, descriptors) => {
    constructor.prototype = Object.create(superConstructor.prototype, descriptors);
    Object.defineProperty(constructor.prototype, "constructor", {
      __proto__: null,
      value: constructor,
      writable: true,
      enumerable: false,
      configurable: true
    });
    Object.defineProperty(constructor, "super", {
      __proto__: null,
      value: superConstructor.prototype
    });
    props && Object.assign(constructor.prototype, props);
  };
  var toFlatObject = (sourceObj, destObj, filter2, propFilter) => {
    let props;
    let i;
    let prop;
    const merged = {};
    destObj = destObj || {};
    if (sourceObj == null) return destObj;
    do {
      props = Object.getOwnPropertyNames(sourceObj);
      i = props.length;
      while (i-- > 0) {
        prop = props[i];
        if ((!propFilter || propFilter(prop, sourceObj, destObj)) && !merged[prop]) {
          destObj[prop] = sourceObj[prop];
          merged[prop] = true;
        }
      }
      sourceObj = filter2 !== false && getPrototypeOf(sourceObj);
    } while (sourceObj && (!filter2 || filter2(sourceObj, destObj)) && sourceObj !== Object.prototype);
    return destObj;
  };
  var endsWith = (str, searchString, position) => {
    str = String(str);
    if (position === void 0 || position > str.length) {
      position = str.length;
    }
    position -= searchString.length;
    const lastIndex = str.indexOf(searchString, position);
    return lastIndex !== -1 && lastIndex === position;
  };
  var toArray = (thing) => {
    if (!thing) return null;
    if (isArray(thing)) return thing;
    let i = thing.length;
    if (!isNumber(i)) return null;
    const arr = new Array(i);
    while (i-- > 0) {
      arr[i] = thing[i];
    }
    return arr;
  };
  var isTypedArray = /* @__PURE__ */ ((TypedArray) => {
    return (thing) => {
      return TypedArray && thing instanceof TypedArray;
    };
  })(typeof Uint8Array !== "undefined" && getPrototypeOf(Uint8Array));
  var forEachEntry = (obj, fn) => {
    const generator = obj && obj[iterator];
    const _iterator = generator.call(obj);
    let result;
    while ((result = _iterator.next()) && !result.done) {
      const pair = result.value;
      fn.call(obj, pair[0], pair[1]);
    }
  };
  var matchAll = (regExp, str) => {
    let matches;
    const arr = [];
    while ((matches = regExp.exec(str)) !== null) {
      arr.push(matches);
    }
    return arr;
  };
  var isHTMLForm = kindOfTest("HTMLFormElement");
  var toCamelCase = (str) => {
    return str.toLowerCase().replace(/[-_\s]([a-z\d])(\w*)/g, function replacer(m, p1, p2) {
      return p1.toUpperCase() + p2;
    });
  };
  var hasOwnProperty = (({ hasOwnProperty: hasOwnProperty2 }) => (obj, prop) => hasOwnProperty2.call(obj, prop))(Object.prototype);
  var isRegExp = kindOfTest("RegExp");
  var reduceDescriptors = (obj, reducer) => {
    const descriptors = Object.getOwnPropertyDescriptors(obj);
    const reducedDescriptors = {};
    forEach(descriptors, (descriptor, name) => {
      let ret;
      if ((ret = reducer(descriptor, name, obj)) !== false) {
        reducedDescriptors[name] = ret || descriptor;
      }
    });
    Object.defineProperties(obj, reducedDescriptors);
  };
  var freezeMethods = (obj) => {
    reduceDescriptors(obj, (descriptor, name) => {
      if (isFunction(obj) && ["arguments", "caller", "callee"].includes(name)) {
        return false;
      }
      const value = obj[name];
      if (!isFunction(value)) return;
      descriptor.enumerable = false;
      if ("writable" in descriptor) {
        descriptor.writable = false;
        return;
      }
      if (!descriptor.set) {
        descriptor.set = () => {
          throw Error("Can not rewrite read-only method '" + name + "'");
        };
      }
    });
  };
  var toObjectSet = (arrayOrString, delimiter) => {
    const obj = {};
    const define = (arr) => {
      arr.forEach((value) => {
        obj[value] = true;
      });
    };
    isArray(arrayOrString) ? define(arrayOrString) : define(String(arrayOrString).split(delimiter));
    return obj;
  };
  var noop = () => {
  };
  var toFiniteNumber = (value, defaultValue) => {
    return value != null && Number.isFinite(value = +value) ? value : defaultValue;
  };
  function isSpecCompliantForm(thing) {
    return !!(thing && isFunction(thing.append) && thing[toStringTag] === "FormData" && thing[iterator]);
  }
  var toJSONObject = (obj) => {
    const visited = /* @__PURE__ */ new WeakSet();
    const visit = (source) => {
      if (isObject(source)) {
        if (visited.has(source)) {
          return;
        }
        if (isBuffer(source)) {
          return source;
        }
        if (!("toJSON" in source)) {
          visited.add(source);
          const target = isArray(source) ? [] : {};
          forEach(source, (value, key) => {
            const reducedValue = visit(value);
            !isUndefined(reducedValue) && (target[key] = reducedValue);
          });
          visited.delete(source);
          return target;
        }
      }
      return source;
    };
    return visit(obj);
  };
  var isAsyncFn = kindOfTest("AsyncFunction");
  var isThenable = (thing) => thing && (isObject(thing) || isFunction(thing)) && isFunction(thing.then) && isFunction(thing.catch);
  var _setImmediate = ((setImmediateSupported, postMessageSupported) => {
    if (setImmediateSupported) {
      return setImmediate;
    }
    return postMessageSupported ? ((token, callbacks) => {
      _global.addEventListener(
        "message",
        ({ source, data: data2 }) => {
          if (source === _global && data2 === token) {
            callbacks.length && callbacks.shift()();
          }
        },
        false
      );
      return (cb) => {
        callbacks.push(cb);
        _global.postMessage(token, "*");
      };
    })(`axios@${Math.random()}`, []) : (cb) => setTimeout(cb);
  })(typeof setImmediate === "function", isFunction(_global.postMessage));
  var asap = typeof queueMicrotask !== "undefined" ? queueMicrotask.bind(_global) : typeof process !== "undefined" && process.nextTick || _setImmediate;
  var isIterable = (thing) => thing != null && isFunction(thing[iterator]);
  var utils_default = {
    isArray,
    isArrayBuffer,
    isBuffer,
    isFormData,
    isArrayBufferView,
    isString,
    isNumber,
    isBoolean,
    isObject,
    isPlainObject,
    isEmptyObject,
    isReadableStream,
    isRequest,
    isResponse,
    isHeaders,
    isUndefined,
    isDate,
    isFile,
    isReactNativeBlob,
    isReactNative,
    isBlob,
    isRegExp,
    isFunction,
    isStream,
    isURLSearchParams,
    isTypedArray,
    isFileList,
    forEach,
    merge,
    extend,
    trim,
    stripBOM,
    inherits,
    toFlatObject,
    kindOf,
    kindOfTest,
    endsWith,
    toArray,
    forEachEntry,
    matchAll,
    isHTMLForm,
    hasOwnProperty,
    hasOwnProp: hasOwnProperty,
    // an alias to avoid ESLint no-prototype-builtins detection
    reduceDescriptors,
    freezeMethods,
    toObjectSet,
    toCamelCase,
    noop,
    toFiniteNumber,
    findKey,
    global: _global,
    isContextDefined,
    isSpecCompliantForm,
    toJSONObject,
    isAsyncFn,
    isThenable,
    setImmediate: _setImmediate,
    asap,
    isIterable
  };

  // frontend-civic/node_modules/axios/lib/core/Axios.js
  init_define_import_meta_env();

  // frontend-civic/node_modules/axios/lib/helpers/buildURL.js
  init_define_import_meta_env();

  // frontend-civic/node_modules/axios/lib/helpers/AxiosURLSearchParams.js
  init_define_import_meta_env();

  // frontend-civic/node_modules/axios/lib/helpers/toFormData.js
  init_define_import_meta_env();

  // frontend-civic/node_modules/axios/lib/core/AxiosError.js
  init_define_import_meta_env();

  // frontend-civic/node_modules/axios/lib/core/AxiosHeaders.js
  init_define_import_meta_env();

  // frontend-civic/node_modules/axios/lib/helpers/parseHeaders.js
  init_define_import_meta_env();
  var ignoreDuplicateOf = utils_default.toObjectSet([
    "age",
    "authorization",
    "content-length",
    "content-type",
    "etag",
    "expires",
    "from",
    "host",
    "if-modified-since",
    "if-unmodified-since",
    "last-modified",
    "location",
    "max-forwards",
    "proxy-authorization",
    "referer",
    "retry-after",
    "user-agent"
  ]);
  var parseHeaders_default = (rawHeaders) => {
    const parsed = {};
    let key;
    let val;
    let i;
    rawHeaders && rawHeaders.split("\n").forEach(function parser(line) {
      i = line.indexOf(":");
      key = line.substring(0, i).trim().toLowerCase();
      val = line.substring(i + 1).trim();
      if (!key || parsed[key] && ignoreDuplicateOf[key]) {
        return;
      }
      if (key === "set-cookie") {
        if (parsed[key]) {
          parsed[key].push(val);
        } else {
          parsed[key] = [val];
        }
      } else {
        parsed[key] = parsed[key] ? parsed[key] + ", " + val : val;
      }
    });
    return parsed;
  };

  // frontend-civic/node_modules/axios/lib/helpers/sanitizeHeaderValue.js
  init_define_import_meta_env();
  function trimSPorHTAB(str) {
    let start = 0;
    let end = str.length;
    while (start < end) {
      const code = str.charCodeAt(start);
      if (code !== 9 && code !== 32) {
        break;
      }
      start += 1;
    }
    while (end > start) {
      const code = str.charCodeAt(end - 1);
      if (code !== 9 && code !== 32) {
        break;
      }
      end -= 1;
    }
    return start === 0 && end === str.length ? str : str.slice(start, end);
  }
  var INVALID_UNICODE_HEADER_VALUE_CHARS = new RegExp("[\\u0000-\\u0008\\u000a-\\u001f\\u007f]+", "g");
  var INVALID_BYTE_STRING_HEADER_VALUE_CHARS = new RegExp("[^\\u0009\\u0020-\\u007e\\u0080-\\u00ff]+", "g");
  function sanitizeValue(value, invalidChars) {
    if (utils_default.isArray(value)) {
      return value.map((item) => sanitizeValue(item, invalidChars));
    }
    return trimSPorHTAB(String(value).replace(invalidChars, ""));
  }
  var sanitizeHeaderValue = (value) => sanitizeValue(value, INVALID_UNICODE_HEADER_VALUE_CHARS);
  var sanitizeByteStringHeaderValue = (value) => sanitizeValue(value, INVALID_BYTE_STRING_HEADER_VALUE_CHARS);
  function toByteStringHeaderObject(headers) {
    const byteStringHeaders = /* @__PURE__ */ Object.create(null);
    utils_default.forEach(headers.toJSON(), (value, header) => {
      byteStringHeaders[header] = sanitizeByteStringHeaderValue(value);
    });
    return byteStringHeaders;
  }

  // frontend-civic/node_modules/axios/lib/core/AxiosHeaders.js
  var $internals = /* @__PURE__ */ Symbol("internals");
  function normalizeHeader(header) {
    return header && String(header).trim().toLowerCase();
  }
  function normalizeValue(value) {
    if (value === false || value == null) {
      return value;
    }
    return utils_default.isArray(value) ? value.map(normalizeValue) : sanitizeHeaderValue(String(value));
  }
  function parseTokens(str) {
    const tokens = /* @__PURE__ */ Object.create(null);
    const tokensRE = /([^\s,;=]+)\s*(?:=\s*([^,;]+))?/g;
    let match;
    while (match = tokensRE.exec(str)) {
      tokens[match[1]] = match[2];
    }
    return tokens;
  }
  var isValidHeaderName = (str) => /^[-_a-zA-Z0-9^`|~,!#$%&'*+.]+$/.test(str.trim());
  function matchHeaderValue(context, value, header, filter2, isHeaderNameFilter) {
    if (utils_default.isFunction(filter2)) {
      return filter2.call(this, value, header);
    }
    if (isHeaderNameFilter) {
      value = header;
    }
    if (!utils_default.isString(value)) return;
    if (utils_default.isString(filter2)) {
      return value.indexOf(filter2) !== -1;
    }
    if (utils_default.isRegExp(filter2)) {
      return filter2.test(value);
    }
  }
  function formatHeader(header) {
    return header.trim().toLowerCase().replace(/([a-z\d])(\w*)/g, (w, char, str) => {
      return char.toUpperCase() + str;
    });
  }
  function buildAccessors(obj, header) {
    const accessorName = utils_default.toCamelCase(" " + header);
    ["get", "set", "has"].forEach((methodName) => {
      Object.defineProperty(obj, methodName + accessorName, {
        // Null-proto descriptor so a polluted Object.prototype.get cannot turn
        // this data descriptor into an accessor descriptor on the way in.
        __proto__: null,
        value: function(arg1, arg2, arg3) {
          return this[methodName].call(this, header, arg1, arg2, arg3);
        },
        configurable: true
      });
    });
  }
  var AxiosHeaders = class {
    constructor(headers) {
      headers && this.set(headers);
    }
    set(header, valueOrRewrite, rewrite) {
      const self2 = this;
      function setHeader(_value, _header, _rewrite) {
        const lHeader = normalizeHeader(_header);
        if (!lHeader) {
          throw new Error("header name must be a non-empty string");
        }
        const key = utils_default.findKey(self2, lHeader);
        if (!key || self2[key] === void 0 || _rewrite === true || _rewrite === void 0 && self2[key] !== false) {
          self2[key || _header] = normalizeValue(_value);
        }
      }
      const setHeaders = (headers, _rewrite) => utils_default.forEach(headers, (_value, _header) => setHeader(_value, _header, _rewrite));
      if (utils_default.isPlainObject(header) || header instanceof this.constructor) {
        setHeaders(header, valueOrRewrite);
      } else if (utils_default.isString(header) && (header = header.trim()) && !isValidHeaderName(header)) {
        setHeaders(parseHeaders_default(header), valueOrRewrite);
      } else if (utils_default.isObject(header) && utils_default.isIterable(header)) {
        let obj = {}, dest, key;
        for (const entry of header) {
          if (!utils_default.isArray(entry)) {
            throw TypeError("Object iterator must return a key-value pair");
          }
          obj[key = entry[0]] = (dest = obj[key]) ? utils_default.isArray(dest) ? [...dest, entry[1]] : [dest, entry[1]] : entry[1];
        }
        setHeaders(obj, valueOrRewrite);
      } else {
        header != null && setHeader(valueOrRewrite, header, rewrite);
      }
      return this;
    }
    get(header, parser) {
      header = normalizeHeader(header);
      if (header) {
        const key = utils_default.findKey(this, header);
        if (key) {
          const value = this[key];
          if (!parser) {
            return value;
          }
          if (parser === true) {
            return parseTokens(value);
          }
          if (utils_default.isFunction(parser)) {
            return parser.call(this, value, key);
          }
          if (utils_default.isRegExp(parser)) {
            return parser.exec(value);
          }
          throw new TypeError("parser must be boolean|regexp|function");
        }
      }
    }
    has(header, matcher) {
      header = normalizeHeader(header);
      if (header) {
        const key = utils_default.findKey(this, header);
        return !!(key && this[key] !== void 0 && (!matcher || matchHeaderValue(this, this[key], key, matcher)));
      }
      return false;
    }
    delete(header, matcher) {
      const self2 = this;
      let deleted = false;
      function deleteHeader(_header) {
        _header = normalizeHeader(_header);
        if (_header) {
          const key = utils_default.findKey(self2, _header);
          if (key && (!matcher || matchHeaderValue(self2, self2[key], key, matcher))) {
            delete self2[key];
            deleted = true;
          }
        }
      }
      if (utils_default.isArray(header)) {
        header.forEach(deleteHeader);
      } else {
        deleteHeader(header);
      }
      return deleted;
    }
    clear(matcher) {
      const keys = Object.keys(this);
      let i = keys.length;
      let deleted = false;
      while (i--) {
        const key = keys[i];
        if (!matcher || matchHeaderValue(this, this[key], key, matcher, true)) {
          delete this[key];
          deleted = true;
        }
      }
      return deleted;
    }
    normalize(format) {
      const self2 = this;
      const headers = {};
      utils_default.forEach(this, (value, header) => {
        const key = utils_default.findKey(headers, header);
        if (key) {
          self2[key] = normalizeValue(value);
          delete self2[header];
          return;
        }
        const normalized = format ? formatHeader(header) : String(header).trim();
        if (normalized !== header) {
          delete self2[header];
        }
        self2[normalized] = normalizeValue(value);
        headers[normalized] = true;
      });
      return this;
    }
    concat(...targets) {
      return this.constructor.concat(this, ...targets);
    }
    toJSON(asStrings) {
      const obj = /* @__PURE__ */ Object.create(null);
      utils_default.forEach(this, (value, header) => {
        value != null && value !== false && (obj[header] = asStrings && utils_default.isArray(value) ? value.join(", ") : value);
      });
      return obj;
    }
    [Symbol.iterator]() {
      return Object.entries(this.toJSON())[Symbol.iterator]();
    }
    toString() {
      return Object.entries(this.toJSON()).map(([header, value]) => header + ": " + value).join("\n");
    }
    getSetCookie() {
      return this.get("set-cookie") || [];
    }
    get [Symbol.toStringTag]() {
      return "AxiosHeaders";
    }
    static from(thing) {
      return thing instanceof this ? thing : new this(thing);
    }
    static concat(first, ...targets) {
      const computed = new this(first);
      targets.forEach((target) => computed.set(target));
      return computed;
    }
    static accessor(header) {
      const internals = this[$internals] = this[$internals] = {
        accessors: {}
      };
      const accessors = internals.accessors;
      const prototype2 = this.prototype;
      function defineAccessor(_header) {
        const lHeader = normalizeHeader(_header);
        if (!accessors[lHeader]) {
          buildAccessors(prototype2, _header);
          accessors[lHeader] = true;
        }
      }
      utils_default.isArray(header) ? header.forEach(defineAccessor) : defineAccessor(header);
      return this;
    }
  };
  AxiosHeaders.accessor([
    "Content-Type",
    "Content-Length",
    "Accept",
    "Accept-Encoding",
    "User-Agent",
    "Authorization"
  ]);
  utils_default.reduceDescriptors(AxiosHeaders.prototype, ({ value }, key) => {
    let mapped = key[0].toUpperCase() + key.slice(1);
    return {
      get: () => value,
      set(headerValue) {
        this[mapped] = headerValue;
      }
    };
  });
  utils_default.freezeMethods(AxiosHeaders);
  var AxiosHeaders_default = AxiosHeaders;

  // frontend-civic/node_modules/axios/lib/core/AxiosError.js
  var REDACTED = "[REDACTED ****]";
  function hasOwnOrPrototypeToJSON(source) {
    if (utils_default.hasOwnProp(source, "toJSON")) {
      return true;
    }
    let prototype2 = Object.getPrototypeOf(source);
    while (prototype2 && prototype2 !== Object.prototype) {
      if (utils_default.hasOwnProp(prototype2, "toJSON")) {
        return true;
      }
      prototype2 = Object.getPrototypeOf(prototype2);
    }
    return false;
  }
  function redactConfig(config, redactKeys) {
    const lowerKeys = new Set(redactKeys.map((k) => String(k).toLowerCase()));
    const seen = [];
    const visit = (source) => {
      if (source === null || typeof source !== "object") return source;
      if (utils_default.isBuffer(source)) return source;
      if (seen.indexOf(source) !== -1) return void 0;
      if (source instanceof AxiosHeaders_default) {
        source = source.toJSON();
      }
      seen.push(source);
      let result;
      if (utils_default.isArray(source)) {
        result = [];
        source.forEach((v, i) => {
          const reducedValue = visit(v);
          if (!utils_default.isUndefined(reducedValue)) {
            result[i] = reducedValue;
          }
        });
      } else {
        if (!utils_default.isPlainObject(source) && hasOwnOrPrototypeToJSON(source)) {
          seen.pop();
          return source;
        }
        result = /* @__PURE__ */ Object.create(null);
        for (const [key, value] of Object.entries(source)) {
          const reducedValue = lowerKeys.has(key.toLowerCase()) ? REDACTED : visit(value);
          if (!utils_default.isUndefined(reducedValue)) {
            result[key] = reducedValue;
          }
        }
      }
      seen.pop();
      return result;
    };
    return visit(config);
  }
  var AxiosError = class _AxiosError extends Error {
    static from(error, code, config, request, response, customProps) {
      const axiosError = new _AxiosError(error.message, code || error.code, config, request, response);
      axiosError.cause = error;
      axiosError.name = error.name;
      if (error.status != null && axiosError.status == null) {
        axiosError.status = error.status;
      }
      customProps && Object.assign(axiosError, customProps);
      return axiosError;
    }
    /**
     * Create an Error with the specified message, config, error code, request and response.
     *
     * @param {string} message The error message.
     * @param {string} [code] The error code (for example, 'ECONNABORTED').
     * @param {Object} [config] The config.
     * @param {Object} [request] The request.
     * @param {Object} [response] The response.
     *
     * @returns {Error} The created error.
     */
    constructor(message, code, config, request, response) {
      super(message);
      Object.defineProperty(this, "message", {
        // Null-proto descriptor so a polluted Object.prototype.get cannot turn
        // this data descriptor into an accessor descriptor on the way in.
        __proto__: null,
        value: message,
        enumerable: true,
        writable: true,
        configurable: true
      });
      this.name = "AxiosError";
      this.isAxiosError = true;
      code && (this.code = code);
      config && (this.config = config);
      request && (this.request = request);
      if (response) {
        this.response = response;
        this.status = response.status;
      }
    }
    toJSON() {
      const config = this.config;
      const redactKeys = config && utils_default.hasOwnProp(config, "redact") ? config.redact : void 0;
      const serializedConfig = utils_default.isArray(redactKeys) && redactKeys.length > 0 ? redactConfig(config, redactKeys) : utils_default.toJSONObject(config);
      return {
        // Standard
        message: this.message,
        name: this.name,
        // Microsoft
        description: this.description,
        number: this.number,
        // Mozilla
        fileName: this.fileName,
        lineNumber: this.lineNumber,
        columnNumber: this.columnNumber,
        stack: this.stack,
        // Axios
        config: serializedConfig,
        code: this.code,
        status: this.status
      };
    }
  };
  AxiosError.ERR_BAD_OPTION_VALUE = "ERR_BAD_OPTION_VALUE";
  AxiosError.ERR_BAD_OPTION = "ERR_BAD_OPTION";
  AxiosError.ECONNABORTED = "ECONNABORTED";
  AxiosError.ETIMEDOUT = "ETIMEDOUT";
  AxiosError.ECONNREFUSED = "ECONNREFUSED";
  AxiosError.ERR_NETWORK = "ERR_NETWORK";
  AxiosError.ERR_FR_TOO_MANY_REDIRECTS = "ERR_FR_TOO_MANY_REDIRECTS";
  AxiosError.ERR_DEPRECATED = "ERR_DEPRECATED";
  AxiosError.ERR_BAD_RESPONSE = "ERR_BAD_RESPONSE";
  AxiosError.ERR_BAD_REQUEST = "ERR_BAD_REQUEST";
  AxiosError.ERR_CANCELED = "ERR_CANCELED";
  AxiosError.ERR_NOT_SUPPORT = "ERR_NOT_SUPPORT";
  AxiosError.ERR_INVALID_URL = "ERR_INVALID_URL";
  AxiosError.ERR_FORM_DATA_DEPTH_EXCEEDED = "ERR_FORM_DATA_DEPTH_EXCEEDED";
  var AxiosError_default = AxiosError;

  // frontend-civic/node_modules/axios/lib/helpers/null.js
  init_define_import_meta_env();
  var null_default = null;

  // frontend-civic/node_modules/axios/lib/helpers/toFormData.js
  function isVisitable(thing) {
    return utils_default.isPlainObject(thing) || utils_default.isArray(thing);
  }
  function removeBrackets(key) {
    return utils_default.endsWith(key, "[]") ? key.slice(0, -2) : key;
  }
  function renderKey(path, key, dots) {
    if (!path) return key;
    return path.concat(key).map(function each(token, i) {
      token = removeBrackets(token);
      return !dots && i ? "[" + token + "]" : token;
    }).join(dots ? "." : "");
  }
  function isFlatArray(arr) {
    return utils_default.isArray(arr) && !arr.some(isVisitable);
  }
  var predicates = utils_default.toFlatObject(utils_default, {}, null, function filter(prop) {
    return /^is[A-Z]/.test(prop);
  });
  function toFormData(obj, formData, options) {
    if (!utils_default.isObject(obj)) {
      throw new TypeError("target must be an object");
    }
    formData = formData || new (null_default || FormData)();
    options = utils_default.toFlatObject(
      options,
      {
        metaTokens: true,
        dots: false,
        indexes: false
      },
      false,
      function defined(option, source) {
        return !utils_default.isUndefined(source[option]);
      }
    );
    const metaTokens = options.metaTokens;
    const visitor = options.visitor || defaultVisitor;
    const dots = options.dots;
    const indexes = options.indexes;
    const _Blob = options.Blob || typeof Blob !== "undefined" && Blob;
    const maxDepth = options.maxDepth === void 0 ? 100 : options.maxDepth;
    const useBlob = _Blob && utils_default.isSpecCompliantForm(formData);
    if (!utils_default.isFunction(visitor)) {
      throw new TypeError("visitor must be a function");
    }
    function convertValue(value) {
      if (value === null) return "";
      if (utils_default.isDate(value)) {
        return value.toISOString();
      }
      if (utils_default.isBoolean(value)) {
        return value.toString();
      }
      if (!useBlob && utils_default.isBlob(value)) {
        throw new AxiosError_default("Blob is not supported. Use a Buffer instead.");
      }
      if (utils_default.isArrayBuffer(value) || utils_default.isTypedArray(value)) {
        return useBlob && typeof Blob === "function" ? new Blob([value]) : Buffer.from(value);
      }
      return value;
    }
    function defaultVisitor(value, key, path) {
      let arr = value;
      if (utils_default.isReactNative(formData) && utils_default.isReactNativeBlob(value)) {
        formData.append(renderKey(path, key, dots), convertValue(value));
        return false;
      }
      if (value && !path && typeof value === "object") {
        if (utils_default.endsWith(key, "{}")) {
          key = metaTokens ? key : key.slice(0, -2);
          value = JSON.stringify(value);
        } else if (utils_default.isArray(value) && isFlatArray(value) || (utils_default.isFileList(value) || utils_default.endsWith(key, "[]")) && (arr = utils_default.toArray(value))) {
          key = removeBrackets(key);
          arr.forEach(function each(el, index) {
            !(utils_default.isUndefined(el) || el === null) && formData.append(
              // eslint-disable-next-line no-nested-ternary
              indexes === true ? renderKey([key], index, dots) : indexes === null ? key : key + "[]",
              convertValue(el)
            );
          });
          return false;
        }
      }
      if (isVisitable(value)) {
        return true;
      }
      formData.append(renderKey(path, key, dots), convertValue(value));
      return false;
    }
    const stack = [];
    const exposedHelpers = Object.assign(predicates, {
      defaultVisitor,
      convertValue,
      isVisitable
    });
    function build(value, path, depth = 0) {
      if (utils_default.isUndefined(value)) return;
      if (depth > maxDepth) {
        throw new AxiosError_default(
          "Object is too deeply nested (" + depth + " levels). Max depth: " + maxDepth,
          AxiosError_default.ERR_FORM_DATA_DEPTH_EXCEEDED
        );
      }
      if (stack.indexOf(value) !== -1) {
        throw Error("Circular reference detected in " + path.join("."));
      }
      stack.push(value);
      utils_default.forEach(value, function each(el, key) {
        const result = !(utils_default.isUndefined(el) || el === null) && visitor.call(formData, el, utils_default.isString(key) ? key.trim() : key, path, exposedHelpers);
        if (result === true) {
          build(el, path ? path.concat(key) : [key], depth + 1);
        }
      });
      stack.pop();
    }
    if (!utils_default.isObject(obj)) {
      throw new TypeError("data must be an object");
    }
    build(obj);
    return formData;
  }
  var toFormData_default = toFormData;

  // frontend-civic/node_modules/axios/lib/helpers/AxiosURLSearchParams.js
  function encode(str) {
    const charMap = {
      "!": "%21",
      "'": "%27",
      "(": "%28",
      ")": "%29",
      "~": "%7E",
      "%20": "+"
    };
    return encodeURIComponent(str).replace(/[!'()~]|%20/g, function replacer(match) {
      return charMap[match];
    });
  }
  function AxiosURLSearchParams(params, options) {
    this._pairs = [];
    params && toFormData_default(params, this, options);
  }
  var prototype = AxiosURLSearchParams.prototype;
  prototype.append = function append(name, value) {
    this._pairs.push([name, value]);
  };
  prototype.toString = function toString2(encoder) {
    const _encode = encoder ? function(value) {
      return encoder.call(this, value, encode);
    } : encode;
    return this._pairs.map(function each(pair) {
      return _encode(pair[0]) + "=" + _encode(pair[1]);
    }, "").join("&");
  };
  var AxiosURLSearchParams_default = AxiosURLSearchParams;

  // frontend-civic/node_modules/axios/lib/helpers/buildURL.js
  function encode2(val) {
    return encodeURIComponent(val).replace(/%3A/gi, ":").replace(/%24/g, "$").replace(/%2C/gi, ",").replace(/%20/g, "+");
  }
  function buildURL(url, params, options) {
    if (!params) {
      return url;
    }
    const _encode = options && options.encode || encode2;
    const _options = utils_default.isFunction(options) ? {
      serialize: options
    } : options;
    const serializeFn = _options && _options.serialize;
    let serializedParams;
    if (serializeFn) {
      serializedParams = serializeFn(params, _options);
    } else {
      serializedParams = utils_default.isURLSearchParams(params) ? params.toString() : new AxiosURLSearchParams_default(params, _options).toString(_encode);
    }
    if (serializedParams) {
      const hashmarkIndex = url.indexOf("#");
      if (hashmarkIndex !== -1) {
        url = url.slice(0, hashmarkIndex);
      }
      url += (url.indexOf("?") === -1 ? "?" : "&") + serializedParams;
    }
    return url;
  }

  // frontend-civic/node_modules/axios/lib/core/InterceptorManager.js
  init_define_import_meta_env();
  var InterceptorManager = class {
    constructor() {
      this.handlers = [];
    }
    /**
     * Add a new interceptor to the stack
     *
     * @param {Function} fulfilled The function to handle `then` for a `Promise`
     * @param {Function} rejected The function to handle `reject` for a `Promise`
     * @param {Object} options The options for the interceptor, synchronous and runWhen
     *
     * @return {Number} An ID used to remove interceptor later
     */
    use(fulfilled, rejected, options) {
      this.handlers.push({
        fulfilled,
        rejected,
        synchronous: options ? options.synchronous : false,
        runWhen: options ? options.runWhen : null
      });
      return this.handlers.length - 1;
    }
    /**
     * Remove an interceptor from the stack
     *
     * @param {Number} id The ID that was returned by `use`
     *
     * @returns {void}
     */
    eject(id) {
      if (this.handlers[id]) {
        this.handlers[id] = null;
      }
    }
    /**
     * Clear all interceptors from the stack
     *
     * @returns {void}
     */
    clear() {
      if (this.handlers) {
        this.handlers = [];
      }
    }
    /**
     * Iterate over all the registered interceptors
     *
     * This method is particularly useful for skipping over any
     * interceptors that may have become `null` calling `eject`.
     *
     * @param {Function} fn The function to call for each interceptor
     *
     * @returns {void}
     */
    forEach(fn) {
      utils_default.forEach(this.handlers, function forEachHandler(h) {
        if (h !== null) {
          fn(h);
        }
      });
    }
  };
  var InterceptorManager_default = InterceptorManager;

  // frontend-civic/node_modules/axios/lib/core/dispatchRequest.js
  init_define_import_meta_env();

  // frontend-civic/node_modules/axios/lib/core/transformData.js
  init_define_import_meta_env();

  // frontend-civic/node_modules/axios/lib/defaults/index.js
  init_define_import_meta_env();

  // frontend-civic/node_modules/axios/lib/defaults/transitional.js
  init_define_import_meta_env();
  var transitional_default = {
    silentJSONParsing: true,
    forcedJSONParsing: true,
    clarifyTimeoutError: false,
    legacyInterceptorReqResOrdering: true
  };

  // frontend-civic/node_modules/axios/lib/helpers/toURLEncodedForm.js
  init_define_import_meta_env();

  // frontend-civic/node_modules/axios/lib/platform/index.js
  init_define_import_meta_env();

  // frontend-civic/node_modules/axios/lib/platform/browser/index.js
  init_define_import_meta_env();

  // frontend-civic/node_modules/axios/lib/platform/browser/classes/URLSearchParams.js
  init_define_import_meta_env();
  var URLSearchParams_default = typeof URLSearchParams !== "undefined" ? URLSearchParams : AxiosURLSearchParams_default;

  // frontend-civic/node_modules/axios/lib/platform/browser/classes/FormData.js
  init_define_import_meta_env();
  var FormData_default = typeof FormData !== "undefined" ? FormData : null;

  // frontend-civic/node_modules/axios/lib/platform/browser/classes/Blob.js
  init_define_import_meta_env();
  var Blob_default = typeof Blob !== "undefined" ? Blob : null;

  // frontend-civic/node_modules/axios/lib/platform/browser/index.js
  var browser_default = {
    isBrowser: true,
    classes: {
      URLSearchParams: URLSearchParams_default,
      FormData: FormData_default,
      Blob: Blob_default
    },
    protocols: ["http", "https", "file", "blob", "url", "data"]
  };

  // frontend-civic/node_modules/axios/lib/platform/common/utils.js
  var utils_exports = {};
  __export(utils_exports, {
    hasBrowserEnv: () => hasBrowserEnv,
    hasStandardBrowserEnv: () => hasStandardBrowserEnv,
    hasStandardBrowserWebWorkerEnv: () => hasStandardBrowserWebWorkerEnv,
    navigator: () => _navigator,
    origin: () => origin
  });
  init_define_import_meta_env();
  var hasBrowserEnv = typeof window !== "undefined" && typeof document !== "undefined";
  var _navigator = typeof navigator === "object" && navigator || void 0;
  var hasStandardBrowserEnv = hasBrowserEnv && (!_navigator || ["ReactNative", "NativeScript", "NS"].indexOf(_navigator.product) < 0);
  var hasStandardBrowserWebWorkerEnv = (() => {
    return typeof WorkerGlobalScope !== "undefined" && // eslint-disable-next-line no-undef
    self instanceof WorkerGlobalScope && typeof self.importScripts === "function";
  })();
  var origin = hasBrowserEnv && window.location.href || "http://localhost";

  // frontend-civic/node_modules/axios/lib/platform/index.js
  var platform_default = {
    ...utils_exports,
    ...browser_default
  };

  // frontend-civic/node_modules/axios/lib/helpers/toURLEncodedForm.js
  function toURLEncodedForm(data2, options) {
    return toFormData_default(data2, new platform_default.classes.URLSearchParams(), {
      visitor: function(value, key, path, helpers) {
        if (platform_default.isNode && utils_default.isBuffer(value)) {
          this.append(key, value.toString("base64"));
          return false;
        }
        return helpers.defaultVisitor.apply(this, arguments);
      },
      ...options
    });
  }

  // frontend-civic/node_modules/axios/lib/helpers/formDataToJSON.js
  init_define_import_meta_env();
  function parsePropPath(name) {
    return utils_default.matchAll(/\w+|\[(\w*)]/g, name).map((match) => {
      return match[0] === "[]" ? "" : match[1] || match[0];
    });
  }
  function arrayToObject(arr) {
    const obj = {};
    const keys = Object.keys(arr);
    let i;
    const len = keys.length;
    let key;
    for (i = 0; i < len; i++) {
      key = keys[i];
      obj[key] = arr[key];
    }
    return obj;
  }
  function formDataToJSON(formData) {
    function buildPath(path, value, target, index) {
      let name = path[index++];
      if (name === "__proto__") return true;
      const isNumericKey = Number.isFinite(+name);
      const isLast = index >= path.length;
      name = !name && utils_default.isArray(target) ? target.length : name;
      if (isLast) {
        if (utils_default.hasOwnProp(target, name)) {
          target[name] = utils_default.isArray(target[name]) ? target[name].concat(value) : [target[name], value];
        } else {
          target[name] = value;
        }
        return !isNumericKey;
      }
      if (!utils_default.hasOwnProp(target, name) || !utils_default.isObject(target[name])) {
        target[name] = [];
      }
      const result = buildPath(path, value, target[name], index);
      if (result && utils_default.isArray(target[name])) {
        target[name] = arrayToObject(target[name]);
      }
      return !isNumericKey;
    }
    if (utils_default.isFormData(formData) && utils_default.isFunction(formData.entries)) {
      const obj = {};
      utils_default.forEachEntry(formData, (name, value) => {
        buildPath(parsePropPath(name), value, obj, 0);
      });
      return obj;
    }
    return null;
  }
  var formDataToJSON_default = formDataToJSON;

  // frontend-civic/node_modules/axios/lib/defaults/index.js
  var own = (obj, key) => obj != null && utils_default.hasOwnProp(obj, key) ? obj[key] : void 0;
  function stringifySafely(rawValue, parser, encoder) {
    if (utils_default.isString(rawValue)) {
      try {
        (parser || JSON.parse)(rawValue);
        return utils_default.trim(rawValue);
      } catch (e) {
        if (e.name !== "SyntaxError") {
          throw e;
        }
      }
    }
    return (encoder || JSON.stringify)(rawValue);
  }
  var defaults = {
    transitional: transitional_default,
    adapter: ["xhr", "http", "fetch"],
    transformRequest: [
      function transformRequest(data2, headers) {
        const contentType = headers.getContentType() || "";
        const hasJSONContentType = contentType.indexOf("application/json") > -1;
        const isObjectPayload = utils_default.isObject(data2);
        if (isObjectPayload && utils_default.isHTMLForm(data2)) {
          data2 = new FormData(data2);
        }
        const isFormData2 = utils_default.isFormData(data2);
        if (isFormData2) {
          return hasJSONContentType ? JSON.stringify(formDataToJSON_default(data2)) : data2;
        }
        if (utils_default.isArrayBuffer(data2) || utils_default.isBuffer(data2) || utils_default.isStream(data2) || utils_default.isFile(data2) || utils_default.isBlob(data2) || utils_default.isReadableStream(data2)) {
          return data2;
        }
        if (utils_default.isArrayBufferView(data2)) {
          return data2.buffer;
        }
        if (utils_default.isURLSearchParams(data2)) {
          headers.setContentType("application/x-www-form-urlencoded;charset=utf-8", false);
          return data2.toString();
        }
        let isFileList2;
        if (isObjectPayload) {
          const formSerializer = own(this, "formSerializer");
          if (contentType.indexOf("application/x-www-form-urlencoded") > -1) {
            return toURLEncodedForm(data2, formSerializer).toString();
          }
          if ((isFileList2 = utils_default.isFileList(data2)) || contentType.indexOf("multipart/form-data") > -1) {
            const env = own(this, "env");
            const _FormData = env && env.FormData;
            return toFormData_default(
              isFileList2 ? { "files[]": data2 } : data2,
              _FormData && new _FormData(),
              formSerializer
            );
          }
        }
        if (isObjectPayload || hasJSONContentType) {
          headers.setContentType("application/json", false);
          return stringifySafely(data2);
        }
        return data2;
      }
    ],
    transformResponse: [
      function transformResponse(data2) {
        const transitional2 = own(this, "transitional") || defaults.transitional;
        const forcedJSONParsing = transitional2 && transitional2.forcedJSONParsing;
        const responseType = own(this, "responseType");
        const JSONRequested = responseType === "json";
        if (utils_default.isResponse(data2) || utils_default.isReadableStream(data2)) {
          return data2;
        }
        if (data2 && utils_default.isString(data2) && (forcedJSONParsing && !responseType || JSONRequested)) {
          const silentJSONParsing = transitional2 && transitional2.silentJSONParsing;
          const strictJSONParsing = !silentJSONParsing && JSONRequested;
          try {
            return JSON.parse(data2, own(this, "parseReviver"));
          } catch (e) {
            if (strictJSONParsing) {
              if (e.name === "SyntaxError") {
                throw AxiosError_default.from(e, AxiosError_default.ERR_BAD_RESPONSE, this, null, own(this, "response"));
              }
              throw e;
            }
          }
        }
        return data2;
      }
    ],
    /**
     * A timeout in milliseconds to abort a request. If set to 0 (default) a
     * timeout is not created.
     */
    timeout: 0,
    xsrfCookieName: "XSRF-TOKEN",
    xsrfHeaderName: "X-XSRF-TOKEN",
    maxContentLength: -1,
    maxBodyLength: -1,
    env: {
      FormData: platform_default.classes.FormData,
      Blob: platform_default.classes.Blob
    },
    validateStatus: function validateStatus(status) {
      return status >= 200 && status < 300;
    },
    headers: {
      common: {
        Accept: "application/json, text/plain, */*",
        "Content-Type": void 0
      }
    }
  };
  utils_default.forEach(["delete", "get", "head", "post", "put", "patch", "query"], (method) => {
    defaults.headers[method] = {};
  });
  var defaults_default = defaults;

  // frontend-civic/node_modules/axios/lib/core/transformData.js
  function transformData(fns, response) {
    const config = this || defaults_default;
    const context = response || config;
    const headers = AxiosHeaders_default.from(context.headers);
    let data2 = context.data;
    utils_default.forEach(fns, function transform(fn) {
      data2 = fn.call(config, data2, headers.normalize(), response ? response.status : void 0);
    });
    headers.normalize();
    return data2;
  }

  // frontend-civic/node_modules/axios/lib/cancel/isCancel.js
  init_define_import_meta_env();
  function isCancel(value) {
    return !!(value && value.__CANCEL__);
  }

  // frontend-civic/node_modules/axios/lib/cancel/CanceledError.js
  init_define_import_meta_env();
  var CanceledError = class extends AxiosError_default {
    /**
     * A `CanceledError` is an object that is thrown when an operation is canceled.
     *
     * @param {string=} message The message.
     * @param {Object=} config The config.
     * @param {Object=} request The request.
     *
     * @returns {CanceledError} The created error.
     */
    constructor(message, config, request) {
      super(message == null ? "canceled" : message, AxiosError_default.ERR_CANCELED, config, request);
      this.name = "CanceledError";
      this.__CANCEL__ = true;
    }
  };
  var CanceledError_default = CanceledError;

  // frontend-civic/node_modules/axios/lib/adapters/adapters.js
  init_define_import_meta_env();

  // frontend-civic/node_modules/axios/lib/adapters/xhr.js
  init_define_import_meta_env();

  // frontend-civic/node_modules/axios/lib/core/settle.js
  init_define_import_meta_env();
  function settle(resolve, reject, response) {
    const validateStatus2 = response.config.validateStatus;
    if (!response.status || !validateStatus2 || validateStatus2(response.status)) {
      resolve(response);
    } else {
      reject(new AxiosError_default(
        "Request failed with status code " + response.status,
        response.status >= 400 && response.status < 500 ? AxiosError_default.ERR_BAD_REQUEST : AxiosError_default.ERR_BAD_RESPONSE,
        response.config,
        response.request,
        response
      ));
    }
  }

  // frontend-civic/node_modules/axios/lib/helpers/parseProtocol.js
  init_define_import_meta_env();
  function parseProtocol(url) {
    const match = /^([-+\w]{1,25}):(?:\/\/)?/.exec(url);
    return match && match[1] || "";
  }

  // frontend-civic/node_modules/axios/lib/helpers/progressEventReducer.js
  init_define_import_meta_env();

  // frontend-civic/node_modules/axios/lib/helpers/speedometer.js
  init_define_import_meta_env();
  function speedometer(samplesCount, min) {
    samplesCount = samplesCount || 10;
    const bytes = new Array(samplesCount);
    const timestamps = new Array(samplesCount);
    let head = 0;
    let tail = 0;
    let firstSampleTS;
    min = min !== void 0 ? min : 1e3;
    return function push(chunkLength) {
      const now = Date.now();
      const startedAt = timestamps[tail];
      if (!firstSampleTS) {
        firstSampleTS = now;
      }
      bytes[head] = chunkLength;
      timestamps[head] = now;
      let i = tail;
      let bytesCount = 0;
      while (i !== head) {
        bytesCount += bytes[i++];
        i = i % samplesCount;
      }
      head = (head + 1) % samplesCount;
      if (head === tail) {
        tail = (tail + 1) % samplesCount;
      }
      if (now - firstSampleTS < min) {
        return;
      }
      const passed = startedAt && now - startedAt;
      return passed ? Math.round(bytesCount * 1e3 / passed) : void 0;
    };
  }
  var speedometer_default = speedometer;

  // frontend-civic/node_modules/axios/lib/helpers/throttle.js
  init_define_import_meta_env();
  function throttle(fn, freq) {
    let timestamp = 0;
    let threshold = 1e3 / freq;
    let lastArgs;
    let timer;
    const invoke = (args, now = Date.now()) => {
      timestamp = now;
      lastArgs = null;
      if (timer) {
        clearTimeout(timer);
        timer = null;
      }
      fn(...args);
    };
    const throttled = (...args) => {
      const now = Date.now();
      const passed = now - timestamp;
      if (passed >= threshold) {
        invoke(args, now);
      } else {
        lastArgs = args;
        if (!timer) {
          timer = setTimeout(() => {
            timer = null;
            invoke(lastArgs);
          }, threshold - passed);
        }
      }
    };
    const flush = () => lastArgs && invoke(lastArgs);
    return [throttled, flush];
  }
  var throttle_default = throttle;

  // frontend-civic/node_modules/axios/lib/helpers/progressEventReducer.js
  var progressEventReducer = (listener, isDownloadStream, freq = 3) => {
    let bytesNotified = 0;
    const _speedometer = speedometer_default(50, 250);
    return throttle_default((e) => {
      if (!e || typeof e.loaded !== "number") {
        return;
      }
      const rawLoaded = e.loaded;
      const total = e.lengthComputable ? e.total : void 0;
      const loaded = total != null ? Math.min(rawLoaded, total) : rawLoaded;
      const progressBytes = Math.max(0, loaded - bytesNotified);
      const rate = _speedometer(progressBytes);
      bytesNotified = Math.max(bytesNotified, loaded);
      const data2 = {
        loaded,
        total,
        progress: total ? loaded / total : void 0,
        bytes: progressBytes,
        rate: rate ? rate : void 0,
        estimated: rate && total ? (total - loaded) / rate : void 0,
        event: e,
        lengthComputable: total != null,
        [isDownloadStream ? "download" : "upload"]: true
      };
      listener(data2);
    }, freq);
  };
  var progressEventDecorator = (total, throttled) => {
    const lengthComputable = total != null;
    return [
      (loaded) => throttled[0]({
        lengthComputable,
        total,
        loaded
      }),
      throttled[1]
    ];
  };
  var asyncDecorator = (fn) => (...args) => utils_default.asap(() => fn(...args));

  // frontend-civic/node_modules/axios/lib/helpers/resolveConfig.js
  init_define_import_meta_env();

  // frontend-civic/node_modules/axios/lib/helpers/isURLSameOrigin.js
  init_define_import_meta_env();
  var isURLSameOrigin_default = platform_default.hasStandardBrowserEnv ? /* @__PURE__ */ ((origin2, isMSIE) => (url) => {
    url = new URL(url, platform_default.origin);
    return origin2.protocol === url.protocol && origin2.host === url.host && (isMSIE || origin2.port === url.port);
  })(
    new URL(platform_default.origin),
    platform_default.navigator && /(msie|trident)/i.test(platform_default.navigator.userAgent)
  ) : () => true;

  // frontend-civic/node_modules/axios/lib/helpers/cookies.js
  init_define_import_meta_env();
  var cookies_default = platform_default.hasStandardBrowserEnv ? (
    // Standard browser envs support document.cookie
    {
      write(name, value, expires, path, domain, secure, sameSite) {
        if (typeof document === "undefined") return;
        const cookie = [`${name}=${encodeURIComponent(value)}`];
        if (utils_default.isNumber(expires)) {
          cookie.push(`expires=${new Date(expires).toUTCString()}`);
        }
        if (utils_default.isString(path)) {
          cookie.push(`path=${path}`);
        }
        if (utils_default.isString(domain)) {
          cookie.push(`domain=${domain}`);
        }
        if (secure === true) {
          cookie.push("secure");
        }
        if (utils_default.isString(sameSite)) {
          cookie.push(`SameSite=${sameSite}`);
        }
        document.cookie = cookie.join("; ");
      },
      read(name) {
        if (typeof document === "undefined") return null;
        const cookies = document.cookie.split(";");
        for (let i = 0; i < cookies.length; i++) {
          const cookie = cookies[i].replace(/^\s+/, "");
          const eq = cookie.indexOf("=");
          if (eq !== -1 && cookie.slice(0, eq) === name) {
            return decodeURIComponent(cookie.slice(eq + 1));
          }
        }
        return null;
      },
      remove(name) {
        this.write(name, "", Date.now() - 864e5, "/");
      }
    }
  ) : (
    // Non-standard browser env (web workers, react-native) lack needed support.
    {
      write() {
      },
      read() {
        return null;
      },
      remove() {
      }
    }
  );

  // frontend-civic/node_modules/axios/lib/core/buildFullPath.js
  init_define_import_meta_env();

  // frontend-civic/node_modules/axios/lib/helpers/isAbsoluteURL.js
  init_define_import_meta_env();
  function isAbsoluteURL(url) {
    if (typeof url !== "string") {
      return false;
    }
    return /^([a-z][a-z\d+\-.]*:)?\/\//i.test(url);
  }

  // frontend-civic/node_modules/axios/lib/helpers/combineURLs.js
  init_define_import_meta_env();
  function combineURLs(baseURL, relativeURL) {
    return relativeURL ? baseURL.replace(/\/?\/$/, "") + "/" + relativeURL.replace(/^\/+/, "") : baseURL;
  }

  // frontend-civic/node_modules/axios/lib/core/buildFullPath.js
  function buildFullPath(baseURL, requestedURL, allowAbsoluteUrls) {
    let isRelativeUrl = !isAbsoluteURL(requestedURL);
    if (baseURL && (isRelativeUrl || allowAbsoluteUrls === false)) {
      return combineURLs(baseURL, requestedURL);
    }
    return requestedURL;
  }

  // frontend-civic/node_modules/axios/lib/core/mergeConfig.js
  init_define_import_meta_env();
  var headersToObject = (thing) => thing instanceof AxiosHeaders_default ? { ...thing } : thing;
  function mergeConfig(config1, config2) {
    config2 = config2 || {};
    const config = /* @__PURE__ */ Object.create(null);
    Object.defineProperty(config, "hasOwnProperty", {
      // Null-proto descriptor so a polluted Object.prototype.get cannot turn
      // this data descriptor into an accessor descriptor on the way in.
      __proto__: null,
      value: Object.prototype.hasOwnProperty,
      enumerable: false,
      writable: true,
      configurable: true
    });
    function getMergedValue(target, source, prop, caseless) {
      if (utils_default.isPlainObject(target) && utils_default.isPlainObject(source)) {
        return utils_default.merge.call({ caseless }, target, source);
      } else if (utils_default.isPlainObject(source)) {
        return utils_default.merge({}, source);
      } else if (utils_default.isArray(source)) {
        return source.slice();
      }
      return source;
    }
    function mergeDeepProperties(a, b, prop, caseless) {
      if (!utils_default.isUndefined(b)) {
        return getMergedValue(a, b, prop, caseless);
      } else if (!utils_default.isUndefined(a)) {
        return getMergedValue(void 0, a, prop, caseless);
      }
    }
    function valueFromConfig2(a, b) {
      if (!utils_default.isUndefined(b)) {
        return getMergedValue(void 0, b);
      }
    }
    function defaultToConfig2(a, b) {
      if (!utils_default.isUndefined(b)) {
        return getMergedValue(void 0, b);
      } else if (!utils_default.isUndefined(a)) {
        return getMergedValue(void 0, a);
      }
    }
    function mergeDirectKeys(a, b, prop) {
      if (utils_default.hasOwnProp(config2, prop)) {
        return getMergedValue(a, b);
      } else if (utils_default.hasOwnProp(config1, prop)) {
        return getMergedValue(void 0, a);
      }
    }
    const mergeMap = {
      url: valueFromConfig2,
      method: valueFromConfig2,
      data: valueFromConfig2,
      baseURL: defaultToConfig2,
      transformRequest: defaultToConfig2,
      transformResponse: defaultToConfig2,
      paramsSerializer: defaultToConfig2,
      timeout: defaultToConfig2,
      timeoutMessage: defaultToConfig2,
      withCredentials: defaultToConfig2,
      withXSRFToken: defaultToConfig2,
      adapter: defaultToConfig2,
      responseType: defaultToConfig2,
      xsrfCookieName: defaultToConfig2,
      xsrfHeaderName: defaultToConfig2,
      onUploadProgress: defaultToConfig2,
      onDownloadProgress: defaultToConfig2,
      decompress: defaultToConfig2,
      maxContentLength: defaultToConfig2,
      maxBodyLength: defaultToConfig2,
      beforeRedirect: defaultToConfig2,
      transport: defaultToConfig2,
      httpAgent: defaultToConfig2,
      httpsAgent: defaultToConfig2,
      cancelToken: defaultToConfig2,
      socketPath: defaultToConfig2,
      allowedSocketPaths: defaultToConfig2,
      responseEncoding: defaultToConfig2,
      validateStatus: mergeDirectKeys,
      headers: (a, b, prop) => mergeDeepProperties(headersToObject(a), headersToObject(b), prop, true)
    };
    utils_default.forEach(Object.keys({ ...config1, ...config2 }), function computeConfigValue(prop) {
      if (prop === "__proto__" || prop === "constructor" || prop === "prototype") return;
      const merge2 = utils_default.hasOwnProp(mergeMap, prop) ? mergeMap[prop] : mergeDeepProperties;
      const a = utils_default.hasOwnProp(config1, prop) ? config1[prop] : void 0;
      const b = utils_default.hasOwnProp(config2, prop) ? config2[prop] : void 0;
      const configValue = merge2(a, b, prop);
      utils_default.isUndefined(configValue) && merge2 !== mergeDirectKeys || (config[prop] = configValue);
    });
    return config;
  }

  // frontend-civic/node_modules/axios/lib/helpers/resolveConfig.js
  var FORM_DATA_CONTENT_HEADERS = ["content-type", "content-length"];
  function setFormDataHeaders(headers, formHeaders, policy) {
    if (policy !== "content-only") {
      headers.set(formHeaders);
      return;
    }
    Object.entries(formHeaders).forEach(([key, val]) => {
      if (FORM_DATA_CONTENT_HEADERS.includes(key.toLowerCase())) {
        headers.set(key, val);
      }
    });
  }
  var encodeUTF8 = (str) => encodeURIComponent(str).replace(
    /%([0-9A-F]{2})/gi,
    (_, hex) => String.fromCharCode(parseInt(hex, 16))
  );
  var resolveConfig_default = (config) => {
    const newConfig = mergeConfig({}, config);
    const own2 = (key) => utils_default.hasOwnProp(newConfig, key) ? newConfig[key] : void 0;
    const data2 = own2("data");
    let withXSRFToken = own2("withXSRFToken");
    const xsrfHeaderName = own2("xsrfHeaderName");
    const xsrfCookieName = own2("xsrfCookieName");
    let headers = own2("headers");
    const auth = own2("auth");
    const baseURL = own2("baseURL");
    const allowAbsoluteUrls = own2("allowAbsoluteUrls");
    const url = own2("url");
    newConfig.headers = headers = AxiosHeaders_default.from(headers);
    newConfig.url = buildURL(
      buildFullPath(baseURL, url, allowAbsoluteUrls),
      config.params,
      config.paramsSerializer
    );
    if (auth) {
      headers.set(
        "Authorization",
        "Basic " + btoa((auth.username || "") + ":" + (auth.password ? encodeUTF8(auth.password) : ""))
      );
    }
    if (utils_default.isFormData(data2)) {
      if (platform_default.hasStandardBrowserEnv || platform_default.hasStandardBrowserWebWorkerEnv) {
        headers.setContentType(void 0);
      } else if (utils_default.isFunction(data2.getHeaders)) {
        setFormDataHeaders(headers, data2.getHeaders(), own2("formDataHeaderPolicy"));
      }
    }
    if (platform_default.hasStandardBrowserEnv) {
      if (utils_default.isFunction(withXSRFToken)) {
        withXSRFToken = withXSRFToken(newConfig);
      }
      const shouldSendXSRF = withXSRFToken === true || withXSRFToken == null && isURLSameOrigin_default(newConfig.url);
      if (shouldSendXSRF) {
        const xsrfValue = xsrfHeaderName && xsrfCookieName && cookies_default.read(xsrfCookieName);
        if (xsrfValue) {
          headers.set(xsrfHeaderName, xsrfValue);
        }
      }
    }
    return newConfig;
  };

  // frontend-civic/node_modules/axios/lib/adapters/xhr.js
  var isXHRAdapterSupported = typeof XMLHttpRequest !== "undefined";
  var xhr_default = isXHRAdapterSupported && function(config) {
    return new Promise(function dispatchXhrRequest(resolve, reject) {
      const _config = resolveConfig_default(config);
      let requestData = _config.data;
      const requestHeaders = AxiosHeaders_default.from(_config.headers).normalize();
      let { responseType, onUploadProgress, onDownloadProgress } = _config;
      let onCanceled;
      let uploadThrottled, downloadThrottled;
      let flushUpload, flushDownload;
      function done() {
        flushUpload && flushUpload();
        flushDownload && flushDownload();
        _config.cancelToken && _config.cancelToken.unsubscribe(onCanceled);
        _config.signal && _config.signal.removeEventListener("abort", onCanceled);
      }
      let request = new XMLHttpRequest();
      request.open(_config.method.toUpperCase(), _config.url, true);
      request.timeout = _config.timeout;
      function onloadend() {
        if (!request) {
          return;
        }
        const responseHeaders = AxiosHeaders_default.from(
          "getAllResponseHeaders" in request && request.getAllResponseHeaders()
        );
        const responseData = !responseType || responseType === "text" || responseType === "json" ? request.responseText : request.response;
        const response = {
          data: responseData,
          status: request.status,
          statusText: request.statusText,
          headers: responseHeaders,
          config,
          request
        };
        settle(
          function _resolve(value) {
            resolve(value);
            done();
          },
          function _reject(err) {
            reject(err);
            done();
          },
          response
        );
        request = null;
      }
      if ("onloadend" in request) {
        request.onloadend = onloadend;
      } else {
        request.onreadystatechange = function handleLoad() {
          if (!request || request.readyState !== 4) {
            return;
          }
          if (request.status === 0 && !(request.responseURL && request.responseURL.startsWith("file:"))) {
            return;
          }
          setTimeout(onloadend);
        };
      }
      request.onabort = function handleAbort() {
        if (!request) {
          return;
        }
        reject(new AxiosError_default("Request aborted", AxiosError_default.ECONNABORTED, config, request));
        done();
        request = null;
      };
      request.onerror = function handleError(event) {
        const msg = event && event.message ? event.message : "Network Error";
        const err = new AxiosError_default(msg, AxiosError_default.ERR_NETWORK, config, request);
        err.event = event || null;
        reject(err);
        done();
        request = null;
      };
      request.ontimeout = function handleTimeout() {
        let timeoutErrorMessage = _config.timeout ? "timeout of " + _config.timeout + "ms exceeded" : "timeout exceeded";
        const transitional2 = _config.transitional || transitional_default;
        if (_config.timeoutErrorMessage) {
          timeoutErrorMessage = _config.timeoutErrorMessage;
        }
        reject(
          new AxiosError_default(
            timeoutErrorMessage,
            transitional2.clarifyTimeoutError ? AxiosError_default.ETIMEDOUT : AxiosError_default.ECONNABORTED,
            config,
            request
          )
        );
        done();
        request = null;
      };
      requestData === void 0 && requestHeaders.setContentType(null);
      if ("setRequestHeader" in request) {
        utils_default.forEach(toByteStringHeaderObject(requestHeaders), function setRequestHeader(val, key) {
          request.setRequestHeader(key, val);
        });
      }
      if (!utils_default.isUndefined(_config.withCredentials)) {
        request.withCredentials = !!_config.withCredentials;
      }
      if (responseType && responseType !== "json") {
        request.responseType = _config.responseType;
      }
      if (onDownloadProgress) {
        [downloadThrottled, flushDownload] = progressEventReducer(onDownloadProgress, true);
        request.addEventListener("progress", downloadThrottled);
      }
      if (onUploadProgress && request.upload) {
        [uploadThrottled, flushUpload] = progressEventReducer(onUploadProgress);
        request.upload.addEventListener("progress", uploadThrottled);
        request.upload.addEventListener("loadend", flushUpload);
      }
      if (_config.cancelToken || _config.signal) {
        onCanceled = (cancel) => {
          if (!request) {
            return;
          }
          reject(!cancel || cancel.type ? new CanceledError_default(null, config, request) : cancel);
          request.abort();
          done();
          request = null;
        };
        _config.cancelToken && _config.cancelToken.subscribe(onCanceled);
        if (_config.signal) {
          _config.signal.aborted ? onCanceled() : _config.signal.addEventListener("abort", onCanceled);
        }
      }
      const protocol = parseProtocol(_config.url);
      if (protocol && !platform_default.protocols.includes(protocol)) {
        reject(
          new AxiosError_default(
            "Unsupported protocol " + protocol + ":",
            AxiosError_default.ERR_BAD_REQUEST,
            config
          )
        );
        return;
      }
      request.send(requestData || null);
    });
  };

  // frontend-civic/node_modules/axios/lib/adapters/fetch.js
  init_define_import_meta_env();

  // frontend-civic/node_modules/axios/lib/helpers/composeSignals.js
  init_define_import_meta_env();
  var composeSignals = (signals, timeout) => {
    signals = signals ? signals.filter(Boolean) : [];
    if (!timeout && !signals.length) {
      return;
    }
    const controller = new AbortController();
    let aborted = false;
    const onabort = function(reason) {
      if (!aborted) {
        aborted = true;
        unsubscribe();
        const err = reason instanceof Error ? reason : this.reason;
        controller.abort(
          err instanceof AxiosError_default ? err : new CanceledError_default(err instanceof Error ? err.message : err)
        );
      }
    };
    let timer = timeout && setTimeout(() => {
      timer = null;
      onabort(new AxiosError_default(`timeout of ${timeout}ms exceeded`, AxiosError_default.ETIMEDOUT));
    }, timeout);
    const unsubscribe = () => {
      if (!signals) {
        return;
      }
      timer && clearTimeout(timer);
      timer = null;
      signals.forEach((signal2) => {
        signal2.unsubscribe ? signal2.unsubscribe(onabort) : signal2.removeEventListener("abort", onabort);
      });
      signals = null;
    };
    signals.forEach((signal2) => signal2.addEventListener("abort", onabort));
    const { signal } = controller;
    signal.unsubscribe = () => utils_default.asap(unsubscribe);
    return signal;
  };
  var composeSignals_default = composeSignals;

  // frontend-civic/node_modules/axios/lib/helpers/trackStream.js
  init_define_import_meta_env();
  var streamChunk = function* (chunk, chunkSize) {
    let len = chunk.byteLength;
    if (!chunkSize || len < chunkSize) {
      yield chunk;
      return;
    }
    let pos = 0;
    let end;
    while (pos < len) {
      end = pos + chunkSize;
      yield chunk.slice(pos, end);
      pos = end;
    }
  };
  var readBytes = async function* (iterable, chunkSize) {
    for await (const chunk of readStream(iterable)) {
      yield* streamChunk(chunk, chunkSize);
    }
  };
  var readStream = async function* (stream) {
    if (stream[Symbol.asyncIterator]) {
      yield* stream;
      return;
    }
    const reader = stream.getReader();
    try {
      for (; ; ) {
        const { done, value } = await reader.read();
        if (done) {
          break;
        }
        yield value;
      }
    } finally {
      await reader.cancel();
    }
  };
  var trackStream = (stream, chunkSize, onProgress, onFinish) => {
    const iterator2 = readBytes(stream, chunkSize);
    let bytes = 0;
    let done;
    let _onFinish = (e) => {
      if (!done) {
        done = true;
        onFinish && onFinish(e);
      }
    };
    return new ReadableStream(
      {
        async pull(controller) {
          try {
            const { done: done2, value } = await iterator2.next();
            if (done2) {
              _onFinish();
              controller.close();
              return;
            }
            let len = value.byteLength;
            if (onProgress) {
              let loadedBytes = bytes += len;
              onProgress(loadedBytes);
            }
            controller.enqueue(new Uint8Array(value));
          } catch (err) {
            _onFinish(err);
            throw err;
          }
        },
        cancel(reason) {
          _onFinish(reason);
          return iterator2.return();
        }
      },
      {
        highWaterMark: 2
      }
    );
  };

  // frontend-civic/node_modules/axios/lib/helpers/estimateDataURLDecodedBytes.js
  init_define_import_meta_env();
  function estimateDataURLDecodedBytes(url) {
    if (!url || typeof url !== "string") return 0;
    if (!url.startsWith("data:")) return 0;
    const comma = url.indexOf(",");
    if (comma < 0) return 0;
    const meta = url.slice(5, comma);
    const body = url.slice(comma + 1);
    const isBase64 = /;base64/i.test(meta);
    if (isBase64) {
      let effectiveLen = body.length;
      const len = body.length;
      for (let i = 0; i < len; i++) {
        if (body.charCodeAt(i) === 37 && i + 2 < len) {
          const a = body.charCodeAt(i + 1);
          const b = body.charCodeAt(i + 2);
          const isHex = (a >= 48 && a <= 57 || a >= 65 && a <= 70 || a >= 97 && a <= 102) && (b >= 48 && b <= 57 || b >= 65 && b <= 70 || b >= 97 && b <= 102);
          if (isHex) {
            effectiveLen -= 2;
            i += 2;
          }
        }
      }
      let pad2 = 0;
      let idx = len - 1;
      const tailIsPct3D = (j) => j >= 2 && body.charCodeAt(j - 2) === 37 && // '%'
      body.charCodeAt(j - 1) === 51 && // '3'
      (body.charCodeAt(j) === 68 || body.charCodeAt(j) === 100);
      if (idx >= 0) {
        if (body.charCodeAt(idx) === 61) {
          pad2++;
          idx--;
        } else if (tailIsPct3D(idx)) {
          pad2++;
          idx -= 3;
        }
      }
      if (pad2 === 1 && idx >= 0) {
        if (body.charCodeAt(idx) === 61) {
          pad2++;
        } else if (tailIsPct3D(idx)) {
          pad2++;
        }
      }
      const groups = Math.floor(effectiveLen / 4);
      const bytes2 = groups * 3 - (pad2 || 0);
      return bytes2 > 0 ? bytes2 : 0;
    }
    if (typeof Buffer !== "undefined" && typeof Buffer.byteLength === "function") {
      return Buffer.byteLength(body, "utf8");
    }
    let bytes = 0;
    for (let i = 0, len = body.length; i < len; i++) {
      const c = body.charCodeAt(i);
      if (c < 128) {
        bytes += 1;
      } else if (c < 2048) {
        bytes += 2;
      } else if (c >= 55296 && c <= 56319 && i + 1 < len) {
        const next = body.charCodeAt(i + 1);
        if (next >= 56320 && next <= 57343) {
          bytes += 4;
          i++;
        } else {
          bytes += 3;
        }
      } else {
        bytes += 3;
      }
    }
    return bytes;
  }

  // frontend-civic/node_modules/axios/lib/env/data.js
  init_define_import_meta_env();
  var VERSION = "1.16.1";

  // frontend-civic/node_modules/axios/lib/adapters/fetch.js
  var DEFAULT_CHUNK_SIZE = 64 * 1024;
  var { isFunction: isFunction2 } = utils_default;
  var test = (fn, ...args) => {
    try {
      return !!fn(...args);
    } catch (e) {
      return false;
    }
  };
  var factory = (env) => {
    const globalObject = utils_default.global !== void 0 && utils_default.global !== null ? utils_default.global : globalThis;
    const { ReadableStream: ReadableStream2, TextEncoder: TextEncoder2 } = globalObject;
    env = utils_default.merge.call(
      {
        skipUndefined: true
      },
      {
        Request: globalObject.Request,
        Response: globalObject.Response
      },
      env
    );
    const { fetch: envFetch, Request: Request2, Response: Response2 } = env;
    const isFetchSupported = envFetch ? isFunction2(envFetch) : typeof fetch === "function";
    const isRequestSupported = isFunction2(Request2);
    const isResponseSupported = isFunction2(Response2);
    if (!isFetchSupported) {
      return false;
    }
    const isReadableStreamSupported = isFetchSupported && isFunction2(ReadableStream2);
    const encodeText = isFetchSupported && (typeof TextEncoder2 === "function" ? /* @__PURE__ */ ((encoder) => (str) => encoder.encode(str))(new TextEncoder2()) : async (str) => new Uint8Array(await new Request2(str).arrayBuffer()));
    const supportsRequestStream = isRequestSupported && isReadableStreamSupported && test(() => {
      let duplexAccessed = false;
      const request = new Request2(platform_default.origin, {
        body: new ReadableStream2(),
        method: "POST",
        get duplex() {
          duplexAccessed = true;
          return "half";
        }
      });
      const hasContentType = request.headers.has("Content-Type");
      if (request.body != null) {
        request.body.cancel();
      }
      return duplexAccessed && !hasContentType;
    });
    const supportsResponseStream = isResponseSupported && isReadableStreamSupported && test(() => utils_default.isReadableStream(new Response2("").body));
    const resolvers = {
      stream: supportsResponseStream && ((res) => res.body)
    };
    isFetchSupported && (() => {
      ["text", "arrayBuffer", "blob", "formData", "stream"].forEach((type) => {
        !resolvers[type] && (resolvers[type] = (res, config) => {
          let method = res && res[type];
          if (method) {
            return method.call(res);
          }
          throw new AxiosError_default(
            `Response type '${type}' is not supported`,
            AxiosError_default.ERR_NOT_SUPPORT,
            config
          );
        });
      });
    })();
    const getBodyLength = async (body) => {
      if (body == null) {
        return 0;
      }
      if (utils_default.isBlob(body)) {
        return body.size;
      }
      if (utils_default.isSpecCompliantForm(body)) {
        const _request = new Request2(platform_default.origin, {
          method: "POST",
          body
        });
        return (await _request.arrayBuffer()).byteLength;
      }
      if (utils_default.isArrayBufferView(body) || utils_default.isArrayBuffer(body)) {
        return body.byteLength;
      }
      if (utils_default.isURLSearchParams(body)) {
        body = body + "";
      }
      if (utils_default.isString(body)) {
        return (await encodeText(body)).byteLength;
      }
    };
    const resolveBodyLength = async (headers, body) => {
      const length = utils_default.toFiniteNumber(headers.getContentLength());
      return length == null ? getBodyLength(body) : length;
    };
    return async (config) => {
      let {
        url,
        method,
        data: data2,
        signal,
        cancelToken,
        timeout,
        onDownloadProgress,
        onUploadProgress,
        responseType,
        headers,
        withCredentials = "same-origin",
        fetchOptions,
        maxContentLength,
        maxBodyLength
      } = resolveConfig_default(config);
      const hasMaxContentLength = utils_default.isNumber(maxContentLength) && maxContentLength > -1;
      const hasMaxBodyLength = utils_default.isNumber(maxBodyLength) && maxBodyLength > -1;
      let _fetch = envFetch || fetch;
      responseType = responseType ? (responseType + "").toLowerCase() : "text";
      let composedSignal = composeSignals_default(
        [signal, cancelToken && cancelToken.toAbortSignal()],
        timeout
      );
      let request = null;
      const unsubscribe = composedSignal && composedSignal.unsubscribe && (() => {
        composedSignal.unsubscribe();
      });
      let requestContentLength;
      try {
        if (hasMaxContentLength && typeof url === "string" && url.startsWith("data:")) {
          const estimated = estimateDataURLDecodedBytes(url);
          if (estimated > maxContentLength) {
            throw new AxiosError_default(
              "maxContentLength size of " + maxContentLength + " exceeded",
              AxiosError_default.ERR_BAD_RESPONSE,
              config,
              request
            );
          }
        }
        if (hasMaxBodyLength && method !== "get" && method !== "head") {
          const outboundLength = await resolveBodyLength(headers, data2);
          if (typeof outboundLength === "number" && isFinite(outboundLength) && outboundLength > maxBodyLength) {
            throw new AxiosError_default(
              "Request body larger than maxBodyLength limit",
              AxiosError_default.ERR_BAD_REQUEST,
              config,
              request
            );
          }
        }
        if (onUploadProgress && supportsRequestStream && method !== "get" && method !== "head" && (requestContentLength = await resolveBodyLength(headers, data2)) !== 0) {
          let _request = new Request2(url, {
            method: "POST",
            body: data2,
            duplex: "half"
          });
          let contentTypeHeader;
          if (utils_default.isFormData(data2) && (contentTypeHeader = _request.headers.get("content-type"))) {
            headers.setContentType(contentTypeHeader);
          }
          if (_request.body) {
            const [onProgress, flush] = progressEventDecorator(
              requestContentLength,
              progressEventReducer(asyncDecorator(onUploadProgress))
            );
            data2 = trackStream(_request.body, DEFAULT_CHUNK_SIZE, onProgress, flush);
          }
        }
        if (!utils_default.isString(withCredentials)) {
          withCredentials = withCredentials ? "include" : "omit";
        }
        const isCredentialsSupported = isRequestSupported && "credentials" in Request2.prototype;
        if (utils_default.isFormData(data2)) {
          const contentType = headers.getContentType();
          if (contentType && /^multipart\/form-data/i.test(contentType) && !/boundary=/i.test(contentType)) {
            headers.delete("content-type");
          }
        }
        headers.set("User-Agent", "axios/" + VERSION, false);
        const resolvedOptions = {
          ...fetchOptions,
          signal: composedSignal,
          method: method.toUpperCase(),
          headers: toByteStringHeaderObject(headers.normalize()),
          body: data2,
          duplex: "half",
          credentials: isCredentialsSupported ? withCredentials : void 0
        };
        request = isRequestSupported && new Request2(url, resolvedOptions);
        let response = await (isRequestSupported ? _fetch(request, fetchOptions) : _fetch(url, resolvedOptions));
        if (hasMaxContentLength) {
          const declaredLength = utils_default.toFiniteNumber(response.headers.get("content-length"));
          if (declaredLength != null && declaredLength > maxContentLength) {
            throw new AxiosError_default(
              "maxContentLength size of " + maxContentLength + " exceeded",
              AxiosError_default.ERR_BAD_RESPONSE,
              config,
              request
            );
          }
        }
        const isStreamResponse = supportsResponseStream && (responseType === "stream" || responseType === "response");
        if (supportsResponseStream && response.body && (onDownloadProgress || hasMaxContentLength || isStreamResponse && unsubscribe)) {
          const options = {};
          ["status", "statusText", "headers"].forEach((prop) => {
            options[prop] = response[prop];
          });
          const responseContentLength = utils_default.toFiniteNumber(response.headers.get("content-length"));
          const [onProgress, flush] = onDownloadProgress && progressEventDecorator(
            responseContentLength,
            progressEventReducer(asyncDecorator(onDownloadProgress), true)
          ) || [];
          let bytesRead = 0;
          const onChunkProgress = (loadedBytes) => {
            if (hasMaxContentLength) {
              bytesRead = loadedBytes;
              if (bytesRead > maxContentLength) {
                throw new AxiosError_default(
                  "maxContentLength size of " + maxContentLength + " exceeded",
                  AxiosError_default.ERR_BAD_RESPONSE,
                  config,
                  request
                );
              }
            }
            onProgress && onProgress(loadedBytes);
          };
          response = new Response2(
            trackStream(response.body, DEFAULT_CHUNK_SIZE, onChunkProgress, () => {
              flush && flush();
              unsubscribe && unsubscribe();
            }),
            options
          );
        }
        responseType = responseType || "text";
        let responseData = await resolvers[utils_default.findKey(resolvers, responseType) || "text"](
          response,
          config
        );
        if (hasMaxContentLength && !supportsResponseStream && !isStreamResponse) {
          let materializedSize;
          if (responseData != null) {
            if (typeof responseData.byteLength === "number") {
              materializedSize = responseData.byteLength;
            } else if (typeof responseData.size === "number") {
              materializedSize = responseData.size;
            } else if (typeof responseData === "string") {
              materializedSize = typeof TextEncoder2 === "function" ? new TextEncoder2().encode(responseData).byteLength : responseData.length;
            }
          }
          if (typeof materializedSize === "number" && materializedSize > maxContentLength) {
            throw new AxiosError_default(
              "maxContentLength size of " + maxContentLength + " exceeded",
              AxiosError_default.ERR_BAD_RESPONSE,
              config,
              request
            );
          }
        }
        !isStreamResponse && unsubscribe && unsubscribe();
        return await new Promise((resolve, reject) => {
          settle(resolve, reject, {
            data: responseData,
            headers: AxiosHeaders_default.from(response.headers),
            status: response.status,
            statusText: response.statusText,
            config,
            request
          });
        });
      } catch (err) {
        unsubscribe && unsubscribe();
        if (composedSignal && composedSignal.aborted && composedSignal.reason instanceof AxiosError_default) {
          const canceledError = composedSignal.reason;
          canceledError.config = config;
          request && (canceledError.request = request);
          err !== canceledError && (canceledError.cause = err);
          throw canceledError;
        }
        if (err && err.name === "TypeError" && /Load failed|fetch/i.test(err.message)) {
          throw Object.assign(
            new AxiosError_default(
              "Network Error",
              AxiosError_default.ERR_NETWORK,
              config,
              request,
              err && err.response
            ),
            {
              cause: err.cause || err
            }
          );
        }
        throw AxiosError_default.from(err, err && err.code, config, request, err && err.response);
      }
    };
  };
  var seedCache = /* @__PURE__ */ new Map();
  var getFetch = (config) => {
    let env = config && config.env || {};
    const { fetch: fetch2, Request: Request2, Response: Response2 } = env;
    const seeds = [Request2, Response2, fetch2];
    let len = seeds.length, i = len, seed, target, map = seedCache;
    while (i--) {
      seed = seeds[i];
      target = map.get(seed);
      target === void 0 && map.set(seed, target = i ? /* @__PURE__ */ new Map() : factory(env));
      map = target;
    }
    return target;
  };
  var adapter = getFetch();

  // frontend-civic/node_modules/axios/lib/adapters/adapters.js
  var knownAdapters = {
    http: null_default,
    xhr: xhr_default,
    fetch: {
      get: getFetch
    }
  };
  utils_default.forEach(knownAdapters, (fn, value) => {
    if (fn) {
      try {
        Object.defineProperty(fn, "name", { __proto__: null, value });
      } catch (e) {
      }
      Object.defineProperty(fn, "adapterName", { __proto__: null, value });
    }
  });
  var renderReason = (reason) => `- ${reason}`;
  var isResolvedHandle = (adapter2) => utils_default.isFunction(adapter2) || adapter2 === null || adapter2 === false;
  function getAdapter(adapters, config) {
    adapters = utils_default.isArray(adapters) ? adapters : [adapters];
    const { length } = adapters;
    let nameOrAdapter;
    let adapter2;
    const rejectedReasons = {};
    for (let i = 0; i < length; i++) {
      nameOrAdapter = adapters[i];
      let id;
      adapter2 = nameOrAdapter;
      if (!isResolvedHandle(nameOrAdapter)) {
        adapter2 = knownAdapters[(id = String(nameOrAdapter)).toLowerCase()];
        if (adapter2 === void 0) {
          throw new AxiosError_default(`Unknown adapter '${id}'`);
        }
      }
      if (adapter2 && (utils_default.isFunction(adapter2) || (adapter2 = adapter2.get(config)))) {
        break;
      }
      rejectedReasons[id || "#" + i] = adapter2;
    }
    if (!adapter2) {
      const reasons = Object.entries(rejectedReasons).map(
        ([id, state]) => `adapter ${id} ` + (state === false ? "is not supported by the environment" : "is not available in the build")
      );
      let s = length ? reasons.length > 1 ? "since :\n" + reasons.map(renderReason).join("\n") : " " + renderReason(reasons[0]) : "as no adapter specified";
      throw new AxiosError_default(
        `There is no suitable adapter to dispatch the request ` + s,
        "ERR_NOT_SUPPORT"
      );
    }
    return adapter2;
  }
  var adapters_default = {
    /**
     * Resolve an adapter from a list of adapter names or functions.
     * @type {Function}
     */
    getAdapter,
    /**
     * Exposes all known adapters
     * @type {Object<string, Function|Object>}
     */
    adapters: knownAdapters
  };

  // frontend-civic/node_modules/axios/lib/core/dispatchRequest.js
  function throwIfCancellationRequested(config) {
    if (config.cancelToken) {
      config.cancelToken.throwIfRequested();
    }
    if (config.signal && config.signal.aborted) {
      throw new CanceledError_default(null, config);
    }
  }
  function dispatchRequest(config) {
    throwIfCancellationRequested(config);
    config.headers = AxiosHeaders_default.from(config.headers);
    config.data = transformData.call(config, config.transformRequest);
    if (["post", "put", "patch"].indexOf(config.method) !== -1) {
      config.headers.setContentType("application/x-www-form-urlencoded", false);
    }
    const adapter2 = adapters_default.getAdapter(config.adapter || defaults_default.adapter, config);
    return adapter2(config).then(
      function onAdapterResolution(response) {
        throwIfCancellationRequested(config);
        config.response = response;
        try {
          response.data = transformData.call(config, config.transformResponse, response);
        } finally {
          delete config.response;
        }
        response.headers = AxiosHeaders_default.from(response.headers);
        return response;
      },
      function onAdapterRejection(reason) {
        if (!isCancel(reason)) {
          throwIfCancellationRequested(config);
          if (reason && reason.response) {
            config.response = reason.response;
            try {
              reason.response.data = transformData.call(
                config,
                config.transformResponse,
                reason.response
              );
            } finally {
              delete config.response;
            }
            reason.response.headers = AxiosHeaders_default.from(reason.response.headers);
          }
        }
        return Promise.reject(reason);
      }
    );
  }

  // frontend-civic/node_modules/axios/lib/helpers/validator.js
  init_define_import_meta_env();
  var validators = {};
  ["object", "boolean", "number", "function", "string", "symbol"].forEach((type, i) => {
    validators[type] = function validator(thing) {
      return typeof thing === type || "a" + (i < 1 ? "n " : " ") + type;
    };
  });
  var deprecatedWarnings = {};
  validators.transitional = function transitional(validator, version, message) {
    function formatMessage(opt, desc) {
      return "[Axios v" + VERSION + "] Transitional option '" + opt + "'" + desc + (message ? ". " + message : "");
    }
    return (value, opt, opts) => {
      if (validator === false) {
        throw new AxiosError_default(
          formatMessage(opt, " has been removed" + (version ? " in " + version : "")),
          AxiosError_default.ERR_DEPRECATED
        );
      }
      if (version && !deprecatedWarnings[opt]) {
        deprecatedWarnings[opt] = true;
        console.warn(
          formatMessage(
            opt,
            " has been deprecated since v" + version + " and will be removed in the near future"
          )
        );
      }
      return validator ? validator(value, opt, opts) : true;
    };
  };
  validators.spelling = function spelling(correctSpelling) {
    return (value, opt) => {
      console.warn(`${opt} is likely a misspelling of ${correctSpelling}`);
      return true;
    };
  };
  function assertOptions(options, schema, allowUnknown) {
    if (typeof options !== "object") {
      throw new AxiosError_default("options must be an object", AxiosError_default.ERR_BAD_OPTION_VALUE);
    }
    const keys = Object.keys(options);
    let i = keys.length;
    while (i-- > 0) {
      const opt = keys[i];
      const validator = Object.prototype.hasOwnProperty.call(schema, opt) ? schema[opt] : void 0;
      if (validator) {
        const value = options[opt];
        const result = value === void 0 || validator(value, opt, options);
        if (result !== true) {
          throw new AxiosError_default(
            "option " + opt + " must be " + result,
            AxiosError_default.ERR_BAD_OPTION_VALUE
          );
        }
        continue;
      }
      if (allowUnknown !== true) {
        throw new AxiosError_default("Unknown option " + opt, AxiosError_default.ERR_BAD_OPTION);
      }
    }
  }
  var validator_default = {
    assertOptions,
    validators
  };

  // frontend-civic/node_modules/axios/lib/core/Axios.js
  var validators2 = validator_default.validators;
  var Axios = class {
    constructor(instanceConfig) {
      this.defaults = instanceConfig || {};
      this.interceptors = {
        request: new InterceptorManager_default(),
        response: new InterceptorManager_default()
      };
    }
    /**
     * Dispatch a request
     *
     * @param {String|Object} configOrUrl The config specific for this request (merged with this.defaults)
     * @param {?Object} config
     *
     * @returns {Promise} The Promise to be fulfilled
     */
    async request(configOrUrl, config) {
      try {
        return await this._request(configOrUrl, config);
      } catch (err) {
        if (err instanceof Error) {
          let dummy = {};
          Error.captureStackTrace ? Error.captureStackTrace(dummy) : dummy = new Error();
          const stack = (() => {
            if (!dummy.stack) {
              return "";
            }
            const firstNewlineIndex = dummy.stack.indexOf("\n");
            return firstNewlineIndex === -1 ? "" : dummy.stack.slice(firstNewlineIndex + 1);
          })();
          try {
            if (!err.stack) {
              err.stack = stack;
            } else if (stack) {
              const firstNewlineIndex = stack.indexOf("\n");
              const secondNewlineIndex = firstNewlineIndex === -1 ? -1 : stack.indexOf("\n", firstNewlineIndex + 1);
              const stackWithoutTwoTopLines = secondNewlineIndex === -1 ? "" : stack.slice(secondNewlineIndex + 1);
              if (!String(err.stack).endsWith(stackWithoutTwoTopLines)) {
                err.stack += "\n" + stack;
              }
            }
          } catch (e) {
          }
        }
        throw err;
      }
    }
    _request(configOrUrl, config) {
      if (typeof configOrUrl === "string") {
        config = config || {};
        config.url = configOrUrl;
      } else {
        config = configOrUrl || {};
      }
      config = mergeConfig(this.defaults, config);
      const { transitional: transitional2, paramsSerializer, headers } = config;
      if (transitional2 !== void 0) {
        validator_default.assertOptions(
          transitional2,
          {
            silentJSONParsing: validators2.transitional(validators2.boolean),
            forcedJSONParsing: validators2.transitional(validators2.boolean),
            clarifyTimeoutError: validators2.transitional(validators2.boolean),
            legacyInterceptorReqResOrdering: validators2.transitional(validators2.boolean)
          },
          false
        );
      }
      if (paramsSerializer != null) {
        if (utils_default.isFunction(paramsSerializer)) {
          config.paramsSerializer = {
            serialize: paramsSerializer
          };
        } else {
          validator_default.assertOptions(
            paramsSerializer,
            {
              encode: validators2.function,
              serialize: validators2.function
            },
            true
          );
        }
      }
      if (config.allowAbsoluteUrls !== void 0) {
      } else if (this.defaults.allowAbsoluteUrls !== void 0) {
        config.allowAbsoluteUrls = this.defaults.allowAbsoluteUrls;
      } else {
        config.allowAbsoluteUrls = true;
      }
      validator_default.assertOptions(
        config,
        {
          baseUrl: validators2.spelling("baseURL"),
          withXsrfToken: validators2.spelling("withXSRFToken")
        },
        true
      );
      config.method = (config.method || this.defaults.method || "get").toLowerCase();
      let contextHeaders = headers && utils_default.merge(headers.common, headers[config.method]);
      headers && utils_default.forEach(["delete", "get", "head", "post", "put", "patch", "query", "common"], (method) => {
        delete headers[method];
      });
      config.headers = AxiosHeaders_default.concat(contextHeaders, headers);
      const requestInterceptorChain = [];
      let synchronousRequestInterceptors = true;
      this.interceptors.request.forEach(function unshiftRequestInterceptors(interceptor) {
        if (typeof interceptor.runWhen === "function" && interceptor.runWhen(config) === false) {
          return;
        }
        synchronousRequestInterceptors = synchronousRequestInterceptors && interceptor.synchronous;
        const transitional3 = config.transitional || transitional_default;
        const legacyInterceptorReqResOrdering = transitional3 && transitional3.legacyInterceptorReqResOrdering;
        if (legacyInterceptorReqResOrdering) {
          requestInterceptorChain.unshift(interceptor.fulfilled, interceptor.rejected);
        } else {
          requestInterceptorChain.push(interceptor.fulfilled, interceptor.rejected);
        }
      });
      const responseInterceptorChain = [];
      this.interceptors.response.forEach(function pushResponseInterceptors(interceptor) {
        responseInterceptorChain.push(interceptor.fulfilled, interceptor.rejected);
      });
      let promise;
      let i = 0;
      let len;
      if (!synchronousRequestInterceptors) {
        const chain = [dispatchRequest.bind(this), void 0];
        chain.unshift(...requestInterceptorChain);
        chain.push(...responseInterceptorChain);
        len = chain.length;
        promise = Promise.resolve(config);
        while (i < len) {
          promise = promise.then(chain[i++], chain[i++]);
        }
        return promise;
      }
      len = requestInterceptorChain.length;
      let newConfig = config;
      while (i < len) {
        const onFulfilled = requestInterceptorChain[i++];
        const onRejected = requestInterceptorChain[i++];
        try {
          newConfig = onFulfilled(newConfig);
        } catch (error) {
          onRejected.call(this, error);
          break;
        }
      }
      try {
        promise = dispatchRequest.call(this, newConfig);
      } catch (error) {
        return Promise.reject(error);
      }
      i = 0;
      len = responseInterceptorChain.length;
      while (i < len) {
        promise = promise.then(responseInterceptorChain[i++], responseInterceptorChain[i++]);
      }
      return promise;
    }
    getUri(config) {
      config = mergeConfig(this.defaults, config);
      const fullPath = buildFullPath(config.baseURL, config.url, config.allowAbsoluteUrls);
      return buildURL(fullPath, config.params, config.paramsSerializer);
    }
  };
  utils_default.forEach(["delete", "get", "head", "options"], function forEachMethodNoData(method) {
    Axios.prototype[method] = function(url, config) {
      return this.request(
        mergeConfig(config || {}, {
          method,
          url,
          data: (config || {}).data
        })
      );
    };
  });
  utils_default.forEach(["post", "put", "patch", "query"], function forEachMethodWithData(method) {
    function generateHTTPMethod(isForm) {
      return function httpMethod(url, data2, config) {
        return this.request(
          mergeConfig(config || {}, {
            method,
            headers: isForm ? {
              "Content-Type": "multipart/form-data"
            } : {},
            url,
            data: data2
          })
        );
      };
    }
    Axios.prototype[method] = generateHTTPMethod();
    if (method !== "query") {
      Axios.prototype[method + "Form"] = generateHTTPMethod(true);
    }
  });
  var Axios_default = Axios;

  // frontend-civic/node_modules/axios/lib/cancel/CancelToken.js
  init_define_import_meta_env();
  var CancelToken = class _CancelToken {
    constructor(executor) {
      if (typeof executor !== "function") {
        throw new TypeError("executor must be a function.");
      }
      let resolvePromise;
      this.promise = new Promise(function promiseExecutor(resolve) {
        resolvePromise = resolve;
      });
      const token = this;
      this.promise.then((cancel) => {
        if (!token._listeners) return;
        let i = token._listeners.length;
        while (i-- > 0) {
          token._listeners[i](cancel);
        }
        token._listeners = null;
      });
      this.promise.then = (onfulfilled) => {
        let _resolve;
        const promise = new Promise((resolve) => {
          token.subscribe(resolve);
          _resolve = resolve;
        }).then(onfulfilled);
        promise.cancel = function reject() {
          token.unsubscribe(_resolve);
        };
        return promise;
      };
      executor(function cancel(message, config, request) {
        if (token.reason) {
          return;
        }
        token.reason = new CanceledError_default(message, config, request);
        resolvePromise(token.reason);
      });
    }
    /**
     * Throws a `CanceledError` if cancellation has been requested.
     */
    throwIfRequested() {
      if (this.reason) {
        throw this.reason;
      }
    }
    /**
     * Subscribe to the cancel signal
     */
    subscribe(listener) {
      if (this.reason) {
        listener(this.reason);
        return;
      }
      if (this._listeners) {
        this._listeners.push(listener);
      } else {
        this._listeners = [listener];
      }
    }
    /**
     * Unsubscribe from the cancel signal
     */
    unsubscribe(listener) {
      if (!this._listeners) {
        return;
      }
      const index = this._listeners.indexOf(listener);
      if (index !== -1) {
        this._listeners.splice(index, 1);
      }
    }
    toAbortSignal() {
      const controller = new AbortController();
      const abort = (err) => {
        controller.abort(err);
      };
      this.subscribe(abort);
      controller.signal.unsubscribe = () => this.unsubscribe(abort);
      return controller.signal;
    }
    /**
     * Returns an object that contains a new `CancelToken` and a function that, when called,
     * cancels the `CancelToken`.
     */
    static source() {
      let cancel;
      const token = new _CancelToken(function executor(c) {
        cancel = c;
      });
      return {
        token,
        cancel
      };
    }
  };
  var CancelToken_default = CancelToken;

  // frontend-civic/node_modules/axios/lib/helpers/spread.js
  init_define_import_meta_env();
  function spread(callback) {
    return function wrap(arr) {
      return callback.apply(null, arr);
    };
  }

  // frontend-civic/node_modules/axios/lib/helpers/isAxiosError.js
  init_define_import_meta_env();
  function isAxiosError(payload) {
    return utils_default.isObject(payload) && payload.isAxiosError === true;
  }

  // frontend-civic/node_modules/axios/lib/helpers/HttpStatusCode.js
  init_define_import_meta_env();
  var HttpStatusCode = {
    Continue: 100,
    SwitchingProtocols: 101,
    Processing: 102,
    EarlyHints: 103,
    Ok: 200,
    Created: 201,
    Accepted: 202,
    NonAuthoritativeInformation: 203,
    NoContent: 204,
    ResetContent: 205,
    PartialContent: 206,
    MultiStatus: 207,
    AlreadyReported: 208,
    ImUsed: 226,
    MultipleChoices: 300,
    MovedPermanently: 301,
    Found: 302,
    SeeOther: 303,
    NotModified: 304,
    UseProxy: 305,
    Unused: 306,
    TemporaryRedirect: 307,
    PermanentRedirect: 308,
    BadRequest: 400,
    Unauthorized: 401,
    PaymentRequired: 402,
    Forbidden: 403,
    NotFound: 404,
    MethodNotAllowed: 405,
    NotAcceptable: 406,
    ProxyAuthenticationRequired: 407,
    RequestTimeout: 408,
    Conflict: 409,
    Gone: 410,
    LengthRequired: 411,
    PreconditionFailed: 412,
    PayloadTooLarge: 413,
    UriTooLong: 414,
    UnsupportedMediaType: 415,
    RangeNotSatisfiable: 416,
    ExpectationFailed: 417,
    ImATeapot: 418,
    MisdirectedRequest: 421,
    UnprocessableEntity: 422,
    Locked: 423,
    FailedDependency: 424,
    TooEarly: 425,
    UpgradeRequired: 426,
    PreconditionRequired: 428,
    TooManyRequests: 429,
    RequestHeaderFieldsTooLarge: 431,
    UnavailableForLegalReasons: 451,
    InternalServerError: 500,
    NotImplemented: 501,
    BadGateway: 502,
    ServiceUnavailable: 503,
    GatewayTimeout: 504,
    HttpVersionNotSupported: 505,
    VariantAlsoNegotiates: 506,
    InsufficientStorage: 507,
    LoopDetected: 508,
    NotExtended: 510,
    NetworkAuthenticationRequired: 511,
    WebServerIsDown: 521,
    ConnectionTimedOut: 522,
    OriginIsUnreachable: 523,
    TimeoutOccurred: 524,
    SslHandshakeFailed: 525,
    InvalidSslCertificate: 526
  };
  Object.entries(HttpStatusCode).forEach(([key, value]) => {
    HttpStatusCode[value] = key;
  });
  var HttpStatusCode_default = HttpStatusCode;

  // frontend-civic/node_modules/axios/lib/axios.js
  function createInstance(defaultConfig) {
    const context = new Axios_default(defaultConfig);
    const instance = bind(Axios_default.prototype.request, context);
    utils_default.extend(instance, Axios_default.prototype, context, { allOwnKeys: true });
    utils_default.extend(instance, context, null, { allOwnKeys: true });
    instance.create = function create2(instanceConfig) {
      return createInstance(mergeConfig(defaultConfig, instanceConfig));
    };
    return instance;
  }
  var axios = createInstance(defaults_default);
  axios.Axios = Axios_default;
  axios.CanceledError = CanceledError_default;
  axios.CancelToken = CancelToken_default;
  axios.isCancel = isCancel;
  axios.VERSION = VERSION;
  axios.toFormData = toFormData_default;
  axios.AxiosError = AxiosError_default;
  axios.Cancel = axios.CanceledError;
  axios.all = function all(promises) {
    return Promise.all(promises);
  };
  axios.spread = spread;
  axios.isAxiosError = isAxiosError;
  axios.mergeConfig = mergeConfig;
  axios.AxiosHeaders = AxiosHeaders_default;
  axios.formToJSON = (thing) => formDataToJSON_default(utils_default.isHTMLForm(thing) ? new FormData(thing) : thing);
  axios.getAdapter = adapters_default.getAdapter;
  axios.HttpStatusCode = HttpStatusCode_default;
  axios.default = axios;
  var axios_default = axios;

  // frontend-civic/node_modules/axios/index.js
  var {
    Axios: Axios2,
    AxiosError: AxiosError2,
    CanceledError: CanceledError2,
    isCancel: isCancel2,
    CancelToken: CancelToken2,
    VERSION: VERSION2,
    all: all2,
    Cancel,
    isAxiosError: isAxiosError2,
    spread: spread2,
    toFormData: toFormData2,
    AxiosHeaders: AxiosHeaders2,
    HttpStatusCode: HttpStatusCode2,
    formToJSON,
    getAdapter: getAdapter2,
    mergeConfig: mergeConfig2,
    create
  } = axios_default;

  // frontend-civic/src/api/client.ts
  var USER_ID_KEY = "civic-user-id";
  var ACCESS_TOKEN_KEY = "arena-access-token";
  function getAnonymousUserId() {
    let id = localStorage.getItem(USER_ID_KEY);
    if (!id) {
      id = crypto.randomUUID();
      localStorage.setItem(USER_ID_KEY, id);
    }
    return id;
  }
  var civicApi = axios_default.create({
    baseURL: define_import_meta_env_default.VITE_CIVIC_API_URL ?? "http://localhost:5050/api",
    headers: { "Content-Type": "application/json" }
  });
  civicApi.interceptors.request.use((config) => {
    const token = localStorage.getItem(ACCESS_TOKEN_KEY);
    if (token) {
      config.headers.set("Authorization", `Bearer ${token}`);
    } else {
      config.headers.set("X-User-Id", getAnonymousUserId());
    }
    return config;
  });

  // frontend-civic/src/api/elections.ts
  function buildParams(q) {
    if (!q) return void 0;
    const params = {};
    if (q.scope) params.scope = q.scope;
    if (q.region) params.region = q.region;
    return Object.keys(params).length ? params : void 0;
  }
  async function getNextElection(query) {
    try {
      const { data: data2 } = await civicApi.get("/elections/next", {
        params: buildParams(query)
      });
      return data2;
    } catch (err) {
      if (err.response?.status === 404) {
        return void 0;
      }
      throw err;
    }
  }

  // frontend-civic/src/prototypes/magazine/components/CountdownTimer.tsx
  function diffParts(target, now) {
    const ms = Math.max(0, target.getTime() - now.getTime());
    const totalSeconds = Math.floor(ms / 1e3);
    const days = Math.floor(totalSeconds / 86400);
    const hours = Math.floor(totalSeconds % 86400 / 3600);
    const minutes = Math.floor(totalSeconds % 3600 / 60);
    const seconds = totalSeconds % 60;
    return { days, hours, minutes, seconds };
  }
  function pad(n) {
    return n.toString().padStart(2, "0");
  }
  function dateLabel(iso) {
    const d = new Date(iso);
    return d.toLocaleDateString(void 0, {
      weekday: "long",
      month: "long",
      day: "numeric",
      year: "numeric"
    });
  }
  function CountdownTimer({ scope, region, testId, className }) {
    const containerClass = className ?? "my-10";
    const [election, setElection] = (0, import_react6.useState)(void 0);
    const [now, setNow] = (0, import_react6.useState)(() => /* @__PURE__ */ new Date());
    (0, import_react6.useEffect)(() => {
      let cancelled = false;
      void getNextElection({ scope, region }).then((e) => {
        if (!cancelled) setElection(e ?? null);
      });
      return () => {
        cancelled = true;
      };
    }, [scope, region]);
    (0, import_react6.useEffect)(() => {
      const id = window.setInterval(() => setNow(/* @__PURE__ */ new Date()), 1e3);
      return () => window.clearInterval(id);
    }, []);
    if (election === void 0) {
      return /* @__PURE__ */ React.createElement(
        "section",
        {
          className: `${containerClass} border border-[var(--border)] bg-[var(--bg-elev)] p-6`,
          "data-testid": testId ?? `countdown-${scope.toLowerCase()}`
        },
        /* @__PURE__ */ React.createElement("p", { className: "text-xs uppercase tracking-[0.3em] text-[var(--muted)]" }, "Next ", scope.toLowerCase(), " election"),
        /* @__PURE__ */ React.createElement("p", { className: "mt-2 text-sm text-[var(--muted)]" }, "Loading\u2026")
      );
    }
    if (election === null) {
      return /* @__PURE__ */ React.createElement(
        "section",
        {
          className: `${containerClass} border border-[var(--border)] bg-[var(--bg-elev)] p-6`,
          "data-testid": testId ?? `countdown-${scope.toLowerCase()}`
        },
        /* @__PURE__ */ React.createElement("p", { className: "text-xs uppercase tracking-[0.3em] text-[var(--muted)]" }, "Next ", scope.toLowerCase(), " election"),
        /* @__PURE__ */ React.createElement("p", { className: "mt-2 text-sm text-[var(--muted)]" }, "No upcoming election on file.")
      );
    }
    const target = new Date(election.scheduledAt);
    const { days, hours, minutes, seconds } = diffParts(target, now);
    return /* @__PURE__ */ React.createElement(
      "section",
      {
        className: `${containerClass} border border-[var(--border)] bg-[var(--bg-elev)] p-6`,
        "data-testid": testId ?? `countdown-${scope.toLowerCase()}`
      },
      /* @__PURE__ */ React.createElement("p", { className: "text-xs font-semibold uppercase tracking-[0.3em] text-[var(--accent)]" }, "Next ", scope.toLowerCase(), " election"),
      /* @__PURE__ */ React.createElement(
        "h2",
        {
          className: "display mt-2 text-3xl",
          "data-testid": `${testId ?? `countdown-${scope.toLowerCase()}`}-name`
        },
        election.name
      ),
      /* @__PURE__ */ React.createElement("p", { className: "mt-1 text-sm text-[var(--fg-soft)]" }, dateLabel(election.scheduledAt)),
      /* @__PURE__ */ React.createElement(
        "dl",
        {
          className: "mt-5 grid max-w-md grid-cols-4 gap-3 text-center",
          "data-testid": `${testId ?? `countdown-${scope.toLowerCase()}`}-clock`
        },
        /* @__PURE__ */ React.createElement(CountdownCell, { label: "days", value: days, testId: "cd-days" }),
        /* @__PURE__ */ React.createElement(CountdownCell, { label: "hrs", value: hours, testId: "cd-hours", pad: true }),
        /* @__PURE__ */ React.createElement(CountdownCell, { label: "min", value: minutes, testId: "cd-minutes", pad: true }),
        /* @__PURE__ */ React.createElement(CountdownCell, { label: "sec", value: seconds, testId: "cd-seconds", pad: true })
      )
    );
  }
  function CountdownCell({
    label,
    value,
    testId,
    pad: padded
  }) {
    return /* @__PURE__ */ React.createElement("div", { className: "rounded border border-[var(--border)] bg-[var(--bg)] py-3" }, /* @__PURE__ */ React.createElement("dt", { className: "text-[10px] uppercase tracking-[0.2em] text-[var(--muted)]" }, label), /* @__PURE__ */ React.createElement(
      "dd",
      {
        className: "display mt-1 text-2xl tabular-nums",
        "data-testid": testId
      },
      padded ? pad(value) : value
    ));
  }

  // frontend-civic/src/prototypes/magazine/components/CoverStory.tsx
  init_define_import_meta_env();
  function CoverStory({ briefing }) {
    return /* @__PURE__ */ React.createElement(
      Link,
      {
        to: `/briefings/${briefing.slug}`,
        className: "group block overflow-hidden border border-[var(--border)]"
      },
      /* @__PURE__ */ React.createElement(
        "div",
        {
          className: "relative flex h-[360px] items-end p-8 text-white md:h-[460px] md:p-16",
          style: {
            background: "linear-gradient(135deg, oklch(0.42 0.18 30) 0%, oklch(0.22 0.12 280) 100%)"
          }
        },
        /* @__PURE__ */ React.createElement("div", { className: "absolute right-6 top-6 flex items-center gap-2 text-xs font-semibold uppercase tracking-[0.2em] md:right-10 md:top-10" }, "Cover story", /* @__PURE__ */ React.createElement(ArrowUpRight, { className: "h-4 w-4 transition group-hover:rotate-45" })),
        /* @__PURE__ */ React.createElement("div", { className: "min-w-0 max-w-3xl" }, /* @__PURE__ */ React.createElement("p", { className: "display text-xs font-semibold uppercase tracking-[0.3em]" }, briefing.institution, " \xB7 ", briefing.status), /* @__PURE__ */ React.createElement("h2", { className: "display mt-3 text-3xl [overflow-wrap:anywhere] hyphens-auto md:text-6xl" }, briefing.headline), /* @__PURE__ */ React.createElement("p", { className: "mt-3 line-clamp-3 max-w-xl text-sm leading-relaxed text-white/90 [overflow-wrap:anywhere] md:mt-4 md:line-clamp-2 md:text-base" }, briefing.summary30))
      )
    );
  }

  // frontend-civic/src/prototypes/magazine/components/CampaignPostCard.tsx
  init_define_import_meta_env();
  var import_react7 = __toESM(require_react_shim(), 1);

  // frontend-civic/src/lib/campaignVisuals.ts
  init_define_import_meta_env();
  var TONE_META = {
    Stern: { label: "Stern", color: "#475569" },
    Angry: { label: "Defiant", color: "#dc2626" },
    Casual: { label: "Casual", color: "#0891b2" },
    Hopeful: { label: "Hopeful", color: "#16a34a" },
    Sarcastic: { label: "Dry", color: "#9333ea" },
    Presidential: { label: "Statesmanlike", color: "#1d4ed8" },
    Folksy: { label: "Folksy", color: "#d97706" },
    Wonkish: { label: "Wonkish", color: "#0d9488" }
  };
  function toneColor(tone) {
    return TONE_META[tone]?.color ?? "#6366f1";
  }
  function intensityBorderWidth(intensity) {
    return Math.min(5, Math.max(1, intensity));
  }
  function netSentimentColor(net) {
    if (net > 0.15) {
      const a = Math.min(0.35, 0.12 + net * 0.25);
      return `rgba(22, 163, 74, ${a.toFixed(2)})`;
    }
    if (net < -0.15) {
      const a = Math.min(0.35, 0.12 + Math.abs(net) * 0.25);
      return `rgba(220, 38, 38, ${a.toFixed(2)})`;
    }
    return "rgba(100, 116, 139, 0.10)";
  }

  // frontend-civic/src/api/campaign.ts
  init_define_import_meta_env();
  async function reactToPost(id, type) {
    const { data: data2 } = await civicApi.post(`/posts/${id}/reactions`, { type });
    return data2;
  }
  async function removePostReaction(id) {
    const { data: data2 } = await civicApi.delete(`/posts/${id}/reactions`);
    return data2;
  }
  async function reactToFragment(id, fragmentId, type) {
    const { data: data2 } = await civicApi.post(
      `/posts/${id}/fragments/${fragmentId}/reactions`,
      { type }
    );
    return data2;
  }

  // frontend-civic/src/prototypes/magazine/components/CampaignPostCard.tsx
  function timeAgo(iso) {
    const secs = Math.max(0, (Date.now() - new Date(iso).getTime()) / 1e3);
    if (secs < 60) return "now";
    const mins = Math.floor(secs / 60);
    if (mins < 60) return `${mins}m ago`;
    const hrs = Math.floor(mins / 60);
    if (hrs < 24) return `${hrs}h ago`;
    return `${Math.floor(hrs / 24)}d ago`;
  }
  function initialFragState(fragments) {
    const state = {};
    for (const f of fragments) {
      const total = f.up + f.down;
      state[f.id] = { up: f.up, down: f.down, net: total === 0 ? 0 : (f.up - f.down) / total };
    }
    return state;
  }
  function HeatBody({
    post,
    fragState,
    activeId,
    onSelect
  }) {
    const frags = [...post.fragments].sort((a, b) => a.start - b.start);
    const nodes = [];
    let cursor = 0;
    frags.forEach((f) => {
      if (f.start > cursor) nodes.push(/* @__PURE__ */ React.createElement("span", { key: `gap-${f.id}` }, post.body.slice(cursor, f.start)));
      const fs = fragState[f.id] ?? { up: 0, down: 0, net: 0 };
      nodes.push(
        /* @__PURE__ */ React.createElement(
          "button",
          {
            key: f.id,
            type: "button",
            onClick: () => onSelect(f.id),
            className: cn(
              "rounded px-0.5 text-left transition",
              activeId === f.id && "ring-2 ring-indigo-400"
            ),
            style: { backgroundColor: netSentimentColor(fs.net) },
            "data-testid": "fragment-span",
            title: `\u{1F44D} ${fs.up} \xB7 \u{1F44E} ${fs.down}`
          },
          post.body.slice(f.start, f.end)
        )
      );
      cursor = Math.max(cursor, f.end);
    });
    if (cursor < post.body.length) nodes.push(/* @__PURE__ */ React.createElement("span", { key: "tail" }, post.body.slice(cursor)));
    return /* @__PURE__ */ React.createElement("p", { className: "text-lg leading-relaxed text-[var(--fg)]" }, nodes);
  }
  function CampaignPostCard({
    post,
    showCompare = true,
    initialMine = null,
    votingDisabled = false,
    votingDisabledLabel,
    onReact,
    onRemoveReaction
  }) {
    const candidate = post.candidate;
    const [up, setUp] = (0, import_react7.useState)(post.up);
    const [down, setDown] = (0, import_react7.useState)(post.down);
    const [mine, setMine] = (0, import_react7.useState)(initialMine);
    const [heatOn, setHeatOn] = (0, import_react7.useState)(false);
    const [fragState, setFragState] = (0, import_react7.useState)(() => initialFragState(post.fragments));
    const [activeFrag, setActiveFrag] = (0, import_react7.useState)(null);
    const [busy, setBusy] = (0, import_react7.useState)(false);
    const tone = TONE_META[post.tone] ?? TONE_META.Casual;
    const accent = toneColor(post.tone);
    async function vote(type) {
      if (busy || votingDisabled) return;
      setBusy(true);
      try {
        const removing = mine === type;
        const res = removing ? await (onRemoveReaction ? onRemoveReaction() : removePostReaction(post.id)) : await (onReact ? onReact(type) : reactToPost(post.id, type));
        setUp(res.postUp);
        setDown(res.postDown);
        setMine(removing ? null : type);
      } finally {
        setBusy(false);
      }
    }
    async function voteFragment(fragmentId, type) {
      if (votingDisabled) return;
      const res = await reactToFragment(post.id, fragmentId, type);
      if (res.fragmentUp != null && res.fragmentDown != null) {
        const total = res.fragmentUp + res.fragmentDown;
        setFragState((s) => ({
          ...s,
          [fragmentId]: {
            up: res.fragmentUp,
            down: res.fragmentDown,
            net: total === 0 ? 0 : (res.fragmentUp - res.fragmentDown) / total
          }
        }));
      }
    }
    const officeLine = candidate ? candidate.office === "President" ? "President" : candidate.office === "Senate" ? `Senate \xB7 ${candidate.state}` : `House \xB7 ${candidate.state}-${candidate.district}` : "";
    return /* @__PURE__ */ React.createElement(
      "article",
      {
        className: "border bg-[var(--bg-elev)] p-5",
        style: { borderColor: accent, borderLeftWidth: intensityBorderWidth(post.intensity) + 2 },
        "data-testid": "campaign-post-card"
      },
      /* @__PURE__ */ React.createElement("header", { className: "flex items-start gap-3" }, candidate && /* @__PURE__ */ React.createElement(CandidateAvatar, { candidate }), /* @__PURE__ */ React.createElement("div", { className: "min-w-0 flex-1" }, /* @__PURE__ */ React.createElement("div", { className: "flex flex-wrap items-center gap-x-2 gap-y-1" }, candidate ? /* @__PURE__ */ React.createElement(
        Link,
        {
          to: `/candidates/${candidate.slug}`,
          className: "font-semibold text-[var(--fg)] hover:text-[var(--accent)]"
        },
        candidate.name
      ) : /* @__PURE__ */ React.createElement("span", { className: "font-semibold" }, "Candidate"), /* @__PURE__ */ React.createElement("span", { className: "text-sm text-[var(--muted)]" }, officeLine, candidate && ` \xB7 ${candidate.party}`, " \xB7 ", timeAgo(post.createdAt))), /* @__PURE__ */ React.createElement("div", { className: "mt-1 flex flex-wrap items-center gap-2" }, /* @__PURE__ */ React.createElement(
        "span",
        {
          className: "inline-flex items-center gap-1 rounded-full px-2 py-0.5 text-[11px] font-semibold uppercase tracking-wider text-white",
          style: { backgroundColor: accent },
          "data-testid": "tone-chip"
        },
        post.intensity >= 5 && /* @__PURE__ */ React.createElement(Flame, { className: "h-3 w-3" }),
        post.intensityLabel,
        " \xB7 ",
        tone.label
      ), /* @__PURE__ */ React.createElement(DisclaimerBadge, null)))),
      /* @__PURE__ */ React.createElement("div", { className: "mt-4" }, heatOn ? /* @__PURE__ */ React.createElement(HeatBody, { post, fragState, activeId: activeFrag, onSelect: setActiveFrag }) : /* @__PURE__ */ React.createElement("p", { className: "text-lg leading-relaxed text-[var(--fg)]" }, post.body)),
      heatOn && activeFrag && !votingDisabled && /* @__PURE__ */ React.createElement(
        "div",
        {
          className: "mt-3 flex items-center gap-2 rounded border border-[var(--border)] bg-[var(--bg)] p-2 text-sm",
          "data-testid": "fragment-react-bar"
        },
        /* @__PURE__ */ React.createElement("span", { className: "truncate text-[var(--muted)]" }, "React to this line:"),
        /* @__PURE__ */ React.createElement(
          "button",
          {
            type: "button",
            onClick: () => voteFragment(activeFrag, "up"),
            className: "inline-flex items-center gap-1 rounded-full border border-[var(--border)] px-2 py-0.5 hover:border-green-500",
            "data-testid": "fragment-up"
          },
          /* @__PURE__ */ React.createElement(ThumbsUp, { className: "h-3 w-3" }),
          " ",
          fragState[activeFrag]?.up ?? 0
        ),
        /* @__PURE__ */ React.createElement(
          "button",
          {
            type: "button",
            onClick: () => voteFragment(activeFrag, "down"),
            className: "inline-flex items-center gap-1 rounded-full border border-[var(--border)] px-2 py-0.5 hover:border-red-500",
            "data-testid": "fragment-down"
          },
          /* @__PURE__ */ React.createElement(ThumbsDown, { className: "h-3 w-3" }),
          " ",
          fragState[activeFrag]?.down ?? 0
        )
      ),
      post.triggerBriefingSlug && /* @__PURE__ */ React.createElement(
        Link,
        {
          to: `/briefings/${post.triggerBriefingSlug}`,
          className: "mt-4 block rounded-lg border border-[var(--border)] bg-[var(--bg)] p-3 transition hover:border-[var(--accent)]",
          "data-testid": "post-trigger-briefing"
        },
        /* @__PURE__ */ React.createElement("span", { className: "flex items-center gap-1.5 text-[11px] font-semibold uppercase tracking-wider text-[var(--muted)]" }, /* @__PURE__ */ React.createElement(Quote, { className: "h-3 w-3 shrink-0" }), " Responding to"),
        /* @__PURE__ */ React.createElement("span", { className: "mt-1 block font-semibold text-[var(--fg)]" }, post.triggerBriefingHeadline ?? post.triggerBriefingSlug),
        post.triggerBriefingSummary && /* @__PURE__ */ React.createElement(
          "span",
          {
            className: "mt-1 block text-sm leading-relaxed text-[var(--fg-soft)]",
            "data-testid": "post-trigger-briefing-snippet"
          },
          post.triggerBriefingSummary
        ),
        /* @__PURE__ */ React.createElement("span", { className: "mt-1.5 block text-xs font-semibold text-[var(--accent)]" }, "Read the briefing \u2192")
      ),
      post.citedReference && /* @__PURE__ */ React.createElement("p", { className: "mt-2 text-xs text-[var(--muted)]", "data-testid": "post-cited-reference" }, "Source: ", post.citedReference),
      /* @__PURE__ */ React.createElement("footer", { className: "mt-4 flex flex-wrap items-center gap-2" }, /* @__PURE__ */ React.createElement(
        "button",
        {
          type: "button",
          disabled: busy || votingDisabled,
          title: votingDisabled ? votingDisabledLabel : void 0,
          onClick: () => vote("up"),
          className: cn(
            "inline-flex items-center gap-1 rounded-full border px-3 py-1 text-sm font-semibold transition",
            votingDisabled && "cursor-not-allowed opacity-50",
            mine === "up" ? "border-green-600 bg-green-50 text-green-700" : "border-[var(--border)] text-[var(--fg-soft)] hover:border-green-500"
          ),
          "data-testid": "post-up"
        },
        /* @__PURE__ */ React.createElement(ThumbsUp, { className: "h-4 w-4" }),
        " ",
        up
      ), /* @__PURE__ */ React.createElement(
        "button",
        {
          type: "button",
          disabled: busy || votingDisabled,
          title: votingDisabled ? votingDisabledLabel : void 0,
          onClick: () => vote("down"),
          className: cn(
            "inline-flex items-center gap-1 rounded-full border px-3 py-1 text-sm font-semibold transition",
            votingDisabled && "cursor-not-allowed opacity-50",
            mine === "down" ? "border-red-600 bg-red-50 text-red-700" : "border-[var(--border)] text-[var(--fg-soft)] hover:border-red-500"
          ),
          "data-testid": "post-down"
        },
        /* @__PURE__ */ React.createElement(ThumbsDown, { className: "h-4 w-4" }),
        " ",
        down
      ), /* @__PURE__ */ React.createElement(
        "button",
        {
          type: "button",
          onClick: () => setHeatOn((v) => !v),
          className: cn(
            "inline-flex items-center gap-1 rounded-full border px-3 py-1 text-sm font-semibold transition",
            heatOn ? "border-indigo-500 bg-indigo-50 text-indigo-700" : "border-[var(--border)] text-[var(--fg-soft)] hover:border-indigo-400"
          ),
          "data-testid": "heatmap-toggle"
        },
        /* @__PURE__ */ React.createElement(Highlighter, { className: "h-4 w-4" }),
        " Heat map"
      ), showCompare && /* @__PURE__ */ React.createElement(
        Link,
        {
          to: `/posts/${post.id}`,
          className: "ml-auto text-sm font-semibold text-[var(--accent)] hover:underline",
          "data-testid": "post-open"
        },
        "Open \u2192"
      ))
    );
  }

  // frontend-civic/src/prototypes/magazine/components/SharePreviewCard.tsx
  init_define_import_meta_env();
  function SharePreviewCard({ briefing }) {
    return /* @__PURE__ */ React.createElement("div", { className: "my-10 border border-[var(--border)] bg-[var(--bg-elev)]" }, /* @__PURE__ */ React.createElement(
      "div",
      {
        className: "p-8 text-white",
        style: {
          background: "linear-gradient(135deg, oklch(0.45 0.18 25), oklch(0.28 0.14 300))"
        }
      },
      /* @__PURE__ */ React.createElement("p", { className: "display text-xs font-semibold uppercase tracking-[0.3em]" }, "Civersify \xB7 Briefing"),
      /* @__PURE__ */ React.createElement("p", { className: "display mt-3 text-3xl" }, briefing.headline),
      /* @__PURE__ */ React.createElement("p", { className: "mt-3 text-sm leading-relaxed text-white/90" }, briefing.summary30)
    ), /* @__PURE__ */ React.createElement("div", { className: "flex items-center justify-between border-t border-[var(--border)] px-6 py-3" }, /* @__PURE__ */ React.createElement("p", { className: "text-xs font-semibold text-[var(--muted)]" }, "Share as a post \u2014 preview only"), /* @__PURE__ */ React.createElement(Button, null, /* @__PURE__ */ React.createElement(Share2, { className: "h-4 w-4" }), " Share")));
  }

  // frontend-civic/src/prototypes/magazine/components/SignInPrompt.tsx
  init_define_import_meta_env();
  function SignInPrompt({
    title = "Sign in to play",
    message = "Create a free account to manage a campaign and save your progress.",
    compact = false
  }) {
    const location = useLocation();
    const redirect2 = encodeURIComponent(location.pathname + location.search);
    return /* @__PURE__ */ React.createElement(
      "div",
      {
        className: `border border-[var(--border)] bg-[var(--bg-elev)] ${compact ? "p-4" : "p-6"} text-center`,
        "data-testid": "sign-in-prompt"
      },
      /* @__PURE__ */ React.createElement(LogIn, { className: "mx-auto h-6 w-6 text-[var(--accent)]" }),
      /* @__PURE__ */ React.createElement("p", { className: `mt-2 font-semibold text-[var(--fg)] ${compact ? "text-base" : "text-lg"}` }, title),
      /* @__PURE__ */ React.createElement("p", { className: "mt-1 text-sm text-[var(--fg-soft)]" }, message),
      /* @__PURE__ */ React.createElement("div", { className: "mt-4 flex items-center justify-center gap-2" }, /* @__PURE__ */ React.createElement(
        ButtonLink,
        {
          to: `/login?redirect=${redirect2}`,
          "data-testid": "sign-in-prompt-login"
        },
        "Sign in"
      ), /* @__PURE__ */ React.createElement(
        ButtonLink,
        {
          to: `/register?redirect=${redirect2}`,
          "data-testid": "sign-in-prompt-register",
          variant: "ghost"
        },
        "Create account"
      ))
    );
  }

  // frontend-civic/src/prototypes/magazine/components/BudgetFactCard.tsx
  init_define_import_meta_env();
  function BudgetFactCard({ fact }) {
    return /* @__PURE__ */ React.createElement("article", { className: "border border-[var(--border)] bg-[var(--bg-elev)]" }, /* @__PURE__ */ React.createElement("header", { className: "flex flex-wrap items-baseline gap-x-3 gap-y-1 border-b border-[var(--border)] px-6 py-4 md:px-8" }, /* @__PURE__ */ React.createElement("p", { className: "text-xs font-semibold uppercase tracking-[0.3em] text-[var(--accent)]" }, "Did you know?"), /* @__PURE__ */ React.createElement("p", { className: "text-xs font-semibold uppercase tracking-wider text-[var(--muted)]" }, fact.category), /* @__PURE__ */ React.createElement("h3", { className: "display w-full text-2xl md:w-auto md:flex-1 md:text-right md:text-xl" }, fact.tensionLabel)), /* @__PURE__ */ React.createElement("div", { className: "relative grid md:grid-cols-2" }, /* @__PURE__ */ React.createElement("div", { className: "p-6 md:p-8 md:pr-10" }, /* @__PURE__ */ React.createElement(Perspective, { text: fact.perspectiveA, source: fact.sourceA, url: fact.sourceUrlA })), /* @__PURE__ */ React.createElement("div", { className: "border-t border-[var(--border)] p-6 md:border-l md:border-t-0 md:p-8 md:pl-10" }, /* @__PURE__ */ React.createElement(Perspective, { text: fact.perspectiveB, source: fact.sourceB, url: fact.sourceUrlB })), /* @__PURE__ */ React.createElement("span", { className: "display absolute left-1/2 top-1/2 hidden -translate-x-1/2 -translate-y-1/2 bg-[var(--bg-elev)] px-2 py-1 text-sm font-semibold uppercase tracking-[0.2em] text-[var(--accent)] md:block" }, "But")), fact.explanation && /* @__PURE__ */ React.createElement("footer", { className: "border-t border-[var(--border)] px-6 py-3 md:px-8" }, /* @__PURE__ */ React.createElement("p", { className: "text-sm italic leading-relaxed text-[var(--fg-soft)]" }, fact.explanation)));
  }
  function Perspective({ text, source, url }) {
    return /* @__PURE__ */ React.createElement(React.Fragment, null, /* @__PURE__ */ React.createElement("p", { className: "text-base leading-relaxed text-[var(--fg)]" }, text), source && (url ? /* @__PURE__ */ React.createElement(
      "a",
      {
        href: url,
        target: "_blank",
        rel: "noopener noreferrer",
        className: "mt-3 inline-flex items-center gap-1 text-xs font-semibold uppercase tracking-wider text-[var(--muted)] hover:text-[var(--accent)]"
      },
      source,
      " ",
      /* @__PURE__ */ React.createElement(ArrowUpRight, { className: "h-3 w-3" })
    ) : /* @__PURE__ */ React.createElement("p", { className: "mt-3 text-xs font-semibold uppercase tracking-wider text-[var(--muted)]" }, source)));
  }

  // frontend-civic/src/prototypes/magazine/components/NavDropdown.tsx
  init_define_import_meta_env();
  var import_react8 = __toESM(require_react_shim(), 1);
  function NavDropdown({
    label,
    links
  }) {
    const [open, setOpen] = (0, import_react8.useState)(false);
    const ref = (0, import_react8.useRef)(null);
    const location = useLocation();
    const containsActive = links.some(
      (l) => location.pathname === l.to || location.pathname.startsWith(l.to + "/")
    );
    (0, import_react8.useEffect)(() => {
      setOpen(false);
    }, [location.pathname]);
    (0, import_react8.useEffect)(() => {
      if (!open) return;
      const onDown = (e) => {
        if (ref.current && !ref.current.contains(e.target)) setOpen(false);
      };
      const onKey = (e) => {
        if (e.key === "Escape") setOpen(false);
      };
      document.addEventListener("mousedown", onDown);
      window.addEventListener("keydown", onKey);
      return () => {
        document.removeEventListener("mousedown", onDown);
        window.removeEventListener("keydown", onKey);
      };
    }, [open]);
    return /* @__PURE__ */ React.createElement("div", { ref, className: "relative" }, /* @__PURE__ */ React.createElement(
      "button",
      {
        type: "button",
        onClick: () => setOpen((v) => !v),
        "aria-haspopup": "menu",
        "aria-expanded": open,
        "data-testid": `top-nav-${label.toLowerCase()}`,
        className: [
          "text-xs font-semibold uppercase tracking-wider transition",
          containsActive || open ? "text-[var(--accent)]" : "text-[var(--muted)] hover:text-[var(--fg)]"
        ].join(" ")
      },
      label,
      /* @__PURE__ */ React.createElement("span", { "aria-hidden": true, className: "ml-0.5 text-[0.85em] opacity-70" }, open ? "\u25B4" : "\u25BE")
    ), open && /* @__PURE__ */ React.createElement(
      "div",
      {
        role: "menu",
        "aria-label": label,
        "data-testid": `top-nav-${label.toLowerCase()}-menu`,
        className: "absolute left-0 top-full z-30 mt-2 min-w-44 border border-[var(--border)] bg-[var(--bg-elev)] py-1 shadow-lg"
      },
      links.map((l) => /* @__PURE__ */ React.createElement(
        NavLink,
        {
          key: l.to,
          to: l.to,
          role: "menuitem",
          "data-testid": `top-nav-${l.label.toLowerCase().replace(/\s+/g, "-")}`,
          className: ({ isActive }) => [
            "block px-4 py-2 text-xs font-semibold uppercase tracking-wider transition",
            isActive ? "bg-[var(--accent)]/10 text-[var(--accent)]" : "text-[var(--fg-soft)] hover:bg-[var(--border)]/40 hover:text-[var(--fg)]"
          ].join(" ")
        },
        l.label
      ))
    ));
  }

  // frontend-civic/src/prototypes/magazine/components/Flyout.tsx
  init_define_import_meta_env();
  var import_react9 = __toESM(require_react_shim(), 1);
  function Flyout({ open, onClose, title, subtitle, children }) {
    (0, import_react9.useEffect)(() => {
      if (!open) return;
      const onKey = (e) => {
        if (e.key === "Escape") onClose();
      };
      window.addEventListener("keydown", onKey);
      return () => window.removeEventListener("keydown", onKey);
    }, [open, onClose]);
    return /* @__PURE__ */ React.createElement(
      "div",
      {
        className: `fixed inset-0 z-50 ${open ? "" : "pointer-events-none"}`,
        "aria-hidden": !open
      },
      /* @__PURE__ */ React.createElement(
        "div",
        {
          onClick: onClose,
          className: `absolute inset-0 bg-black/30 transition-opacity duration-200 ${open ? "opacity-100" : "opacity-0"}`
        }
      ),
      /* @__PURE__ */ React.createElement(
        "aside",
        {
          role: "dialog",
          "aria-modal": "true",
          "aria-label": title,
          className: `absolute right-0 top-0 flex h-full w-full max-w-md flex-col bg-[var(--bg-elev)] shadow-2xl transition-transform duration-200 ease-out ${open ? "translate-x-0" : "translate-x-full"}`
        },
        /* @__PURE__ */ React.createElement("header", { className: "flex items-start justify-between gap-4 border-b border-[var(--line)] px-5 py-4" }, /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("h2", { className: "text-lg font-semibold" }, title), subtitle && /* @__PURE__ */ React.createElement("p", { className: "mt-0.5 text-xs text-[var(--muted)]" }, subtitle)), /* @__PURE__ */ React.createElement(
          "button",
          {
            onClick: onClose,
            "aria-label": "Close",
            className: "rounded-full p-1 text-[var(--muted)] hover:bg-[var(--line)] hover:text-[var(--fg)]"
          },
          /* @__PURE__ */ React.createElement(X, { size: 18 })
        )),
        /* @__PURE__ */ React.createElement("div", { className: "flex-1 overflow-y-auto px-5 py-5" }, children)
      )
    );
  }

  // frontend-civic/src/prototypes/magazine/components/BottomTabs.tsx
  init_define_import_meta_env();
  var tabs = [
    { to: "/", label: "Home", icon: House, end: true, testId: "tab-home" },
    { to: "/leagues", label: "Leagues", icon: Trophy, testId: "tab-leagues" },
    { to: "/campaigns", label: "Campaign", icon: Megaphone, testId: "tab-campaign" },
    { to: "/coalition", label: "Coalition", icon: Handshake, testId: "tab-coalition" },
    { to: "/settings", label: "Profile", icon: User, testId: "tab-profile" }
  ];
  function BottomTabs() {
    return /* @__PURE__ */ React.createElement(
      "nav",
      {
        className: "fixed inset-x-0 bottom-0 z-30 border-t border-[var(--border)] bg-[var(--bg-elev)]/95 backdrop-blur md:hidden",
        "data-testid": "bottom-tabs"
      },
      /* @__PURE__ */ React.createElement("ul", { className: "mx-auto flex max-w-md items-stretch justify-around px-2 py-1" }, tabs.map((t) => {
        const Icon2 = t.icon;
        return /* @__PURE__ */ React.createElement("li", { key: t.to, className: "flex-1" }, /* @__PURE__ */ React.createElement(
          NavLink,
          {
            to: t.to,
            end: t.end,
            "data-testid": t.testId,
            className: ({ isActive }) => [
              "flex flex-col items-center gap-0.5 py-2 text-[10px] font-semibold uppercase tracking-[0.12em] transition",
              isActive ? "text-[var(--accent)]" : "text-[var(--muted)] hover:text-[var(--fg)]"
            ].join(" ")
          },
          /* @__PURE__ */ React.createElement(Icon2, { className: "h-5 w-5" }),
          t.label
        ));
      }))
    );
  }

  // frontend-civic/src/prototypes/magazine/components/MobileMenu.tsx
  init_define_import_meta_env();
  var import_react10 = __toESM(require_react_shim(), 1);
  var import_react_dom = __toESM(require_react_dom_shim(), 1);

  // frontend-civic/src/lib/links.ts
  init_define_import_meta_env();
  var DEBATE_ARENA_URL = define_import_meta_env_default.VITE_DEBATE_ARENA_URL ?? "https://www.debatearena.fun";

  // frontend-civic/src/prototypes/magazine/nav.ts
  init_define_import_meta_env();
  var NAV_PRIMARY = [
    { to: "/", label: "Home", end: true },
    { to: "/candidates", label: "Feed" }
  ];
  var NAV_COMPETE = {
    heading: "Compete",
    links: [
      { to: "/leagues", label: "Leagues" },
      { to: "/campaigns", label: "Campaign" },
      { to: "/cohort", label: "Cohort" },
      { to: "/coalition", label: "Coalition" }
    ]
  };
  var NAV_EXPLORE = {
    heading: "Explore",
    links: [
      { to: "/quizzes", label: "Quizzes" },
      { to: "/zeitgeist", label: "Zeitgeist" },
      { to: "/briefings/who-gets-your-tax-dollar", label: "Tax Dollar" },
      { to: "/timelines/bill", label: "Bill Timeline" }
    ]
  };
  var NAV_TRAILING = [{ to: "/about", label: "About" }];
  var NAV_GROUPS = [NAV_COMPETE, NAV_EXPLORE];

  // frontend-civic/src/prototypes/magazine/components/MobileMenu.tsx
  function MobileMenu() {
    const [open, setOpen] = (0, import_react10.useState)(false);
    const location = useLocation();
    (0, import_react10.useEffect)(() => {
      setOpen(false);
    }, [location.pathname]);
    (0, import_react10.useEffect)(() => {
      if (!open) return;
      const prevOverflow = document.body.style.overflow;
      document.body.style.overflow = "hidden";
      const onKey = (e) => {
        if (e.key === "Escape") setOpen(false);
      };
      window.addEventListener("keydown", onKey);
      return () => {
        document.body.style.overflow = prevOverflow;
        window.removeEventListener("keydown", onKey);
      };
    }, [open]);
    const linkClass = ({ isActive }) => [
      "block rounded-lg px-3 py-2.5 text-sm font-semibold uppercase tracking-wider transition",
      isActive ? "bg-[var(--accent)]/10 text-[var(--accent)]" : "text-[var(--fg-soft)] hover:bg-[var(--border)]/40 hover:text-[var(--fg)]"
    ].join(" ");
    return /* @__PURE__ */ React.createElement(React.Fragment, null, /* @__PURE__ */ React.createElement(
      "button",
      {
        type: "button",
        onClick: () => setOpen(true),
        "aria-label": "Open menu",
        "aria-expanded": open,
        "data-testid": "mobile-menu-button",
        className: "inline-flex h-9 w-9 items-center justify-center rounded-full text-[var(--fg-soft)] transition hover:bg-[var(--border)]/40 hover:text-[var(--fg)] md:hidden"
      },
      /* @__PURE__ */ React.createElement(Menu, { className: "h-5 w-5" })
    ), open && (0, import_react_dom.createPortal)(
      /* @__PURE__ */ React.createElement("div", { className: "fixed inset-0 z-40 md:hidden", "data-testid": "mobile-menu-overlay" }, /* @__PURE__ */ React.createElement(
        "button",
        {
          type: "button",
          "aria-label": "Close menu",
          tabIndex: -1,
          onClick: () => setOpen(false),
          className: "absolute inset-0 bg-black/30 backdrop-blur-sm"
        }
      ), /* @__PURE__ */ React.createElement(
        "nav",
        {
          "aria-label": "All sections",
          "data-testid": "mobile-menu-panel",
          className: "absolute inset-y-0 right-0 flex w-72 max-w-[82%] flex-col border-l border-[var(--border)] bg-[var(--bg-elev)] shadow-xl"
        },
        /* @__PURE__ */ React.createElement("div", { className: "flex items-center justify-between border-b border-[var(--border)] px-4 py-3" }, /* @__PURE__ */ React.createElement("span", { className: "text-xs font-semibold uppercase tracking-[0.2em] text-[var(--muted)]" }, "Menu"), /* @__PURE__ */ React.createElement(
          "button",
          {
            type: "button",
            onClick: () => setOpen(false),
            "aria-label": "Close menu",
            "data-testid": "mobile-menu-close",
            className: "inline-flex h-8 w-8 items-center justify-center rounded-full text-[var(--fg-soft)] transition hover:bg-[var(--border)]/40 hover:text-[var(--fg)]"
          },
          /* @__PURE__ */ React.createElement(X, { className: "h-4 w-4" })
        )),
        /* @__PURE__ */ React.createElement("div", { className: "flex-1 overflow-y-auto p-2" }, NAV_PRIMARY.map((l) => /* @__PURE__ */ React.createElement(
          NavLink,
          {
            key: l.to,
            to: l.to,
            end: l.end,
            "data-testid": `mobile-menu-${l.label.toLowerCase()}`,
            className: linkClass
          },
          l.label
        )), NAV_GROUPS.map((group) => /* @__PURE__ */ React.createElement("div", { key: group.heading, className: "mt-3" }, /* @__PURE__ */ React.createElement("p", { className: "px-3 pb-1 text-[10px] font-semibold uppercase tracking-[0.2em] text-[var(--muted)]" }, group.heading), group.links.map((l) => /* @__PURE__ */ React.createElement(
          NavLink,
          {
            key: l.to,
            to: l.to,
            end: l.end,
            "data-testid": `mobile-menu-${l.label.toLowerCase().replace(/\s+/g, "-")}`,
            className: linkClass
          },
          l.label
        )))), /* @__PURE__ */ React.createElement("div", { className: "mt-3 border-t border-[var(--border)] pt-2" }, /* @__PURE__ */ React.createElement(NavLink, { to: "/settings", "data-testid": "mobile-menu-profile", className: linkClass }, "Profile & settings"), /* @__PURE__ */ React.createElement(NavLink, { to: "/profile", "data-testid": "mobile-menu-compass", className: linkClass }, "Civic Compass"), NAV_TRAILING.map((l) => /* @__PURE__ */ React.createElement(
          NavLink,
          {
            key: l.to,
            to: l.to,
            end: l.end,
            "data-testid": `mobile-menu-${l.label.toLowerCase()}`,
            className: linkClass
          },
          l.label
        ))), /* @__PURE__ */ React.createElement(
          "a",
          {
            href: DEBATE_ARENA_URL,
            target: "_blank",
            rel: "noreferrer",
            "data-testid": "mobile-menu-debate-arena",
            className: "block rounded-lg px-3 py-2.5 text-sm font-semibold uppercase tracking-wider text-[var(--fg-soft)] transition hover:bg-[var(--border)]/40 hover:text-[var(--fg)]"
          },
          "Debate Arena \u2197"
        ))
      )),
      document.body
    ));
  }

  // frontend-civic/src/prototypes/magazine/components/PlayReadToggle.tsx
  init_define_import_meta_env();
  var MAGAZINE_HOME = "/magazine";
  var PLAYER_HOME = "/";
  function PlayReadToggle({ active }) {
    const playActive = active === "play";
    return /* @__PURE__ */ React.createElement(
      "div",
      {
        className: "inline-flex items-center gap-1 rounded-full border border-[var(--border)] bg-[var(--accent)]/5 p-1",
        role: "tablist",
        "aria-label": "Play or read"
      },
      /* @__PURE__ */ React.createElement(
        Link,
        {
          to: PLAYER_HOME,
          role: "tab",
          "aria-selected": playActive,
          "aria-current": playActive ? "page" : void 0,
          className: `rounded-full px-4 py-1.5 text-[11px] font-bold uppercase tracking-[0.12em] transition ${playActive ? "bg-[var(--accent)] text-white" : "text-[var(--muted)] hover:text-[var(--accent)]"}`,
          "data-testid": "toggle-play"
        },
        "Play"
      ),
      /* @__PURE__ */ React.createElement(
        Link,
        {
          to: MAGAZINE_HOME,
          role: "tab",
          "aria-selected": !playActive,
          "aria-current": !playActive ? "page" : void 0,
          className: `rounded-full px-4 py-1.5 text-[11px] font-bold uppercase tracking-[0.12em] transition ${!playActive ? "bg-[var(--accent)] text-white" : "text-[var(--muted)] hover:text-[var(--accent)]"}`,
          "data-testid": "toggle-read"
        },
        "Read"
      )
    );
  }

  // frontend-civic/src/prototypes/magazine/components/taxApportionment/CaveatGrid.tsx
  init_define_import_meta_env();
  var CAVEATS = [
    {
      title: "Consumption \u2014 sales tax is an estimate",
      body: "Assumes a household spends a taxable 40% of income; real spending varies with income, and groceries/services/rent are often exempt."
    },
    {
      title: "Housing \u2014 property tax assumes you own",
      body: "Imputes a home worth a multiple of income; renters pay indirectly through rent, so this overstates the renter burden."
    },
    {
      title: "Local \u2014 city taxes mostly excluded",
      body: "NYC, Yonkers, and many PA/OH municipalities levy their own income taxes on top of the state."
    },
    {
      title: "Capital \u2014 investment income ignored",
      body: "Model treats all income as wages; capital gains face different federal rates, escape FICA, and are taxed differently by states."
    },
    {
      title: "Behavior \u2014 caps and credits simplified",
      body: "SALT cap, child tax credit, EITC, retirement exclusions, homestead exemptions all move real bills; omitted for legibility."
    },
    {
      title: "Employer FICA \u2014 only your half shown",
      body: "Employer's matching 7.65% never appears on the stub; counting it would raise the federal share."
    }
  ];
  function CaveatGrid() {
    return /* @__PURE__ */ React.createElement("div", { className: "grid gap-4 sm:grid-cols-2 lg:grid-cols-3", "data-testid": "tax-caveats" }, CAVEATS.map((c) => /* @__PURE__ */ React.createElement("div", { key: c.title, className: "border border-[var(--border)] bg-[var(--bg-elev)] p-5" }, /* @__PURE__ */ React.createElement("h3", { className: "display text-base font-semibold" }, c.title), /* @__PURE__ */ React.createElement("p", { className: "mt-2 text-sm leading-snug text-[var(--fg-soft)]" }, c.body))));
  }

  // frontend-civic/src/prototypes/magazine/components/taxApportionment/HouseholdCalculator.tsx
  init_define_import_meta_env();

  // frontend-civic/src/prototypes/magazine/components/taxApportionment/SplitBar.tsx
  init_define_import_meta_env();

  // frontend-civic/src/prototypes/magazine/components/taxApportionment/format.ts
  init_define_import_meta_env();
  var usd0 = new Intl.NumberFormat("en-US", {
    style: "currency",
    currency: "USD",
    maximumFractionDigits: 0
  });
  function usd(value) {
    return usd0.format(Math.round(value));
  }
  function pct(fraction) {
    return `${(fraction * 100).toFixed(1)}%`;
  }
  function pct0(fraction) {
    return `${Math.round(fraction * 100)}%`;
  }

  // frontend-civic/src/prototypes/magazine/components/taxApportionment/SplitBar.tsx
  function SplitBar({
    federalShare,
    stateShare,
    size = "lg",
    showLabels = true
  }) {
    const fed = Math.max(0, Math.min(1, federalShare));
    const state = Math.max(0, Math.min(1, stateShare));
    const height = size === "lg" ? "h-10" : "h-3";
    return /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement(
      "div",
      {
        className: `flex w-full overflow-hidden border border-[var(--border)] ${height}`,
        role: "img",
        "aria-label": `Federal ${pct0(fed)}, state and local ${pct0(state)}`
      },
      /* @__PURE__ */ React.createElement(
        "div",
        {
          className: "tax-animate flex items-center justify-start pl-2 transition-[width] duration-300 ease-out",
          style: { width: `${fed * 100}%`, backgroundColor: "var(--federal)" }
        },
        showLabels && fed > 0.12 && /* @__PURE__ */ React.createElement("span", { className: "text-xs font-semibold text-white" }, pct0(fed))
      ),
      /* @__PURE__ */ React.createElement(
        "div",
        {
          className: "tax-animate flex items-center justify-end pr-2 transition-[width] duration-300 ease-out",
          style: { width: `${state * 100}%`, backgroundColor: "var(--state)" }
        },
        showLabels && state > 0.12 && /* @__PURE__ */ React.createElement("span", { className: "text-xs font-semibold text-white" }, pct0(state))
      )
    ), showLabels && size === "lg" && /* @__PURE__ */ React.createElement("div", { className: "mt-2 flex justify-between text-xs font-semibold uppercase tracking-wider" }, /* @__PURE__ */ React.createElement("span", { style: { color: "var(--federal)" } }, "Federal"), /* @__PURE__ */ React.createElement("span", { style: { color: "var(--state)" } }, "State & local")));
  }

  // frontend-civic/src/prototypes/magazine/components/taxApportionment/StatePicker.tsx
  init_define_import_meta_env();
  function StatePicker({
    states,
    selected,
    onSelect
  }) {
    const sorted = [...states].sort((a, b) => a.name.localeCompare(b.name));
    const current = states.find((s) => s.code === selected);
    return /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement(
      "label",
      {
        htmlFor: "tax-state-select",
        className: "text-xs font-semibold uppercase tracking-wider text-[var(--muted)]"
      },
      "State"
    ), /* @__PURE__ */ React.createElement("div", { className: "mt-2 flex flex-wrap items-center gap-3" }, /* @__PURE__ */ React.createElement("div", { className: "relative" }, /* @__PURE__ */ React.createElement(
      "select",
      {
        id: "tax-state-select",
        value: selected,
        onChange: (e) => onSelect(e.target.value),
        "data-testid": "tax-state-select",
        className: "appearance-none border border-[var(--border)] bg-[var(--bg-elev)] py-2 pl-3 pr-9 text-base font-semibold text-[var(--fg)] transition hover:border-[var(--fg)] focus-visible:outline focus-visible:outline-2 focus-visible:outline-[var(--accent)]"
      },
      sorted.map((s) => /* @__PURE__ */ React.createElement("option", { key: s.code, value: s.code }, s.name))
    ), /* @__PURE__ */ React.createElement(
      "span",
      {
        "aria-hidden": true,
        className: "pointer-events-none absolute right-3 top-1/2 -translate-y-1/2 text-[var(--muted)]"
      },
      "\u25BE"
    )), current && /* @__PURE__ */ React.createElement("span", { className: "text-sm text-[var(--fg-soft)]", "data-testid": "tax-state-summary" }, /* @__PURE__ */ React.createElement("span", { "aria-hidden": true }, current.glyph, " "), "Income tax: ", /* @__PURE__ */ React.createElement("span", { className: "font-semibold" }, current.incomeSummary))));
  }

  // frontend-civic/src/prototypes/magazine/components/taxApportionment/HouseholdCalculator.tsx
  var INCOME_MIN = 2e4;
  var INCOME_MAX = 8e5;
  var INCOME_STEP = 5e3;
  function LineColumn({
    title,
    color,
    total,
    effectiveRate,
    items
  }) {
    return /* @__PURE__ */ React.createElement("div", { className: "border border-[var(--border)] bg-[var(--bg-elev)] p-5" }, /* @__PURE__ */ React.createElement("div", { className: "flex items-baseline justify-between border-b border-[var(--border)] pb-3" }, /* @__PURE__ */ React.createElement("h3", { className: "display text-lg font-semibold", style: { color } }, title), /* @__PURE__ */ React.createElement("div", { className: "text-right" }, /* @__PURE__ */ React.createElement("div", { className: "text-xl font-semibold", style: { color } }, usd(total)), /* @__PURE__ */ React.createElement("div", { className: "text-xs uppercase tracking-wider text-[var(--muted)]" }, pct(effectiveRate), " of income"))), /* @__PURE__ */ React.createElement("ul", { className: "mt-3 space-y-3" }, items.map((it) => /* @__PURE__ */ React.createElement("li", { key: it.label, className: it.strong ? "border-t border-[var(--border)] pt-3" : "" }, /* @__PURE__ */ React.createElement("div", { className: "flex items-baseline justify-between gap-3" }, /* @__PURE__ */ React.createElement("span", { className: `text-sm ${it.strong ? "font-semibold" : ""}` }, it.label), /* @__PURE__ */ React.createElement("span", { className: `tabular-nums text-sm ${it.strong ? "font-semibold" : ""}` }, usd(it.amount))), /* @__PURE__ */ React.createElement("p", { className: "mt-0.5 text-xs leading-snug text-[var(--muted)]" }, it.note)))));
  }
  function HouseholdCalculator({
    income,
    filing,
    stateCode,
    profile,
    result,
    states,
    onIncomeChange,
    onFilingChange,
    onStateChange
  }) {
    const { federal, stateLocal, combined } = result;
    const federalItems = [
      {
        label: "Federal income tax",
        amount: federal.incomeTax,
        note: "Progressive brackets on income after the standard deduction."
      },
      {
        label: "Social Security",
        amount: federal.socialSecurity,
        note: "6.2% on wages up to the $176,100 cap."
      },
      { label: "Medicare", amount: federal.medicare, note: "1.45% on all wages, no cap." },
      ...federal.addlMedicare > 0 ? [
        {
          label: "Additional Medicare",
          amount: federal.addlMedicare,
          note: `0.9% on wages over the ${filing === "mfj" ? "$250,000" : "$200,000"} threshold.`
        }
      ] : [],
      {
        label: "Federal total",
        amount: federal.total,
        note: "Income tax plus your half of FICA. Employer matches 7.65% invisibly.",
        strong: true
      }
    ];
    const stateItems = [
      {
        label: "State income tax",
        amount: stateLocal.incomeTax,
        note: profile.income.type === "none" ? "This state has no wage income tax." : "Applied to income under this state's schedule."
      },
      {
        label: "Sales tax",
        amount: stateLocal.salesTax,
        note: "Estimated: a taxable 40% of income at the combined rate."
      },
      {
        label: "Property tax",
        amount: stateLocal.propertyTax,
        note: "Imputed: a home worth a multiple of income at the effective rate."
      },
      {
        label: "State & local total",
        amount: stateLocal.total,
        note: "Income, sales, and property combined.",
        strong: true
      }
    ];
    return /* @__PURE__ */ React.createElement("div", { "data-testid": "tax-calculator" }, /* @__PURE__ */ React.createElement("div", { className: "grid gap-6 md:grid-cols-[1fr_auto] md:items-end" }, /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement(
      "label",
      {
        htmlFor: "tax-income",
        className: "flex items-baseline justify-between text-xs font-semibold uppercase tracking-wider text-[var(--muted)]"
      },
      /* @__PURE__ */ React.createElement("span", null, "Household income"),
      /* @__PURE__ */ React.createElement("span", { className: "display text-2xl text-[var(--fg)]" }, usd(income))
    ), /* @__PURE__ */ React.createElement(
      "input",
      {
        id: "tax-income",
        type: "range",
        min: INCOME_MIN,
        max: INCOME_MAX,
        step: INCOME_STEP,
        value: income,
        onChange: (e) => onIncomeChange(Number(e.target.value)),
        className: "mt-3 w-full accent-[var(--accent)]",
        "data-testid": "tax-income-slider",
        "aria-valuetext": usd(income)
      }
    ), /* @__PURE__ */ React.createElement("div", { className: "mt-1 flex justify-between text-xs text-[var(--muted)]" }, /* @__PURE__ */ React.createElement("span", null, usd(INCOME_MIN)), /* @__PURE__ */ React.createElement("span", null, usd(INCOME_MAX)))), /* @__PURE__ */ React.createElement("fieldset", null, /* @__PURE__ */ React.createElement("legend", { className: "text-xs font-semibold uppercase tracking-wider text-[var(--muted)]" }, "Filing"), /* @__PURE__ */ React.createElement("div", { className: "mt-2 inline-flex border border-[var(--border)]", role: "radiogroup", "aria-label": "Filing status" }, ["single", "mfj"].map((f) => /* @__PURE__ */ React.createElement(
      "button",
      {
        key: f,
        type: "button",
        role: "radio",
        "aria-checked": filing === f,
        onClick: () => onFilingChange(f),
        "data-testid": `tax-filing-${f}`,
        className: `px-4 py-2 text-sm font-semibold transition focus-visible:outline focus-visible:outline-2 focus-visible:outline-[var(--accent)] ${filing === f ? "bg-[var(--fg)] text-[var(--bg)]" : "text-[var(--muted)] hover:text-[var(--fg)]"}`
      },
      f === "single" ? "Single" : "Married / joint"
    ))))), /* @__PURE__ */ React.createElement("div", { className: "mt-6" }, /* @__PURE__ */ React.createElement(StatePicker, { states, selected: stateCode, onSelect: onStateChange })), /* @__PURE__ */ React.createElement("div", { className: "mt-8" }, /* @__PURE__ */ React.createElement(SplitBar, { federalShare: combined.federalShare, stateShare: combined.stateShare }), /* @__PURE__ */ React.createElement("p", { className: "mt-3 text-sm text-[var(--fg-soft)]" }, "On ", usd(income), " in ", profile.name, ", about", " ", /* @__PURE__ */ React.createElement("strong", { style: { color: "var(--federal)" } }, pct(combined.federalShare)), " of your", " ", usd(combined.total), " tax bill flows to the federal government and", " ", /* @__PURE__ */ React.createElement("strong", { style: { color: "var(--state)" } }, pct(combined.stateShare)), " stays state and local \u2014 a combined effective rate of ", pct(combined.effectiveRate), ".")), /* @__PURE__ */ React.createElement("div", { className: "mt-8 grid gap-5 md:grid-cols-2" }, /* @__PURE__ */ React.createElement(
      LineColumn,
      {
        title: "To the Federal Government",
        color: "var(--federal)",
        total: federal.total,
        effectiveRate: federal.effectiveRate,
        items: federalItems
      }
    ), /* @__PURE__ */ React.createElement(
      LineColumn,
      {
        title: `Stays in ${profile.name}`,
        color: "var(--state)",
        total: stateLocal.total,
        effectiveRate: stateLocal.effectiveRate,
        items: stateItems
      }
    )));
  }

  // frontend-civic/src/prototypes/magazine/components/taxApportionment/PonderSection.tsx
  init_define_import_meta_env();
  var PONDER_QUESTIONS = [
    "If Washington collects most of the money, should it also decide most of how it's spent \u2014 or send more back with no strings?",
    "A bridge in one state is funded by taxpayers in all fifty. When is that fair burden-sharing, and when is it one state's project on everyone's bill?",
    "Low earners often pay a larger share to state and local government. Does that change who should fund what?",
    "Would you trade a lower federal tax for a higher state one, keeping more decisions local \u2014 even if your state raises less overall?"
  ];
  function PonderSection() {
    return /* @__PURE__ */ React.createElement(
      "section",
      {
        className: "-mx-4 bg-[var(--fg)] px-4 py-12 text-[var(--bg)] md:-mx-8 md:px-12 md:py-16",
        "data-testid": "tax-ponder"
      },
      /* @__PURE__ */ React.createElement("div", { className: "mx-auto max-w-3xl" }, /* @__PURE__ */ React.createElement("p", { className: "display text-xs font-semibold uppercase tracking-[0.3em] text-[var(--bg)]/60" }, "Now the judgment"), /* @__PURE__ */ React.createElement("h2", { className: "display mt-3 text-4xl text-[var(--bg)] md:text-5xl" }, "The arithmetic is settled. The split is yours to argue."), /* @__PURE__ */ React.createElement("ol", { className: "mt-8 space-y-6" }, PONDER_QUESTIONS.map((q, i) => /* @__PURE__ */ React.createElement("li", { key: i, className: "flex gap-4" }, /* @__PURE__ */ React.createElement("span", { className: "display text-2xl text-[var(--state)]" }, i + 1), /* @__PURE__ */ React.createElement("p", { className: "text-lg leading-relaxed text-[var(--bg)]/90" }, q)))), /* @__PURE__ */ React.createElement(
        Link,
        {
          to: "/profile",
          className: "mt-10 inline-block border border-[var(--bg)]/40 px-5 py-2.5 text-sm font-semibold uppercase tracking-wider text-[var(--bg)] transition hover:bg-[var(--bg)] hover:text-[var(--fg)] focus-visible:outline focus-visible:outline-2 focus-visible:outline-[var(--state)]",
          "data-testid": "tax-ponder-profile-link"
        },
        "See how this shapes your values profile \u2192"
      ))
    );
  }

  // frontend-civic/src/prototypes/magazine/components/taxApportionment/ScalingTable.tsx
  init_define_import_meta_env();

  // frontend-civic/src/taxModel/engine/index.ts
  init_define_import_meta_env();

  // frontend-civic/src/taxModel/engine/types.ts
  init_define_import_meta_env();

  // frontend-civic/src/taxModel/engine/constants.ts
  init_define_import_meta_env();
  var SOCIAL_SECURITY_RATE = 0.062;
  var SOCIAL_SECURITY_WAGE_BASE = 176100;
  var MEDICARE_RATE = 0.0145;
  var ADDITIONAL_MEDICARE_RATE = 9e-3;
  var ADDITIONAL_MEDICARE_THRESHOLD = {
    single: 2e5,
    mfj: 25e4
  };
  var STANDARD_DEDUCTION = {
    single: 15750,
    mfj: 31500
  };
  var BRACKETS = {
    single: [
      { lower: 0, rate: 0.1 },
      { lower: 11925, rate: 0.12 },
      { lower: 48475, rate: 0.22 },
      { lower: 103350, rate: 0.24 },
      { lower: 197300, rate: 0.32 },
      { lower: 250525, rate: 0.35 },
      { lower: 626350, rate: 0.37 }
    ],
    mfj: [
      { lower: 0, rate: 0.1 },
      { lower: 23850, rate: 0.12 },
      { lower: 96950, rate: 0.22 },
      { lower: 206700, rate: 0.24 },
      { lower: 394600, rate: 0.32 },
      { lower: 501050, rate: 0.35 },
      { lower: 751600, rate: 0.37 }
    ]
  };
  var LADDER_INCOMES = [3e4, 6e4, 1e5, 175e3, 35e4, 75e4];

  // frontend-civic/src/taxModel/engine/engine.ts
  init_define_import_meta_env();
  function progressive(taxableIncome, brackets) {
    if (taxableIncome <= 0 || brackets.length === 0) return 0;
    let tax = 0;
    for (let i = 0; i < brackets.length; i++) {
      const lower = brackets[i].lower;
      if (taxableIncome <= lower) break;
      const upper = i + 1 < brackets.length ? brackets[i + 1].lower : Infinity;
      const bandTop = Math.min(taxableIncome, upper);
      tax += (bandTop - lower) * brackets[i].rate;
    }
    return tax;
  }
  function computeFederal(income, filing) {
    const inc = Math.max(0, income);
    const taxableIncome = Math.max(0, inc - STANDARD_DEDUCTION[filing]);
    const incomeTax = progressive(taxableIncome, BRACKETS[filing]);
    const socialSecurity = Math.min(inc, SOCIAL_SECURITY_WAGE_BASE) * SOCIAL_SECURITY_RATE;
    const medicare = inc * MEDICARE_RATE;
    const addlMedicare = Math.max(0, inc - ADDITIONAL_MEDICARE_THRESHOLD[filing]) * ADDITIONAL_MEDICARE_RATE;
    const fica = socialSecurity + medicare + addlMedicare;
    const total = incomeTax + fica;
    return {
      incomeTax,
      socialSecurity,
      medicare,
      addlMedicare,
      fica,
      total,
      effectiveRate: inc > 0 ? total / inc : 0
    };
  }
  function computeStateIncomeTax(income, rule) {
    const inc = Math.max(0, income);
    switch (rule.type) {
      case "none":
        return 0;
      case "flat":
        return Math.max(0, inc - rule.stdDed) * rule.rate;
      case "progressive":
        return progressive(Math.max(0, inc - rule.stdDed), rule.brackets);
    }
  }
  function computeState(income, profile) {
    const inc = Math.max(0, income);
    const incomeTax = computeStateIncomeTax(inc, profile.income);
    const salesTax = inc * profile.consumptionShare * profile.salesRate;
    const propertyTax = inc * profile.homeMultiple * profile.propRate;
    const total = incomeTax + salesTax + propertyTax;
    return {
      incomeTax,
      salesTax,
      propertyTax,
      total,
      effectiveRate: inc > 0 ? total / inc : 0
    };
  }
  function combine(income, federal, state) {
    const total = federal.total + state.total;
    return {
      total,
      effectiveRate: income > 0 ? total / income : 0,
      federalShare: total > 0 ? federal.total / total : 0,
      stateShare: total > 0 ? state.total / total : 0
    };
  }

  // frontend-civic/src/taxModel/engine/stateProfiles.ts
  init_define_import_meta_env();
  var CONSUMPTION_SHARE = 0.4;
  var STATE_PROFILES = [
    {
      code: "CA",
      name: "California",
      glyph: "\u{1F305}",
      incomeSummary: "Progressive, 1%\u201313.3% (incl. 1% MHS surcharge >$1M)",
      // State std. deduction ≈ $5,540. Top 13.3% = 12.3% + 1% Mental Health Services
      // surcharge on taxable income over $1M.
      income: {
        type: "progressive",
        stdDed: 5540,
        brackets: [
          { lower: 0, rate: 0.01 },
          { lower: 10756, rate: 0.02 },
          { lower: 25499, rate: 0.04 },
          { lower: 40245, rate: 0.06 },
          { lower: 55866, rate: 0.08 },
          { lower: 70606, rate: 0.093 },
          { lower: 360659, rate: 0.103 },
          { lower: 432787, rate: 0.113 },
          { lower: 721314, rate: 0.123 },
          { lower: 1e6, rate: 0.133 }
        ]
      },
      salesRate: 0.0882,
      consumptionShare: CONSUMPTION_SHARE,
      propRate: 68e-4,
      homeMultiple: 4.5,
      notes: "Top 13.3% rate includes a 1% Mental Health Services surcharge over $1M. No tax on Social Security benefits. Prop. 13 caps assessed-value growth, so effective rates on long-held homes run lower."
    },
    {
      code: "NY",
      name: "New York",
      glyph: "\u{1F5FD}",
      incomeSummary: "Progressive, 4%\u201310.9%",
      income: {
        type: "progressive",
        stdDed: 8e3,
        brackets: [
          { lower: 0, rate: 0.04 },
          { lower: 8500, rate: 0.045 },
          { lower: 11700, rate: 0.0525 },
          { lower: 80650, rate: 0.055 },
          { lower: 215400, rate: 0.06 },
          { lower: 1077550, rate: 0.0685 },
          { lower: 5e6, rate: 0.0965 },
          { lower: 25e6, rate: 0.109 }
        ]
      },
      salesRate: 0.0853,
      consumptionShare: CONSUMPTION_SHARE,
      propRate: 0.014,
      homeMultiple: 3.5,
      notes: "NYC and Yonkers stack a local income tax on top of the state rate \u2014 not modeled here, so a city resident's burden is higher. High property tax, especially outside NYC."
    },
    {
      code: "TX",
      name: "Texas",
      glyph: "\u{1F920}",
      incomeSummary: "None",
      income: { type: "none" },
      salesRate: 0.082,
      consumptionShare: CONSUMPTION_SHARE,
      propRate: 0.0158,
      homeMultiple: 3.2,
      notes: "No personal income tax. The state leans on some of the nation's highest property taxes and a high combined sales tax to fund itself."
    },
    {
      code: "FL",
      name: "Florida",
      glyph: "\u{1F334}",
      incomeSummary: "None",
      income: { type: "none" },
      salesRate: 0.07,
      consumptionShare: CONSUMPTION_SHARE,
      propRate: 91e-4,
      homeMultiple: 3.6,
      notes: "No personal income tax. Homestead exemption and a 'Save Our Homes' assessment cap soften property tax for primary residents. Tourism shifts some sales-tax burden onto visitors."
    },
    {
      code: "WA",
      name: "Washington",
      glyph: "\u{1F332}",
      incomeSummary: "None (7% cap-gains tax >$262k only)",
      income: { type: "none" },
      salesRate: 0.0938,
      consumptionShare: CONSUMPTION_SHARE,
      propRate: 87e-4,
      homeMultiple: 4.2,
      notes: "No wage income tax, but a 7% capital-gains tax applies above ~$262k of gains (not modeled \u2014 this calculator assumes wage income). Among the highest combined sales-tax rates in the country."
    },
    {
      code: "CO",
      name: "Colorado",
      glyph: "\u{1F3D4}\uFE0F",
      incomeSummary: "Flat 4.40%",
      // Flat 4.4% on federal taxable income — std. deduction mirrors federal $15,750.
      income: { type: "flat", rate: 0.044, stdDed: 15750 },
      salesRate: 0.0781,
      consumptionShare: CONSUMPTION_SHARE,
      propRate: 49e-4,
      homeMultiple: 4.8,
      notes: "Flat 4.4% on federal taxable income. Low effective property tax rate, but high home values raise the dollar amount."
    },
    {
      code: "PA",
      name: "Pennsylvania",
      glyph: "\u{1F514}",
      incomeSummary: "Flat 3.07%, no std. deduction",
      income: { type: "flat", rate: 0.0307, stdDed: 0 },
      salesRate: 0.0634,
      consumptionShare: CONSUMPTION_SHARE,
      propRate: 0.0149,
      homeMultiple: 2.9,
      notes: "Flat 3.07% with no standard deduction \u2014 the first dollar is taxed. Many municipalities add a local earned-income tax (~1%), not modeled here."
    },
    {
      code: "IL",
      name: "Illinois",
      glyph: "\u{1F33D}",
      incomeSummary: "Flat 4.95%, no std. deduction",
      income: { type: "flat", rate: 0.0495, stdDed: 0 },
      salesRate: 0.0886,
      consumptionShare: CONSUMPTION_SHARE,
      propRate: 0.0188,
      homeMultiple: 3,
      notes: "Flat 4.95%, no standard deduction (a small personal exemption applies instead, omitted here). Among the highest effective property tax rates in the nation. Retirement income is exempt."
    }
  ];
  var BY_CODE = new Map(STATE_PROFILES.map((s) => [s.code, s]));

  // frontend-civic/src/prototypes/magazine/components/taxApportionment/ScalingTable.tsx
  function ScalingTable({
    filing,
    profile,
    currentIncome
  }) {
    const highlighted = LADDER_INCOMES.reduce(
      (best, income) => Math.abs(income - currentIncome) < Math.abs(best - currentIncome) ? income : best
    );
    const rows = LADDER_INCOMES.map((income) => {
      const federal = computeFederal(income, filing);
      const stateLocal = computeState(income, profile);
      const combined = combine(income, federal, stateLocal);
      return { income, federal, stateLocal, combined };
    });
    return /* @__PURE__ */ React.createElement("div", { "data-testid": "tax-scaling-table" }, /* @__PURE__ */ React.createElement("div", { className: "overflow-x-auto" }, /* @__PURE__ */ React.createElement("table", { className: "w-full border-collapse text-sm" }, /* @__PURE__ */ React.createElement("thead", null, /* @__PURE__ */ React.createElement("tr", { className: "border-b-2 border-[var(--fg)] text-left text-xs uppercase tracking-wider text-[var(--muted)]" }, /* @__PURE__ */ React.createElement("th", { scope: "col", className: "py-2 pr-3 font-semibold" }, "Income"), /* @__PURE__ */ React.createElement("th", { scope: "col", className: "py-2 pr-3 text-right font-semibold", style: { color: "var(--federal)" } }, "Federal"), /* @__PURE__ */ React.createElement("th", { scope: "col", className: "py-2 pr-3 text-right font-semibold", style: { color: "var(--state)" } }, "State & local"), /* @__PURE__ */ React.createElement("th", { scope: "col", className: "py-2 pr-3 text-right font-semibold" }, "Fed. share"), /* @__PURE__ */ React.createElement("th", { scope: "col", className: "hidden py-2 font-semibold sm:table-cell" }, "Split"))), /* @__PURE__ */ React.createElement("tbody", null, rows.map((r2) => {
      const isCurrent = r2.income === highlighted;
      return /* @__PURE__ */ React.createElement(
        "tr",
        {
          key: r2.income,
          "data-testid": `tax-ladder-row-${r2.income}`,
          "aria-current": isCurrent ? "true" : void 0,
          className: `border-b border-[var(--border)] ${isCurrent ? "bg-[var(--federal-soft)]" : ""}`
        },
        /* @__PURE__ */ React.createElement("th", { scope: "row", className: "py-2.5 pr-3 text-left font-semibold tabular-nums" }, usd(r2.income), isCurrent && /* @__PURE__ */ React.createElement("span", { className: "ml-2 text-[10px] font-semibold uppercase tracking-wider text-[var(--accent)]" }, "you")),
        /* @__PURE__ */ React.createElement("td", { className: "py-2.5 pr-3 text-right tabular-nums" }, usd(r2.federal.total)),
        /* @__PURE__ */ React.createElement("td", { className: "py-2.5 pr-3 text-right tabular-nums" }, usd(r2.stateLocal.total)),
        /* @__PURE__ */ React.createElement("td", { className: "py-2.5 pr-3 text-right font-semibold tabular-nums" }, pct(r2.combined.federalShare)),
        /* @__PURE__ */ React.createElement("td", { className: "hidden w-40 py-2.5 sm:table-cell" }, /* @__PURE__ */ React.createElement(
          SplitBar,
          {
            federalShare: r2.combined.federalShare,
            stateShare: r2.combined.stateShare,
            size: "sm",
            showLabels: false
          }
        ))
      );
    })))), /* @__PURE__ */ React.createElement("p", { className: "mt-4 text-sm leading-relaxed text-[var(--fg-soft)]" }, "The federal share rises with income because the federal income tax is steeply progressive, while sales and property taxes are flat-to-regressive. At low incomes, state and local can be the larger share of the bill."));
  }

  // frontend-civic/src/prototypes/magazine/components/taxApportionment/StateCard.tsx
  init_define_import_meta_env();
  function StateCard({ profile }) {
    const stats = [
      { label: "Income tax", value: profile.incomeSummary },
      { label: "Combined sales", value: pct(profile.salesRate) },
      { label: "Eff. property rate", value: pct(profile.propRate) }
    ];
    return /* @__PURE__ */ React.createElement(
      "div",
      {
        className: "border border-[var(--border)] bg-[var(--bg-elev)] p-6",
        "data-testid": "tax-state-card"
      },
      /* @__PURE__ */ React.createElement("div", { className: "flex items-center gap-3" }, /* @__PURE__ */ React.createElement("span", { className: "text-3xl", "aria-hidden": true }, profile.glyph), /* @__PURE__ */ React.createElement("h3", { className: "display text-3xl" }, profile.name)),
      /* @__PURE__ */ React.createElement("dl", { className: "mt-5 grid gap-4 sm:grid-cols-3" }, stats.map((s) => /* @__PURE__ */ React.createElement("div", { key: s.label, className: "border-l-2 border-[var(--state)] pl-3" }, /* @__PURE__ */ React.createElement("dt", { className: "text-xs font-semibold uppercase tracking-wider text-[var(--muted)]" }, s.label), /* @__PURE__ */ React.createElement("dd", { className: "mt-1 text-base font-semibold" }, s.value)))),
      /* @__PURE__ */ React.createElement("p", { className: "mt-5 text-sm leading-relaxed text-[var(--fg-soft)]" }, profile.notes)
    );
  }

  // frontend-civic/.ds-provider.tsx
  init_define_import_meta_env();
  function DesignProvider({ children }) {
    return /* @__PURE__ */ React.createElement(MemoryRouter, null, /* @__PURE__ */ React.createElement("div", { className: "theme-magazine" }, children));
  }
  return __toCommonJS(ds_barrel_exports);
})();
/*! Bundled license information:

react-router/dist/development/chunk-4N6VE7H7.mjs:
react-router/dist/development/index.mjs:
  (**
   * react-router v7.15.1
   *
   * Copyright (c) Remix Software Inc.
   *
   * This source code is licensed under the MIT license found in the
   * LICENSE.md file in the root directory of this source tree.
   *
   * @license MIT
   *)

lucide-react/dist/esm/shared/src/utils.js:
lucide-react/dist/esm/defaultAttributes.js:
lucide-react/dist/esm/Icon.js:
lucide-react/dist/esm/createLucideIcon.js:
lucide-react/dist/esm/icons/arrow-up-right.js:
lucide-react/dist/esm/icons/bot.js:
lucide-react/dist/esm/icons/flame.js:
lucide-react/dist/esm/icons/handshake.js:
lucide-react/dist/esm/icons/highlighter.js:
lucide-react/dist/esm/icons/house.js:
lucide-react/dist/esm/icons/log-in.js:
lucide-react/dist/esm/icons/megaphone.js:
lucide-react/dist/esm/icons/menu.js:
lucide-react/dist/esm/icons/quote.js:
lucide-react/dist/esm/icons/share-2.js:
lucide-react/dist/esm/icons/thumbs-down.js:
lucide-react/dist/esm/icons/thumbs-up.js:
lucide-react/dist/esm/icons/trophy.js:
lucide-react/dist/esm/icons/user.js:
lucide-react/dist/esm/icons/x.js:
lucide-react/dist/esm/lucide-react.js:
  (**
   * @license lucide-react v0.469.0 - ISC
   *
   * This source code is licensed under the ISC license.
   * See the LICENSE file in the root directory of this source tree.
   *)
*/
window.CivicArena=CivicArena.__dsMainNs?Object.assign({},CivicArena,CivicArena.__dsMainNs,{__dsMainNs:undefined}):CivicArena;
